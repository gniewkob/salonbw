# Active Context

## Agent workflow rules

- **MANDATORY:** After every significant change, immediately update this file with: what was done, what was found, what is next.
- **MANDATORY:** Add findings/blockers to the Backlog section below as soon as they are discovered.
- **MANDATORY:** Do NOT defer documentation to end-of-session — update incrementally after each commit.
- Every session should leave this file more accurate than it found it.
- When handing off: verify the "In-progress work" branch hash matches HEAD on master.

---

## Current focus

- **Z10 DONE 2026-07-11** (branch `claude/sonnet-execution-z7-z9-je0rkj`, commity `e1a5d2f` Z10a-c + `56de8af` Z10d; panel 323/323, było 316 — +7 testów, wszystkie zweryfikowane rytuałem „test najpierw failuje, potem fix" przez git stash na fixie źródłowym). Naprawa 3 realnych bugów + paczka drobnych, znalezionych w przeglądzie Fable brancha Z7-Z9 (PR #1419). **Z10a (kolizja dialogów):** `VisitDetailsPanel` dostał prop `suspended` — gdy `true` (ustawiane w `visits.tsx` jako `confirmCancelId !== null`), panel wyłącza WŁASNY listener ESC/Tab-trap, żeby nie kolidował z `ConfirmModal` otwartym nad nim (bez tego ESC w modalu zamykał OBA dialogi, Shift+Tab w modalu był wyrywany przez trap panelu). Scroll-lock rozdzielony na osobny efekt (niezależny od `suspended` — panel ma zostać zablokowany scrollowo nawet gdy zawieszony). `ConfirmModal` teraz zapamiętuje `document.body.style.overflow` sprzed otwarcia i przywraca TĘ wartość (nie gołe `''`) — inaczej zamknięcie modala odblokowywało scroll mimo wciąż otwartego panelu pod spodem. **Z10b (focus po anulowaniu ginął po refetchu):** `cancelVisit` zapisuje id do `pendingFocusVisitIdRef`, nowy efekt na `[visits]` po refetchu szuka `#visit-{id} .salonbw-appointment-item__details-trigger` (nowa stabilna klasa na przycisku „Szczegóły") w NOWEJ sekcji (np. wizyta przeniesiona z „Nadchodzące" do „Anulowane" → wiersz remontuje się gdzie indziej) i tam przenosi focus; fallback na nagłówek strony (`ClientPageHeader` dostał opcjonalny `titleRef`+`tabIndex=-1`) gdy wiersza nie ma wcale. Test przepisany na wzorzec z Z9 (mock zwraca zmienione dane po PATCH) — **potwierdzone że STARY test maskował bug** (przechodził tylko bo mock zwracał te same dane). **Z10c (auto-scroll wątku psuł otwarcie panelu):** `MessageThread` scrollował do dołu przy KAŻDYM niepustym załadowaniu wiadomości — poprawne sprzed Z7 (wątek montował się po jawnym rozwinięciu), błędne po Z7 (wątek montuje się od razu w panelu, więc wizyta z wiadomościami odjeżdżała do dołu, walcząc z focusem na nagłówku). Fix: `hasScrolledOnceRef` resetowany per `appointmentId`, pierwsze załadowanie danego wątku pomija scroll, kolejne (np. po wysłaniu) scrollują normalnie. **Z10d (4 drobne, jeden commit):** (1) deep-link `?visitId=N` łapał `document.activeElement===body` jako cel przywrócenia focusu → dodany `lastVisitIdRef` + fallback na wiersz wizyty gdy trigger to body/null; (2) `MessageThread.sendMessage`: `focusPendingRef.current=true` przeniesiony do `finally` (przed `setSending(false)`) — refocus textarea działa też po błędzie wysyłki, nie tylko po sukcesie; (3) zaznaczanie tekstu w panelu + puszczenie myszy nad overlayem zamykało panel (click z targetem=overlay nigdy nie przechodził przez stopPropagation panelu) → zamykanie przeniesione z `onClick` na `onMouseDown` (panel stopuje propagację na własnym mousedown, decyzja zapada zanim zacznie się drag); (4) martwa reguła CSS `.visit-details-panel__section:first-of-type` (pierwszym rodzeństwem jest `__meta`, nie inny `__section`, więc reguła nigdy nie matchowała) → jawny modifier `--flush` w JSX na pierwszej sekcji. Wszystkie fixy zweryfikowane `git stash` na samym pliku źródłowym + ponowny przebieg testu (fail bez fixu, pass z fixem) — jedyny wyjątek: CSS (4) bez testu jsdom (czysto wizualne, brak sensownej asercji). tsc+eslint+prettier czyste. **Następny krok: Z11 (utwardzenie spec `visual-sweep.spec.ts` przed pierwszym dispatchem) na tym samym branchu.**

- **REVIEW FABLE brancha Z7-Z9 (PR #1419) — 2026-07-11, plan naprawczy Z10/Z11 dopisany do `docs/SONNET_EXECUTION_PLAN.md`.** Przegląd całego diffu (8 commitów, 17 plików). Werdykt: happy-path i proces solidne, ale interakcje MIĘDZY dialogami i zachowanie po refetchu nieprzemyślane; 2 testy maskują bugi klasy Z9. **3 realne bugi (Z10a-c):** (1) ConfirmModal nad VisitDetailsPanel = dwa niezależne keydown na document → ESC w modalu zamyka OBA dialogi; Shift+Tab w modalu wyrywany przez trap panelu (`!root.contains(activeElement)`); scroll-lock przecieka (modal przy zamknięciu ustawia `overflow=''` mimo otwartego panelu). (2) Focus po anulowaniu: `closeVisit()` → restore na „Szczegóły" → `load()` przenosi wizytę do sekcji „Anulowane" → wiersz REMOUNTUJE się → focus na body; test przechodzi TYLKO bo mock zwraca te same dane po PATCH (wzorzec poprawny był w teście accept — flaga `accepted`). (3) Pre-istniejący auto-scroll `bottomRef.scrollIntoView` w MessageThread: przed Z7 wątek montował się po jawnym rozwinięciu, po Z7 montuje się od razu w panelu → wizyta z ≥1 wiadomością po otwarciu odjeżdża do dołu sekcji Wiadomości (jsdom tego nie pokaże). **Z8 spec — 2 przewidywalne faile pierwszego dispatchu (Z11):** testy card-tabs (8+ nawigacji+settle+fullpage w JEDNYM teście) przekroczą domyślne 30 s; login w `beforeAll` bez `test.setTimeout(180_000)` (wzorzec z auth.setup.ts NIE przeniesiony — backoff 35 s > 30 s hooka). Plus: zrzut robiony PO asercjach (failująca trasa nie zostawia screenshota-deliverable'a), negatywne `not.toBeVisible()` podatne na strict-mode przy >1 dopasowaniu. **Drobne (Z10d):** martwa reguła CSS `__section:first-of-type` (pierwszym divem jest `__meta` → pierwsza sekcja ma niechciany border-top), deep-link `?visitId=` → triggerRef=body → focus po zamknięciu na body, refocus textarea tylko na sukces wysyłki (błąd → focus na body), mousedown-w-panelu+mouseup-na-overlayu zamyka panel (close na click zamiast mousedown), drugi login admina w card-tabs describe. **Co dobre:** disabled-focus fix w MessageThread to realne znalezisko z testem failującym przed fixem; decyzja `--project=desktop-1366`+gate VISUAL_SWEEP zweryfikowana; osobne EMPLOYEE_ROUTES; rytuały wzorowe. Ocena: 6/10 kod, 9/10 proces. **Następny krok Sonneta: Z10 → Z11 na TYM branchu, przed merge PR #1419; szczegóły+kryteria akceptacji w planie.**

- **Z8 Część A DONE 2026-07-11** (branch `claude/sonnet-execution-z7-z9-je0rkj`, commit `d173aab`; panel 316/316 niezmienione — Część A dodaje tylko spec Playwright + workflow, poza zasięgiem Jest). Nowy `apps/panel/tests/e2e/visual-sweep.spec.ts` (dispatch-only, bramkowany `VISUAL_SWEEP==='1'`, żeby żaden istniejący workflow ani lokalny `playwright test` bez filtra nie złapał go przypadkiem) + `.github/workflows/e2e-visual-sweep.yml` (`workflow_dispatch` wyłącznie, NIE na push). Sweep: admin (~58 tras statycznych: dashboard/kalendarz 4 widoki/wizyty/klienci/usługi/magazyn/sprzedaż/lojalność/komunikacja/statystyki 15 podstron/opinie/faktury/ustawienia ~20 podstron z SettingsNav+kategorie) + client (6 tras: dashboard/wizyty/rezerwacja/konto/powiadomienia/pomoc) + employee opcjonalnie gdy `E2E_EMPLOYEE_EMAIL/PASSWORD` istnieją (osobna, węższa lista tras — pracownik NIE ma dostępu do katalogu usług/magazynu/statystyk/komunikacji/większości ustawień, więc reużycie listy admina fałszywie failowałoby na `Nie masz uprawnień`), każda trasa × 2 viewporty (1366×768, 390×844) = zrzut do `screenshots/<rola>/<viewport>/<trasa>.png` (upload jako artifact). Osobno: karta klienta (8 tabów przez `?tab_name=`) i karta usługi (7 tabów przez `?tab=`) — best-effort/soft-check jak istniejące specy (pomija gdy brak wiersza do kliknięcia). Login RAZ per rola w `beforeAll` (reużywana strona; throttle 5/min po stronie backendu — pojedynczy request na rolę, nie per-trasa). **Ważna decyzja projektowa:** spec NIE dostał własnego wpisu w `playwright.config.ts` (żadnej zmiany configu) — zamiast tego workflow uruchamia go z jawnym `--project=desktop-1366` (bez tego filtra złapałyby go WSZYSTKIE 3 generyczne projekty desktop-1366/desktop-1920/mobile-390 broniące się tylko przed `regression/`, co potroiłoby logowania i zrzuty — potwierdzone przez `--list`: 474 testy bez filtra→158 z filtrem). Viewport i tak jest nadpisywany per-test przez `page.setViewportSize()`, więc wybór KTÓREGO z trzech projektów użyć jest arbitralny. **Zweryfikowane lokalnie:** `tsc --noEmit` czyste, `pnpm test` 316/316 (Jest nie dotyka tests/e2e/), `playwright test --list --project=desktop-1366` = 158 testów bez błędów składniowych, 3 skrypty `scripts/check-ops-workflows.sh`/`validate-batch-telemetry-fixtures.sh`/`check-ops-workflow-docs-consistency.sh` (CI ops-guard trafiony przez nowy plik workflow) przechodzą lokalnie. **Część B (dispatch + przegląd zrzutów + raport w Backlogu) ZABLOKOWANA w tej sesji:** `mcp__github__actions_run_trigger` na `ref=claude/sonnet-execution-z7-z9-je0rkj` zwrócił 404 — GitHub rejestruje `workflow_dispatch` tylko dla workflowów obecnych na DOMYŚLNEJ gałęzi (master); świeżo dodany plik na branchu feature nie jest jeszcze dispatchowalny przez API, nawet z jawnym `ref`. Wymaga zmergowania PR #1419 do mastera, DOPIERO wtedy dispatch zadziała. Do zrobienia po merge: dispatch → pobrać artifact `visual-sweep-screenshots` → obejrzeć KAŻDY zrzut → spisać znaleziska 🔴/🟡/🎨 do Backlogu per widok (wzorzec z treści zadania Z8 w planie).

- **Z9 DONE 2026-07-11** (branch `claude/sonnet-execution-z7-z9-je0rkj`, commity `232240a`+`bd1959c` fix literówki; panel 316/316). Audyt „gdzie ląduje focus po akcji klienta" — uogólnienie skargi z Z7. **Realny bug znaleziony w MessageThread (regresja z Z7):** `focusCompose()`/refocus po wysłaniu wiadomości wołał `.focus()` na textarea, GDY textarea było jeszcze `disabled={sending}` (element disabled nie przyjmuje focusu — cichy no-op). Fix: `focusPendingRef` + `useEffect` na `sending===false` (focus dopiero gdy DOM faktycznie się odblokuje). Złapane testem `toHaveFocus()` który celowo failował przed fixem. **VisitDetailsPanel:** akceptacja zmiany terminu odmontowuje przycisk „Akceptuj nowy termin" (status flip → `canAccept=false`) → focus spadał do `<body>`. Fix: `previousStatusRef` + `useEffect` na zmianę `visit.status` (przy otwartym panelu) → refocus nagłówka panelu. **Anulowanie przez ConfirmModal:** zweryfikowane testem (bez zmian kodu) — focus poprawnie wraca na wiersz „Szczegóły", nie do body (ConfirmModal→VisitDetailsPanel łańcuch focus-return działał już poprawnie od Z7). **Kreator rezerwacji:** `BookingStepHeader` nie miał ŻADNEGO zarządzania focusem między krokami — dodany `ref`+`tabIndex=-1` na `<h2>` + `useEffect` na `current.key` (NIE `activeStep` — alias slot/confirm w tym samym wskaźniku by nie złapał zmiany nagłówka), pomija pierwszy mount (nie kradnie focusu lądowania na stronie). `SuccessScreen` już miał poprawny focus (zweryfikowane testem, bez zmian). **`/helps/new` (HelpContactPage):** dynamicznie montowane `role="status"`/`role="alert"` węzły (potwierdzenie sukcesu + błąd wysyłki) nie są niezawodnie odczytywane przez czytniki ekranu przy insercji (live region musi istnieć PRZED zmianą treści) — dodany `ref`+`tabIndex=-1`+`focus()` na oba, co jednocześnie doscrolowuje je do widoku. Testy: `messageThread.test.tsx` +2 (focus-po-wysłaniu, `focusCompose` ref), `visitsPage.test.tsx` +1 (re-anchor po accept), `bookingPage.test.tsx` +1 (focus na każdym kroku + SuccessScreen), `helpContactPage.test.tsx` +2 (sukces+błąd). **Lekcja: `.focus()` na elemencie z `disabled={reactState}` w tym samym tick co zmiana stanu = cichy no-op — zawsze przez `useEffect` reagujący na faktyczną zmianę stanu, nie synchronicznie w handlerze.** PR #1419 (draft) zawiera Z7+Z9; CI zielone poza jednym transient failem (literówka we własnym teście `poprosię`→`poproś`, złapana przez CI, naprawiona w `bd1959c`, potwierdzona w kolejnym runie). Do zrobienia: Z8 (visual sweep — nie zaczęte w tej sesji z powodu ograniczeń zasobów/czasu).

- **Z7 DONE 2026-07-11** (branch `claude/sonnet-execution-z7-z9-je0rkj`; panel 311/311, backend 242/242 niezmienione, tsc+eslint czyste). Klient: szczegóły wizyty przeniesione z dwupoziomowego inline-expand na `/visits` do wysuwanego panelu bocznego (Booksy-style), zgodnie z decyzją projektową ownera z 2026-07-10. **Nowy komponent** `apps/panel/src/components/client/VisitDetailsPanel.tsx` — prawy slide-in (desktop 480px, pełny ekran na mobile ≤767px), `role="dialog"`+`aria-modal`+`aria-labelledby` (nagłówek), ESC zamyka, focus-trap (Tab/Shift+Tab po dynamicznie zapytanych focusable elementach), body scroll-lock. Zawartość: h2 (fokus po otwarciu, tabIndex=-1) + data/specjalista/status-badge, `RescheduleChangeNotice`, `VisitNotes` (pełne), `ClientAppointmentActions` (przeniesione z wiersza listy, teraz WYŁĄCZNIE w panelu), sekcja „Wiadomości" z przyciskiem „Napisz wiadomość"/„Odpowiedz" wywołującym nowe `focusCompose()` na `MessageThread` (forwardRef+useImperativeHandle — scrollIntoView+focus na textarea, sedno skargi ownera „ląduję nie wiadomo gdzie"), `MessageThread` bez drugiego poziomu rozwijania. Zarządzanie focusem: otwarcie → focus na h2; zamknięcie (ESC/X/backdrop) → focus wraca do triggera („Szczegóły" w wierszu, jawnie fokusowany w onClick). `/visits`: wiersz uproszczony (usunięte oba poziomy `openVisitId`+`messagesOpen` inline-expand, usunięte martwe klasy CSS visit-details-grid/wide/copy/actions). Deep-link `?visitId=N` kontrakt zachowany. Przy okazji: `btn btn-primary` (niebieski) na „Wyślij" w MessageThread → `PanelButton variant="primary"`; dodany toast sukcesu przy akceptacji nowego terminu. Testy: `visitsPage.test.tsx` przepisany (8 testów), `messageThread.test.tsx` bez zmian (7/7), Playwright `visits-client.spec.ts` +1 test („Szczegóły"→dialog+fokus, soft-check). Do zrobienia: Z8 (visual sweep), Z9 (reszta audytu focus/scroll).

- **NOWE ZADANIA Z7-Z9 W PLANIE SONNETA — 2026-07-10** (feedback ownera po deployu). Owner: lista wizyt z rozwijanymi treściami mu się nie podoba; przy dodawaniu wiadomości do wizyty jako klient „ląduje nie wiadomo gdzie" i musi szukać pola wpisywania; rozważyć modale albo panel wyjeżdżający z boku jak w Booksy. **Decyzja projektowa (podjęta): wysuwany panel boczny, NIE modal** (treść za długa na modal; spójność ze staffowym AppointmentDrawer; mobile = pełnoekranowy arkusz). Dodane do `docs/SONNET_EXECUTION_PLAN.md`: **Z7** (VisitDetailsPanel — dialog z focus-trapem, focus na textarea po „Napisz wiadomość", powrót focusu po zamknięciu, usunięcie DWÓCH poziomów inline-expand z /visits, deep-link ?visitId bez zmian), **Z8** (wizualno-funkcjonalny sweep WSZYSTKICH widoków per rola: nowy workflow `e2e-visual-sweep.yml` dispatch-only robi fullpage screenshoty 1366+390 dla admin/client przez sekrety CI → artifact → agent przegląda każdy zrzut i spisuje znaleziska 🔴/🟡/🎨 do Backlogu — sandbox nie ma kredencjali prod, dlatego zrzuty przez CI), **Z9** (audyt focus/scroll po KAŻDEJ akcji klienta: wysłanie wiadomości, akceptacja terminu, anulowanie, kroki kreatora, formularz pomocy — uogólnienie skargi ownera). Z1-Z3 oznaczone DONE w planie. Poza Fazą 4 otwarte pozostają: Z4 (wsad ownera), Z5 (zgoda ownera), Z7-Z9 (nowe, Sonnet-ready), semantyka zgód marketing-vs-transakcyjne (decyzja ownera), UPLOADS_DIR (SSH działa — terminal ownera), live mutujące E2E staff-flow (lead+owner).

- **✅ DEPLOY ODBLOKOWANY + CAŁA KOLEJKA NA PRODZIE + E2E 22/22 — 2026-07-10 ~18:50** . Owner włączył dostęp SSH w panelu MyDevil (potwierdzenie hipotezy „wygaszony dostęp SSH": klucz autoryzował, kanały odrzucane). Dispatch `deploy.yml` target=all ref=master (`223f985`): run `29115624410` **SUCCESS** w ~6 min — SSH verify ✓, build+upload landing/panel/api ✓, migracja `1762540000000` (CONSENT_UPDATED) ✓, restarty+smoke ✓. Prod zdrowy (healthz 200, DB/SMTP ok, panel 307, dev 200). Następnie dispatch `E2E Playwright Regression`: run `29116104855` **SUCCESS** — pełna suita regresyjna na żywym prodzie przeszła, W TYM test „search input exposes combobox semantics" który padał na zamrożonym prodzie → **fix `type=search→text` (`e3106c7`) potwierdzony na realnym Chromium przeciw produkcji**. Na prodzie jest teraz WSZYSTKO z kolejki: Z1 (pogrupowane dodatki + remap przy zmianie długości), Z2 (combobox-ARIA), Z3 (specy), fixy po review notes-drop, GDPR audit-trail, avatar u staffa, P3. **Bloker SSH ZAMKNIĘTY.** Incydent + lekcje opisane w `docs/OPENBRAIN_KNOWLEDGE_EXPORT.md` (baza wiedzy pod import do OpenBrain gdy konektor MCP zostanie ponownie autoryzowany — w tej sesji był odłączony). Zostaje z planu: Z4 import danych (wsad ownera), Z5 residuum magazynu (zgoda ownera), Faza 4 ownera (backup/hasło/SMSAPI/domena/Sentry), live E2E staff-flow (potwierdzenie telefoniczne, finalizacja z dodatkami — sesja staff), UPLOADS_DIR (teraz SSH działa — można sprawdzić z terminala ownera lub w następnej sesji jeśli będzie dostęp).

- **PRZEGLĄD PRACY SONNETA (Z1-Z3 + diagnostyka SSH) + 1 fix — DONE 2026-07-10** (`3e6becc`, master; panel 307/307). Ocena: **praca solidna** — Z1/Z2/Z3 wykonane zgodnie z kryteriami akceptacji planu, rytuały (testy/lint/docs po każdym zadaniu) przestrzegane, commit messages wzorowe, poprawnie rozpoznał własny bug (type=search vs role=combobox) z realnego przebiegu E2E i słusznie skorygował diagnozę czerwoności E2E na bloker deployu zamiast ścigać ją commitami. Zweryfikowane punkty ryzyka: podwójny mount omniboksu ma osobne id (`mobile-omnibox`) — brak kolizji DOM; grupowanie/auto-match dodatków poprawne; specy Playwright faktycznie read-only. **1 realny bug znaleziony w Z1 i naprawiony** (`3e6becc`): `handleSelectVariant` nie przemapowywał wybranych dodatków przy zmianie długości włosów (cofnięcie z kroku dodatków → zmiana wariantu → karta odznaczona, ale payload niósł dodatek starej długości). Fix: przemapowanie do nowej długości (drop gdy brak odpowiednika w grupie) + test regresyjny back-and-switch (313→312). Drobne noty (nieblokujące): `type="text"` traci natywny przycisk „×" czyszczenia pola search (własna obsługa Escape zostaje); heurystyka `isLikelyAddon` nadal keyword-based (zaakceptowane wcześniej).

- **DEPLOYMENT — diagnoza domknięta na ile się da zdalnie 2026-07-10:** (1) **SSH z sandboxa Claude jest NIEMOŻLIWE niezależnie od konta MyDevil** — proxy sieciowe środowiska wycisza porty non-HTTP: CONNECT:22 zwraca 200 ale zero danych (test kontrolny: `github.com:22` też bez banneru SSH, port 80 działa normalnie). Prośby „sprawdź przez ssh z lokalnego środowiska" nie da się wykonać z tej maszyny — potrzebny terminal ownera. (2) **Zawężenie przyczyny blokady:** wszystkie 3 serwisy (api healthz 200 + DB/SMTP ok, panel 307, dev 200) DZIAŁAJĄ — Passenger normalnie odpala procesy Node → konto NIE jest zawieszone i NIE wyczerpało limitu procesów. Z trzech hipotez Sonneta zostaje praktycznie jedna: **wyłączony/wygaszony dostęp SSH na koncie** (MyDevil pozwala włączać SSH czasowo — po wygaśnięciu klucz dalej przechodzi autoryzację, ale każdy kanał exec/sftp jest odrzucany — dokładnie nasza sygnatura). **Akcja dla ownera:** panel.mydevil.net → konto `vetternkraft` → sekcja SSH → włączyć dostęp (najlepiej bezterminowo); ALTERNATYWNIE z własnego terminala: `ssh vetternkraft@s0.mydevil.net` i jeśli działa (może być blokada per-klucz/IP tylko dla CI), porównać klucz z `MYDEVIL_SSH_KEY` w GH secrets. Po odblokowaniu: deploy samo-wznowi się przy najbliższym pushu (albo re-run ostatniego), migracje `1762540000000` (CONSENT_UPDATED) pojadą, a `E2E Playwright Regression` powinno dać 22/22 — sprawdzić!

- **🟡 E2E na prod „czerwone" na 1 teście omniboksu — to bloker SSH, NIE regresja kodu (skorygowana diagnoza 2026-07-10).** `E2E Playwright Regression` bije w `https://panel.salon-bw.pl` (realny prod), a `Deploy (MyDevil)` jest czerwony nieprzerwanie od `165e914` (patrz bloker SSH wyżej) — **oznacza to, że prod stoi zamrożony na ostatnim udanym deployu `8a4caf9` i NIE MA na nim ŻADNEGO z commitów Z1/Z2/Z3** (ani mojego fixu `e3106c7`). Dowód: log faila pokazuje żywy DOM z `type="search"` i pustym `role=""` — dokładnie stan SPRZED Z2, mimo że kod na `master` ma już `type="text"`+`role="combobox"` od `e3106c7`. **21/22 testów w każdym przebiegu przechodzi, bo weryfikują funkcje, które były już wdrożone PRZED zamrożeniem deployu** (stepper z `d61193a`, sekcje `visits.tsx`, badge dzwonka z `9c63882`, karta klienta — wszystko sprzed `165e914`); jedyny test bijący w kod stworzony PO zamrożeniu (mój nowy asercja `role=combobox` z Z2/Z3) nie może przejść, dopóki deploy się nie odblokuje — to nie jest coś, co da się naprawić kolejnym commitem. **Fix `type="search"→type="text"` w `SalonGlobalSearch.tsx` (`e3106c7`) POZOSTAJE zasadny** (kanoniczny typ inputa wg WAI-ARIA APG dla wzorca combobox, zero-ryzykowna zmiana, brak zależnego CSS) — ale jest **niezweryfikowany na żywym Chromium** aż do pierwszego udanego deployu po odblokowaniu SSH. **Nie ścigać dalej tej czerwoności** kolejnymi commitami — self-resolve gdy owner naprawi SSH i deploy przejdzie. **Do zrobienia przy najbliższym udanym deployu:** sprawdzić czy `E2E Playwright Regression` na najnowszym mastercie faktycznie daje 22/22 — jeśli test `omnibox.spec.ts` „search input exposes combobox semantics" dalej pada NA ŚWIEŻO wdrożonym kodzie, to dopiero wtedy realny bug wymagający dalszej diagnozy.

- **Z3 DONE 2026-07-09** (`6b3d457`, master; panel unit 306/306 niezmienione — Z3 to WYŁĄCZNIE `tests/e2e/regression/`, poza zasięgiem tsc/eslint panelu i Jest). Rozszerzone read-only specy Playwright (wzorzec skip-guard: brak `E2E_*` credentials → `test.skip`, nigdy fail) pod niedawne zmiany: `visits-client.spec.ts` (+2: sekcja „Odbyte wizyty" się renderuje; wiersze wizyt NIGDY nie pokazują kwoty — skan `.salonbw-appointments-list` pod kątem `zł`), `booking-client.spec.ts` (+2: stepper 4 kroków z `aria-current="step"` na aktywnym — sprawdzenie statyczne, zawsze pewne; best-effort sprawdzenie karty pogrupowanej z Z1 pokazującej licznik wariantów — miękkie, bo zależne od aktualnego katalogu na żywo, wzorzec jak istniejący soft-check w omniboksie), `omnibox.spec.ts` (+1: `role=combobox`→`aria-controls`→`role=listbox`→`role=option` zgodnie z Z2). **2 nowe pliki:** `topbar-notifications.spec.ts` (badge dzwonka zgodny z `data-unread_notifications`), `customer-card.spec.ts` (hero + taby in-page bez nawigacji). **Weryfikacja lokalna:** sandbox ma inną wersję Chromium niż wymaga `@playwright/test` (`browserType.launch: Executable doesn't exist` na `chromium_headless_shell-1208` vs zainstalowany `-1194`) — obejście: tymczasowy `playwright.local-verify.config.ts` z `executablePath` na lokalny binarny plik (NIE commitowany, usunięty po weryfikacji). `--list` = 22 testy w 8 plikach bez błędów składni; pełny bieg bez credentials = 21 skip + 1 pass (jedyny test niewymagający logowania), zero failów. CI (`e2e-playwright-regression.yml`) sam instaluje właściwą przeglądarkę (`playwright install chromium --with-deps`) — realny przebieg na prod nastąpi przy najbliższym pushu. **Plan Z1-Z3 UKOŃCZONY.** Zostaje: Z4 (import danych, zablokowany na wsad ownera), Z5 (residuum magazynu, za zgodą ownera), Z6 (stały nadzór nad streamem ownera — bieżące zadanie).

- **Z2 DONE 2026-07-09** (`8c0ca98`, master; panel 306/306, tsc+eslint czyste — **deploy nie odpalił, patrz bloker SSH poniżej**). Omnibox (`SalonGlobalSearch.tsx`) renderował wyniki jako zwykłe `<button>` w `<div>` bez żadnej relacji ARIA do inputa wyszukiwania — czytnik ekranu nie wiedział, że to lista podpowiedzi, ani który wynik jest aktualnie podświetlony. Dodana pełna semantyka combobox wg WAI-ARIA APG: input `role="combobox"` + `aria-autocomplete="list"` + `aria-haspopup="listbox"` + `aria-expanded` (stan otwarcia) + `aria-controls` (wskazuje na listbox TYLKO gdy istnieje — pusty stan "Brak wyników" nie ma listboxa, więc `aria-controls` jest wtedy pominięty, nie wskazuje martwego id) + `aria-activedescendant` (śledzi podświetlony wynik przy strzałkach). Kontener wyników: `role="listbox"` z `aria-label`; każda grupa (Klienci/Pracownicy/Produkty) to `role="group"` z `aria-labelledby` wskazującym na własny nagłówek (bez podwójnego odczytu tytułu). Każdy wynik: `role="option"` + stabilne `id` + `aria-selected` zsynchronizowane z `searchActive` + `tabIndex={-1}` (nawigacja strzałkami z poziomu inputa, nie osobne przystanki Tab). **Przywrócony zgubiony `onMouseEnter`** (flagowany w review batcha 07-07 jako desync hover/klawiatura) — hover teraz też ustawia `searchActive`. 4 nowe testy (`salonGlobalSearch.test.tsx`): wiring ról, activedescendant+aria-selected przez ArrowDown/ArrowUp z zawijaniem, sync na hover, pusty stan bez listboxa. Następne: Z3 (specy Playwright read-only).

- **Z1 DONE 2026-07-09** (`b8daaa0`, master; panel 302/302, tsc+eslint czyste — **deploy nie odpalił, patrz bloker SSH poniżej**). Addon-picker kreatora rezerwacji był niepogrupowany: płaskie warianty Booksy tego samego zabiegu ("Regeneracja – włosy krótkie/średnie/długie") pokazywały się jako 3 osobne karty dodatków, mimo że krok 1 (wybór usługi głównej) już grupuje te same dane przez `normalizeServicesForBooking`. `AddonStep` dostaje teraz tę samą pogrupowaną listę (`bookingServices`) co `ServiceStep`. Nowa logika: `hairLengthBucket()` (krótk/śred/dług/pasa → wspólna taksonomia z istniejącym `parseFlatVariantName`) automatycznie dopasowuje wariant dodatku do długości włosów wybranej dla usługi głównej — jeśli klientka wybrała "włosy długie" dla usługi głównej, dodatek "Regeneracja" od razu rozwiązuje się do "Regeneracja – włosy długie" (jedno kliknięcie, bez pytania powtórnie). Gdy nie ma pewnego dopasowania (usługa główna bez wariantu długości) — karta rozwija się w podlistę wariantów do wyboru (`.booking-addon-group` + zagnieżdżone `.booking-addon-card` per wariant; nowa klasa CSS w salon-shell.css). Wybór ZAWSZE rozwiązuje się do realnego, bookowalnego id usługi (`addonFromVariant`) — kontrakt payloadu `addonServiceIds`/`/calendar/available-slots` bez zmian. **2 nowe testy w `bookingPage.test.tsx`** (auto-match case + ambiguous-subpicker case), 8 istniejących testów przeszło BEZ zmiany asercji (kryterium akceptacji z planu spełnione). Następne: Z2 (combobox-ARIA omniboksu).

- **🔴 BLOKER INFRA: Deploy (MyDevil) czerwony od `165e914` (2026-07-09 ~19:18) — SSH connectivity, NIE kod.** 5 kolejnych deployów pada identycznie na kroku „Verify SSH Connectivity": `ssh -i ~/.ssh/mydevil ... vetternkraft@***` łączy się i autoryzuje (`Identity added`), ale zdalny serwer odrzuca `exec` (`exec request failed on channel 0`) — SSH handshake OK, shell/exec na koncie MyDevil zablokowany/wygaszony po stronie hostingu. **Dowód że to nie kod:** 2 z 5 failed runów (`b19941e`, `ab35b2a`) to commity zmieniające WYŁĄCZNIE plik `.md` — zero kodu, deploy i tak pada na tym samym kroku. Ostatni sukces: `8a4caf9` (2026-07-09 08:32). CI (testy) na tych samych commitach zielone. **Wymaga ownera/SSH** (poza zakresem Sonneta — sekcja 3 planu wykonawczego): sprawdzić panel MyDevil (konto zawieszone? limit procesów? zmieniony shell?), ew. zrotować `MYDEVIL_SSH_KEY` w GH secrets. Do czasu naprawy: **push na master się gromadzi, migracje NIE lecą na prod** — kolejne commity kodu (nie tylko docs) czekają w kolejce. **Stan na 2026-07-10 07:20 (~11h później): dalej czerwony, identyczny błąd** (`exec request failed on channel 0`), sprawdzone na `46048f1`/`9f20012`. **Efekt uboczny: `E2E Playwright Regression` też będzie czerwony na KAŻDYM nowym teście celującym w kod z Z1/Z2/Z3** (bo bije w żywy prod = wciąż `8a4caf9`) — to nie osobny problem, to ten sam bloker. Zobacz wpis „E2E na prod czerwone" wyżej.

  **DIAGNOZA ZAWĘŻONA 2026-07-10 07:45** (`c144b4d`, dodana diagnostyka SFTP do kroku „Verify SSH Connectivity" w `deploy.yml` — bezpieczna, read-only, nic nie zmienia na serwerze, uruchamia się TYLKO na istniejącej ścieżce błędu). Wynik: **`sftp` też pada** (`subsystem request failed on channel 0` → `Connection closed`) — to WYKLUCZA teorię „tryb SSH-tylko-SFTP" (gdyby to było to, `sftp` by przeszło, tylko `exec` by padał). Uwierzytelnienie kluczem przechodzi (`Identity added`), ale serwer odrzuca **KAŻDY** typ kanału po autoryzacji (ani exec, ani sftp) i zamyka połączenie. To odpowiada: **kontu zawieszonemu na poziomie powłoki** (klucz nadal ważny do weryfikacji, ale wszystkie operacje pokonta zablokowane), **wyczerpanemu limitowi procesów/zasobów** (sshd nie może odpalić żadnego subprocesu — ani shell, ani sftp-server), albo **aktywnej blokadzie bezpieczeństwa** po stronie hostingu. **Jednoznacznie wymaga panelu MyDevil lub kontaktu z supportem MyDevil** — sprawdzić status konta `vetternkraft` (czy jest baner/powiadomienie o zawieszeniu, limit zasobów, wygasła płatność). Diagnostyka SFTP zostaje na stałe w workflow (samo-diagnozuje się przy przyszłych awariach SSH).

- **PLAN WYKONAWCZY DLA SONNETA:** `docs/SONNET_EXECUTION_PLAN.md` (`b19941e`, 2026-07-09) — kompletna instrukcja dokończenia projektu: rytuały sesji, twarde reguły, zadania Z1-Z6 z kryteriami akceptacji (addon-picker grouping, combobox-ARIA, specy Playwright, import danych prod, residuum magazynu, nadzór nad streamem ownera), lista poza-zakresem i checklista GO. Następny agent wykonawczy zaczyna od tego pliku.

- **PRZEGLĄD BATCHA notes-drop (8 commitów ownera 07-08/09) + 2 fixy — DONE 2026-07-09** (`165e914`+`38eabc5`, master, CI push; backend 242/242, panel 300/300). Batch ownera: **kolumna `appointments.notes` USUNIĘTA z prod** (migracje `1762550000000` re-parse resztek → strukturalne pola + `1762560000000` DROP COLUMN; decyzja ownera: dane testowe, destrukcyjność OK). DTOs przemianowane: create `notes`→`clientComment`, finalize `clientNote`→`staffRecommendations`; `composeClientVisibleNotes` usunięty; versum-compat przemapowany na pola strukturalne; klient-dashboard renderuje strukturalne `VisitNotes`; omnibox dostał server-side `search`+`limit` (moje role-gating i products-bez-inactive PRZETRWAŁY). Deploy `4783106` PADŁ (metadane kolumny — TRZECI raz ta sama klasa `Date|null`/brak type w @Column; hotfix `150ffd3` rano). **Review (agent): sweep kompletny — zero referencji do usuniętej kolumny w backendzie i panelu** (pozostałe `.notes` = inne encje: time-blocks/warehouse/customer-notes). **2 realne bugi znalezione+naprawione:**
  1. `165e914` — czyszczenie komentarza klientki nie zapisywało się (`updateClientNote` przypisywał `undefined`, a TypeORM `save()` pomija undefined → stary tekst zostawał). Fix: `?? null`.
  2. `38eabc5` — **finalizacja z pustym (nieotwartym) polem zaleceń WYMAZYWAŁA istniejące `staffRecommendations`** (batch usunął fallback; modal wysyła undefined gdy puste). Fix: przywrócony fallback do bieżącej wartości. + sprzątnięte martwe `notes:` z dev-seeda.
  Drobne (nieblokujące, zanotowane): `staff-options` ma teraz implicite cap 50 bez parametrów; **WYMÓG IMPORTU DANYCH PROD ZOSTAJE:** import musi wypełniać pola strukturalne bezpośrednio (kolumny `notes` już NIE MA — parser migracji już się nie uruchomi ponownie).

- **FAZA 6.2 (GDPR audit-trail) + 6.3 (avatar u staffa) DONE 2026-07-08** (`443d2fc`+`5b66aff`, master, CI push-deploy; backend 241/241, panel 299/299).
  - **Audit-trail zgód (RODO):** każda zmiana zgody (gdpr/terms/sms/whatsapp/email/notifyPanel) trafia do logów RODO jako `CONSENT_UPDATED` z wartościami stare→nowe; dla gdpr/terms payload zachowuje POPRZEDNIĄ datę zgody zanim kolumna zostanie wyzerowana (data revoke = createdAt wpisu). Pokryte obie ścieżki: self-service (`users.updateConsent`) i edycja staff na karcie (`customers.update`). Logowanie non-fatal (try/catch — nie blokuje zapisu). **Nowa wartość enum = migracja `1762540000000` ADD VALUE** (reguła logs-action-enum-drift). LogService wpięty jako `@Optional()` w oba serwisy (nie psuje testów z mock DI); `CustomersModule` importuje `LogsModule`.
  - **Avatar klientki na karcie u staffa:** client-uploadowane avatary 404-owały (GET był self-scoped). Nowy `GET /users/:id/avatar/:fileName` (Admin/Employee/Receptionist) reużywa ten sam utwardzony helper streamujący (basename-check, Content-Type whitelist, bez statycznego katalogu); karta klienta przepisuje self-scoped URL na staff-owy (absolutne URL-e z social-login przechodzą bez zmian). Upload zostaje self-service. **Nadal do weryfikacji na MyDevil: `UPLOADS_DIR`** (czy uploady przetrwają deploy; wymaga SSH/ownera).
  - **W Fazie 6 ZOSTAJE:** 6.1 import danych prod (czeka na wsad), addon-picker kreatora pogrupowany jak krok 1 (P3), combobox-ARIA omniboksu (P3), 6.5 live E2E staff-confirm + finalizacja z dodatkami (wymaga sesji staff na prod), rozdzielenie semantyki marketing-vs-transakcyjne kolumn zgód (większa zmiana modelu — decyzja ownera).

- **FAZA 6.4 (P3 techniczne) DONE 2026-07-08** (`9c63882`, master, CI push-deploy; panel 299/299, backend 241/241). Zrobione: (1) `PanelButton` ma default `type="button"`; (2) feed powiadomień: `id*1000` → stabilne stringi `pending-`/`today-`/`client-` (typ panelu `number|string`); (3) **badge dzwonka = liczba oczekujących rezerwacji online** (usePendingBookingsCount, desktop+mobile) zamiast długości feedu udającej „nieprzeczytane"; (4) debounce 250ms na server-search listy klientów (+ mobile reset key na debounced); (5) **wspólny util `apps/panel/src/utils/date.ts`** (`toISODateLocal`/`todayISODate`) zastąpił `toISOString().slice(0,10)` w 8 plikach (StocktakingTab, DeliveriesTab, ExceptionModal, useStatistics, appointments, sales/new, deliveries/new, services-CSV; booking.tsx używa wspólnego zamiast lokalnej kopii; TimetableEmployeesPage zostaje — trick z 12:00 jest bezpieczny). **Zweryfikowane live na prod: fix P0 rejestracji działa** (POST /auth/register z `whatsappConsent` → 400 tylko o brakujące zgody, bez „property should not exist"; zero residuum). UWAGA: podczas sesji ownerowy stream wrzucił na master 2 batche (`21d3a92`…`dcfc4db`, `c092428`…`ab07f61` — reschedule UX + fixture script) — 2× rebase, testy po każdym zielone. **ZOSTAJE w Fazie 6:** 6.1 import danych prod (czeka na wsad), 6.2 GDPR audit-trail zgód, 6.3 avatar klienta u staffa (404, self-scoped GET) + UPLOADS_DIR na MyDevil, 6.4-reszta: addon-picker kreatora pogrupowany jak krok 1, combobox-ARIA omniboksu; 6.5 live E2E staff-confirm + finalizacji z dodatkami (wymaga sesji staff na prod).

- **DECYZJE WŁAŚCICIELA (5/5) WDROŻONE 2026-07-08** (`f72f00e`, master, api+panel przez CI push; backend 241/241, panel 290/290). Owner odpowiedział na wszystkie flagi z review batcha 07-07:
  1. **Staff-confirm reschedule PRZYWRÓCONY** — `rescheduled_pending→confirmed` znów dozwolone w `updateStatus` (endpoint staff-only, klient dalej ma `/accept-reschedule`); staff-confirm czyści `reschedulePrevious*`; drawer ma przycisk „Potwierdź w imieniu klientki" (btn-outline-dark), akcja confirm wróciła do ReceptionView+MobileReceptionListView; +2 testy przejść (staff-confirm, no_show z rescheduled_pending).
  2. **Dodatki online = pozycje rozliczeniowe finalizacji** — `mapAppointmentToEvent` niesie `extraServices`; FinalizationModal pre-filluje je do „Dodatkowe usługi" (billing+prowizja domyślnie, personel może skorygować/usunąć); usunięcie wszystkich wysyła JAWNIE pustą listę → backend czyści zapis (koniec cichego nadpisywania/gubienia). **Picker odchudzony wg decyzji ownera:** domyślnie 3 „Polecane zabiegi" (heurystyka addon jak w kreatorze), pełny katalog przez wyszukiwarkę lub „Pokaż wszystkie (N)"/„Zwiń listę".
  3. **Osobne zgody kanałów** — rejestracja ma osobny checkbox WhatsApp (SMS już nie podpisuje WhatsAppa); backend NIE dziedziczy `whatsappConsent` z `smsConsent` (`?? false`). Pozostałe punkty GDPR (audit-trail dat przy revoke, marketing-vs-transakcyjne znaczenie kolumn) — NIE zrobione, zostają w backlogu.
  4. **`description` zablokowane dla klienta** — usunięte z `UpdateProfileDto`, `updateProfile` i formularza `/account` (pole = notatki CRM staffu; klient ma dedykowane pola + wątek wiadomości).
  5. **Notatki po migracji split** — owner: dane na prod to dane TESTOWE, spot-check zbędny. **WYMÓG NA IMPORT DANYCH PRODUKCYJNYCH:** przy imporcie pełnych danych wypełnić `clientComment`/`staffRecommendations` bezpośrednio (NIE polegać na back-parserze — nie zna formatu `[Salon] …` i śródtekstowych `Zalecenia:`); `notes` = pole derywowane, przeliczane przez `composeClientVisibleNotes`.
  **RÓWNOLEGŁY STREAM:** w trakcie wdrażania na master weszły 4 commity (Gniewkob/Codex: `21d3a92`…`dcfc4db` — strukturalne notatki w podglądach kalendarza/karty klienta + client reschedule flow) — rebase z 1 konfliktem (union pól CalendarEvent), testy po rebase zielone. Uwaga: ten stream pracuje na tych samych plikach co my — przed każdą sesją `git pull` i sprawdzić czy nowe commity ownera nie wymagają review.

- **PRZEGLĄD BATCHA 2026-07-07 (25 commitów) + fixy po review — DONE 2026-07-08** (fixy: `dba7a33`+`839aa9d`+`7d0d878`, master `7d0d878`, wszystkie przez CI push-deploy; testy panel 290/290, backend 239/239). Batch 07-07 (autor Gniewkob/Codex, NIE był udokumentowany w tym pliku) zawiera: kreator rezerwacji 5-krokowy (warianty z grupowania płaskich usług Booksy „X – włosy Y" + dodatki `addonServiceIds`→`extraServices` z notą „do weryfikacji"), redesign karty klienta (taby in-page, hero z avatarem, usunięty ClientDetailNav), samoobsługowy profil `/account` (imię/nazwisko/adres/avatar upload — multer, 5MB, serwowane TYLKO przez autoryzowany GET self-scoped), unifikacja zgód w formularzach staff (RODO/regulamin/panel/SMS/WA/email + daty), mobile design system (`PanelButton`/`StatusBadge`/`AuthPageShell`, −1100 linii inline styles), globalna wyszukiwarka topbar (klienci+pracownicy+produkty, debounce 250ms, role-scoped), powiadomienia (dzwonek + strona `/notifications`, react-query 30s), kalendarz: mobile dostaje przewijalny grid zamiast listy, **klient-approval dla reschedule** (staff NIE może już potwierdzić za klienta; `rescheduled_pending→confirmed` tylko przez `/accept-reschedule`), kolumny `reschedulePreviousStartTime/EndTime` (porównanie „Było/Propozycja"), **split `appointment.notes`** → `clientComment`/`staffRecommendations`/`onlineAddonsSummary`/`onlineTotalDurationMinutes`/`onlineDurationNeedsVerification` (migracja `1762530000000` z back-parsingiem starych notes; `notes` zostaje jako pole derywowane). Sqlite3 6.0.1 (dependabot, major, bez lockfile) zmergowany i poprawnie zrewertowany 6 min później — drzewo bit-identyczne.
  **REVIEW (3 równoległe agenty) → NAPRAWIONE 2026-07-08:**
  - 🔴 **P0: rejestracja klientów zepsuta od `2499bda`** — panel wysyła `whatsappConsent`, `RegisterDto` go nie whitelistował, globalny `ValidationPipe(forbidNonWhitelisted)` → **400 na KAŻDEJ samodzielnej rejestracji**. Fix: pole dodane do RegisterDto (users.service już je konsumował).
  - 🔴 **Formularz pomocy = 403 dla klienta/pracownika** — `/helps/new` (linkowany z pulpitu klienta!) POST-ował na `/emails/send-auth` (`@Roles(Admin, Receptionist)`); test przechodził bo mockował apiFetch. Fix: przełączony na publiczny `/emails/contact` (odbiorca = CONTACT_RECIPIENT po stronie serwera, zero open-relay; send-auth przyjmuje dowolne `to`, więc otwarcie go klientom = spam relay), kontekst spakowany do `message`, textarea maxLength 1500 (DTO cap 2000), test przepisany na nowy kontrakt.
  - 🔴 **4 KOLEJNE niewidoczne modale** (dokładnie przewidziane w DŁUGU z 07-06): `ServiceVariantsModal` (zarządzaj wariantami), `CustomerGalleryTab` (lightbox), `CustomerSummaryTab` (dodaj do grupy), `services/new` (modal czasu) — wszystkie `modal fade in` bez `.show` → BS5 `.fade:not(.show){opacity:0}`. Fix wzorcem z 07-06 (inline display:block+opacity:1, martwe klasy usunięte). **Grep `modal fade` = 0 wystąpień w panelu.**
  - Dodatki online bez filtra — klient mógł wstrzyknąć KAŻDĄ usługę (w tym 81 legacy) jako addon → filtr `isActive+onlineBooking` w `loadAddonServices` (appointments) i `getAvailableSlots` (calendar).
  - `rescheduled_pending` miał wyjście tylko `cancelled` → klientka która nie odpisała i nie przyszła zaniżała staty no-show; dodane `no_show` do przejść.
  - Mobile client h1 `display:none` (wypadał z accessibility tree) → wzorzec sr-only.
  - Omnibox: wyniki „Pracownicy" linkują do `/settings/employees/*` (RouteGuard admin-only) = ślepy Forbidden dla employee/receptionist → grupa tylko dla admina; produkty bez `includeInactive=true`.
  - „5 warianty" → poprawny pluralizer (wariant/warianty/wariantów).
  **DECYZJE WŁAŚCICIELA (flagowane, celowo nie ruszone):**
  1. **Reschedule bez udziału klienta**: po `cdf0b79` staff nie może potwierdzić nowego terminu w imieniu klientki — dla klientek telefonicznych (bez konta) `rescheduled_pending` to dead-end (zostaje cancel/no_show/finalize). Czy przywrócić staff-confirm np. za flagą „klient potwierdził telefonicznie"?
  2. **Dodatki online a finalizacja**: extraServices z bookingu NIE są billowane/prowizjonowane przy finalizacji dopóki staff nie wpisze ich ręcznie (nota „do weryfikacji" to mityguje); a jeśli staff doda cokolwiek w modalu, dodatki online są cicho nadpisywane. Zdefiniować przepływ.
  3. **GDPR — bundling zgód**: WhatsApp consent kopiowany z checkboxa SMS przy rejestracji (brak osobnej zgody); revoke zeruje datę zgody bez śladu audytowego; te same kolumny znaczą „zgoda marketingowa" I „kanał powiadomień transakcyjnych".
  4. Klient może edytować `description` = to samo pole co notatki CRM staffu na karcie.
  5. Avatar: GET self-scoped → staff widzi 404 na karcie klienta z avatarem klienta; sprawdzić `UPLOADS_DIR` na MyDevil (czy uploady przetrwają deploy i czy katalog nie jest serwowany statycznie).
  **DO SPRAWDZENIA NA PROD (migracja już przeszła!):** back-parser `SplitAppointmentClientNotes` NIE zna formatu wątków `[Salon] …` — całe dwukierunkowe wątki wylądowały w `clientComment` (edytowalnym przez staff jako „komentarz do rezerwacji"); spot-check wierszy zawierających `[Salon]` lub śródtekstowe `Zalecenia:`.
  **POZOSTAŁE P3:** `PanelButton` bez domyślnego `type="button"` (18 call-sites przekazuje jawnie); feed powiadomień hack `id*1000` (kolizja przy #1 vs #1000) + badge = liczba pozycji, nie „nieprzeczytane"; addon-picker pokazuje płaskie warianty (niespójne z pogrupowanym krokiem 1); staff-strip kalendarza `slice(0,3)` bez „+N"; `normalizeCategoryColor` cicho nadpisuje kolor kategorii na `#6e7278` przy edycji; wzorzec `toISOString().slice(0,10)` w 5+ plikach (klasa buga UTC z `611ca89` — audyt per-case: StocktakingTab/DeliveriesTab/ExceptionModal/useStatistics/appointments); brak combobox-ARIA w omniboksie; brak debounce na server-search klientów.


- **🔴 KRYTYCZNY BUG: modal kategorii usług (i 2 inne) całkowicie niewidoczny — naprawiony 2026-07-06** (`251a4e9df`+`2132cc2a2`, deployed+zweryfikowane REALNYM kliknięciem myszy). Owner: „klikam dodaj/edytuj/usuń i nic się nie dzieje, nawet konsola nic nie pokazuje" — słusznie, bo **modal renderował się poprawnie w DOM (React state OK) ale był wizualnie niewidoczny** (nie błąd JS, więc konsola czysta). Mój WCZEŚNIEJSZY test tego dnia (element.click() przez JS) dał fałszywy pozytyw — omijał realne problemy z opacity/renderem, bo dispatchuje event bezpośrednio bez sprawdzania czy element jest faktycznie widoczny/klikalny. Lekcja: **zawsze weryfikować UI realnym myszowym `click` (mcp click na uid), nie `element.click()` w JS.** ROOT CAUSE (2 warstwy, znalezione przez computed style): (1) `className="modal fade in block bg-modal-overlay"` — `.fade`/`.in`/`.block` to relikt Bootstrap 3 (BS3 modal pokazywał się przez `.fade.in`; BS5 wymaga `.show`, którego nic nie dodawało) → `.modal{display:none}` z BS5 wygrywało, mimo custom klasy `.block`. (2) Nawet po naprawieniu display, `.fade` bez `.show` zostawia `opacity:0` na stałe → modal miał realny rozmiar (600×421px) i pozycję ale był przezroczysty. Fix: usunięte martwe klasy `fade in block`, dodany inline `style={{display:'block', opacity:1}}` (inline zawsze wygrywa z klasą, niezależnie od kolejności CSS) w **3 komponentach dzielących ten dokładny wzorzec**: `services/ManageCategoriesModal.tsx` (zgłoszony bug), `services/CategoryFormModal.tsx` (formularz tworzenia/edycji kategorii — to wyjaśnia wcześniejszą uwagę ownera „tworzenie kategorii nie działa w ogóle"), `services/ServiceFormModal.tsx`. Inne modale w repo (wzorzec `modal-backdrop`+`modal-dialog` bez klasy bazowej `.modal`) NIE są dotknięte — sprawdzone grepem przed zmianą. **Live E2E prawdziwym klikiem: otwarcie modalu (4 kategorie widoczne+akcje) → „Dodaj kategorię główną" → formularz w pełni widoczny (nazwa/opis/rodzic/kolor) → utworzono „AUDYT-realny-klik" → widoczna na liście I w filtrze sidebara → usunięcie przez ConfirmModal (wzorzec inny, działał od zawsze) → potwierdzone zniknięcie.** Panel 281/281. **DŁUG:** warto zgrepować cały panel pod kątem innych wystąpień `className="modal fade` bez sprawdzenia — dziś sprawdzone tylko 3 pliki z DOKŁADNIE tym stringiem; podobne warianty mogą się kryć gdzie indziej.


- **IA USTAWIEŃ — Faza 1 (atrapy Versum + bugi renderu) DONE 2026-07-06** (`efb4bc8f1`+`b97ecbef9`+`fd2557b35`, panel deployed+zweryfikowane live; owner: „pomieszanie z poplątaniem, atrapy Versum, klikając linki jestem przenoszony do różnych widoków"). Owner wybrał AskUserQuestion: najpierw atrapy+bugi; SMS→realna konfiguracja, Moment Pay→usuń. Zrobione: **(1)** `SmsSettingsPage` był atrapą-sklepem Versum (SMS Premium 12gr/Standard 10gr, „zamów nadpis 49 zł", martwe przyciski „nie odwzorowane z dumpa") → uczciwa konfiguracja: realne toggle (numery zagraniczne/polskie znaki/edytowalny prefix) + baner „wymaga tokena SMSAPI, respektuje zgody kanałów". Live: storefront zniknął. **(2)** `PaymentConfigurationPage` = storefront „Moment Pay" (pomarańczowe logo, marketing) → uczciwe „Płatności online" (realne przedpłaty + baner „wymaga operatora płatności"); usunięty mylący link „Napiwki"→/statistics/tips. **(3) 🔴 tabela logów RODO połamana** (komórki pionowo) — `<tr className="...row">` → Bootstrap `.row{display:flex}` robił z wiersza flex; PLUS `<table className="data_table">` → `.data_table{display:block}` psuł `display:table`. Usunięte OBA (`row` z tr, `data_table` z table). Live: `table-row`/`table-cell`, kolumny wyrównane. **(4) 🔴 grafik pracy — zdublowany pasek boczny** (`useSetSecondaryNav` W SHELLU + inline `<aside>{secondaryNav}</aside>` w treści = 2 nakładające się navy) → usunięty inline `settings-detail-layout` we WSZYSTKICH 3 gałęziach (loading/error/main; main miał inne wcięcie, wymagał osobnej naprawy + rebalans divów). Live: 1 nav, 0 inline. **(5)** `/settings/privacy` pokazywał OSOBISTĄ zgodę marketingową admina (duplikat /account) → tab informacyjny (prawa RODO + link do /account); usunięty fetch/PATCH/state; test przepisany. Panel 281/281. **LEKCJE: (a) `.row` na `<tr>` = Bootstrap flex psuje tabelę; (b) klasa scroll-wrappera `data_table` na samym `<table>` psuje display:table; (c) custom secondary nav = TYLKO `useSetSecondaryNav`, NIE renderować inline (shell ma kolumnę).** **Faza 2 IA DONE 2026-07-06** (`cb4608182`, deployed+zweryfikowane live). Podczas realizacji odkryty DODATKOWY realny bug: kafelek „kategorie usług" na `/settings` (w DWÓCH miejscach — grupa „Usługi i wizyty" ORAZ sekcja „Narzędzia") linkował do `/settings/categories` czyli do kategorii **PRODUKTÓW**; prawdziwe kategorie usług (`/settings/service-categories`) nie miały żadnego kafelka. Usunięte „Kategorie usług"/„Kategorie produktów" z (a) trwałego sidebara `SettingsNav` (ten ze zrzutów ownera — SALON→Dane salonu/Godziny/.../Kategorie usług/produktów) i (b) siatki kafelków `/settings/index.tsx` (obu wystąpień). Same podstrony (`/settings/categories`, `/settings/service-categories` + stare `/settings/categories/new`+`[id]/edit`) zostawione w kodzie nieusunięte (osiągalne tylko przez URL) — bezpieczne, nie kasuje kodu na wypadek gdyby coś jeszcze go używało. **Live: sidebar Ustawień = 0 wzmianek kategorii; /settings kafelki = 0 kategorii; `/services` „dodaj/edytuj/usuń" (z dzisiejszym reorder) nadal działa.** Analogicznie `/products` (WarehouseNav) już potwierdzone wcześniej dziś. **DŁUG:** trzy równoległe implementacje kategorii produktów istnieją w kodzie (`settings/categories.tsx` pełna strona z bulk-reorder, `settings/categories/new+edit.tsx` stare osobne strony, `salon/modals/ManageCategoriesModal.tsx` używany w WarehouseNav) — do konsolidacji w przyszłości, nieblokujące (moduł ma działającą wersję).


- **🧹 FAZA 5: FINALNE SPRZĄTANIE DANYCH TESTOWYCH DONE 2026-07-06** (`d9f3157ba`, migracja `1761310000000`, api deployed+zweryfikowane live). Owner zatwierdził zakres (AskUserQuestion: zostaw konto CI 53 + zostaw „Test tesr" 54 z jego emailem gniewko@bodora.pl). USUNIĘTE: konto „Test Klient" (49, test.klient@) + ~50 zaseedowanych wizyt + dzieci; „Pracownik (test)" (52, employee); wizyty konta e2e (53) — online_pending #101/#102 + moje audytowe wiadomości (zawyżały pending-count na pulpicie admina). ZOSTAWIONE: 29 Aleksandra, 53 e2e.client.0628 (KONTO zostaje — Playwright regression CI się nim loguje), 54 „Test tesr" (konto klienta ownera). Wzorzec: rekurencyjny FK-safe `pg_temp.cleanup_cascade_del` (schema-agnostic, RESTRICT-safe, self-ref SET NULL) — ten sam co czerwcowy QA-cleanup. **Live-verified: /employees=tylko Aleksandra, 49/52=404, 53/54=ISTNIEJE, online-pending-count=0.** **POZOSTAŁE DROBNE (magazyn, mało widoczne): residuum audytowe — produkty AUDYT/test/flow (nieaktywne), stocktaking #1, dostawy #8/#9, zamówienia #1-2, sprzedaż #9 voided — do sprzątnięcia osobną migracją jeśli owner chce czystą tabelę magazynu.**
- **🔴 BUG: stepper dni w rezerwacji nie działał (toISOString UTC shift) — naprawiony 2026-07-06** (`611ca8921`, panel deployed, zweryfikowane live). Owner (zrzut mobile): „Następny/Poprzedni dzień" nic nie robi. ROOT CAUSE: `addDaysISO`+`todayISODate` formatowały datę przez `new Date(...).toISOString().slice(0,10)` — `toISOString` konwertuje LOKALNĄ północ na UTC, więc w PL (UTC+2 latem) `2026-07-05T00:00:00` = `2026-07-04T22:00Z` → `addDaysISO('2026-07-05',+1)` zwracało `'2026-07-05'` (ten sam dzień!) → przycisk ustawiał tę samą datę = brak efektu. Dodatkowo `todayISODate()` o 00:08 lokalnie (6 lip) zwracało `'2026-07-05'` (poprz. niedziela, zamknięte) → wizard otwierał się na zamkniętym dniu. FIX: `toISODateLocal(d)` formatuje z lokalnych getterów (getFullYear/getMonth/getDate). `MonthCalendarPicker` już budował ISO lokalnie (bez zmian). **Live: dziś=6 lip pon (było 5 lip niedz), Następny 12→13, Poprzedni 19→18.** LEKCJA: do „YYYY-MM-DD z lokalnej daty" NIGDY `toISOString().slice(0,10)` — to daje UTC; użyć lokalnych getterów.


- **FIXY PO AUDYCIE — 4/5 bugów funkcjonalnych DONE 2026-07-05** (`66c39acc2`, deployed+zweryfikowane live): (1) modale lojalności (`loyalty/index.tsx`) miały `w-100` bez max-width → full-bleed przez cały ekran (wrażenie „poza ekranem") → dodane maxWidth 460/560, zweryfikowane live: karta 460px wyśrodkowana. (2) `/statistics/comments` = martwa atrapa Booksy/Moment → redirect na natywne `/reviews`, nav „Komentarze"→„Opinie klientów"→/reviews, usunięte osierocone booksy.tsx/moment.tsx; live: redirect działa. (3) Recharts −1/−1 = `RevenueChart` używał Tailwindowego `h-64` (nieistniejący w Bootstrap panelu) → kontener 0px → `style={{height:256}}` na wykresie+loading+empty; brak błędu w konsoli. (4) `/settings/privacy` pokazywał stary zlepiony „SMS/WhatsApp" → rozbite na SMS+WhatsApp osobno (whatsappConsent), spójne z /account. Panel 283/283. **(10) USTAWIENIA: martwe atrapy nawigacji USUNIĘTE + modal usług hover** (`37a6a7a7f`, deployed+live; owner „to istotny aspekt"). ŹRÓDŁO „Adres i język albo zgody a tam nic nie ma": `SettingsDetailLayout` renderuje navItems bez `href` jako `__nav-disabled` (martwe wyszarzone). Atrapy Versum: branch („Adres i język", „Zgody"), payment („Przedpłaty", „Metody płatności"), online-booking („Widoczność usług", „Widget"). → usunięte; „Zgody"→realny link `/settings/privacy`. Live: sidebar branch = tylko „Dane salonu"+„Zgody i prywatność", 0 martwych. Modal kategorii usług (`.salonbw-list-item`) już czysty (border/padding) — dodany tylko hover. **(9) DESIGN: taby lojalności + modal kategorii produktów DONE** (`d30a01706`+`fc13835d0`, deployed+live; owner: „przyciski nie wyglądają profesjonalnie, użyj skilla ui-ux"). Skill ui-ux-pro-max: **(a)** taby lojalności (Przegląd/Nagrody/...) były surowymi `<button>` bez `.btn` → dostawały beżowy UA-chrome (Bootstrap resetuje tylko `.btn`!) = „klocki". → `.loyalty-tabs/.loyalty-tab`: reset chrome, uppercase+tracking micro-type, aktywny=ciemny+dolne podkreślenie+aria-current, focus ring. „Zrealizuj kupon" → `btn btn-dark` z ikoną. **(b)** modal kategorii PRODUKTÓW: każda kategoria była zawsze-otwartym edytorem 4-polowym (zagracone) → progressive disclosure: kompaktowy wiersz (nazwa+badge nieaktywna+góra/dół/edytuj/usuń) → rozwija edytor na Edytuj (zapisz btn-dark/anuluj); `.salonbw-cat-row` z hover; labelki `form-label`→`loyalty-kpi-label` (uppercase). Live-verified: taby przezroczyste+uppercase, CTA rgb(33,37,41), modal 2 wiersze kompaktowe→rozwijany edytor. **LEKCJA: surowy `<button>` bez `.btn` = beżowy chrome systemowy; reset albo użyj `.btn`.** **(8) DESIGN LOJALNOŚCI + niebieski link DONE** (`c606e1c20`, deployed+zweryfikowane live): karty statystyk lojalności były 4× full-width pionowo (wrażenie „atrapy/pustki") → siatka KPI (`row-cols-1/2/4`) z ramką + brandowa micro-typografia `.loyalty-kpi-label` (uppercase, tracking 0.08em, srebrny #6e7278 AA), liczby ujednolicone na ciemne (usunięty text-primary); „Zasady programu"+„Nagrody" obok siebie (2-kol). Fix: „Otwórz kartę klienta" w drawerze był `btn-outline-primary` = **Bootstrap trzyma tam zaszyty niebieski** (retune `--bs-primary` NIE łapie outline-buttonów!) → `btn-outline-dark`. Live: 4 KPI w rzędzie, labelki rgb(110,114,120) uppercase. **LEKCJA: `.btn-outline-primary`/`.btn-primary` mają skompilowany niebieski, nie zależą od `--bs-primary` — używać `-dark`/`-secondary`.** **(6) KATEGORIE PRODUKTÓW — reorder DONE** (`5909fe8b8`, deployed+zweryfikowane live): `salon/modals/ManageCategoriesModal` (produkty) dostał przyciski góra/dół na kategoriach głównych (persist `useReorderProductCategories`→PATCH /product-categories/reorder, body `{items:[{id,sortOrder}]}`) obok istniejącego pola numerycznego. Live E2E: utworzenie AUDYT-A/B → reorder → B,A (200) → posprzątane. **(7) PODGLĄD OSTATNICH WIZYT W DRAWERZE — POTWIERDZONY (owner pytał „czy na pewno")**: pełny drawer wizyty (Otwórz szczegóły) pokazuje `Podgląd klienta` (liczba wizyt/no-show/wydatki/ostatnia wizyta data+godz) + `Ostatnie wizyty` (3 ostatnie completed: data+usługa+notatki). Endpoint `/customers/:id/events-history?limit=3&status=completed` zwraca `{items,total,...}`, komponent czyta `data.items` — OK. Zweryfikowane live na Wizyta #40/Test Klient: „Ostatnia wizyta 24 cze 13:00", „Ostatnie wizyty: 24 cze Fryzura wieczorowa". DROBNY DŁUG: link „Otwórz kartę klienta" w drawerze jest NIEBIESKI (brand=ciemny #1a1a1a) — do poprawy. **(5) KATEGORIE USŁUG — reorder DONE** (`ac4457d9e`, deployed+zweryfikowane live): `services/ManageCategoriesModal` dostał przyciski góra/dół na kategoriach głównych (persist przez `useReorderCategories`→PATCH /service-categories/reorder) + wyświetlanie opisu pod nazwą (formularz już go zbierał). Live E2E: Koloryzacja↑ → kolejność UI+API zmieniona → przywrócona. **ZOSTAŁO:** 🔴 modal kategorii PRODUKTÓW (`salon/modals/ManageCategoriesModal`, 462 lin.) ma tylko numeryczne pole sortOrder (nieintuicyjne) — do ujednolicenia z góra/dół; 🟡 design lojalności/kategorii (dopieszczenie); 🧹 dane testowe (Faza 5).
- **AUDYT ADMINA (live, bez fixów) 2026-07-05** — owner sfrustrowany: „bez przejścia wszystkich ścieżek nie wydajemy". Przeszedłem panel admina na prod. WYNIK (priorytety):
  - 🔴 **BUGI FUNKCJONALNE:** (1) `/statistics/comments` = martwa atrapa „Wymagana integracja z API Booksy" — bez sensu (odchodzą od Booksy, mamy natywne oceny gwiazdkowe); do usunięcia/redirect na natywne opinie. (2) Program lojalnościowy: modal „Zrealizuj kupon" renderuje się POZA EKRANEM (CSS pozycjonowanie) — nie da się zrealizować kuponu. (3) Kategorie usług+produktów: backend 100% (POST/reorder/DELETE = 201/200/200 zweryfikowane live), ale UI NIE MA kontrolki reorder mimo wsparcia backendu; produkty bez zaseedowanych kategorii („Brak kategorii"); UX = modal-w-modalu; brak kryteriów/filtrów definiowalnych (owner chce „definiować, nazywać, dawać kryteria" jak segmenty klientów). (4) Recharts `width(-1) height(-1)` w konsoli — wykres w kontenerze zerowego rozmiaru (ukryta zakładka/brak min-height). (5) `/settings/privacy` wciąż pokazuje STARY zlepiony „SMS/WhatsApp" — niespójne z nowym 4-kanałowym /account.
  - 🟡 **DESIGN „atrapa":** lojalność (przygaszone karty), modale kategorii brzydkie, ustawienia surowe.
  - 🟢 **DZIAŁA wbrew wrażeniu ownera:** usługi „edytuj" + „zarządzaj wariantami" DZIAŁAJĄ (oba otwierają modal); Dane salonu (wypełnione), Funkcje (dwubooking/same-day/reschedule toggles), Godziny, Prywatność (zgody), Grafik pracy (tydzień, Aleksandra, Edytuj/Szablony/Kopiuj) — renderują się z treścią.
  - 🧹 **DŁUG TESTOWY (Faza 5):** „Pracownik (test)" w grafiku, „Test Klient"/„Test tesr"/„E2E Klient Zmieniony" na liście klientów, moje wiadomości audytowe na #101.
  - **Decyzja ownera:** najpierw sam audyt (zrobione), Versum jako wzorzec dla designu kategorii/lojalności (dostęp obieca dać). Fixy = następny sprint, kolejność od bugów funkcjonalnych.
- **Full-session sprint — COMPLETE (2026-06-08/09)** (commits `ea63309b`–`9c875628`, master `3b1d03f5`)
  See `docs/IMPLEMENTATION_BACKLOG_STATUS.md` for full per-phase details. Summary:

  **Phase 0 — Feature hardening:**
  - `'use client'` removed from all 73 Pages Router components (was causing SSR issues; Pages Router doesn't use this directive)
  - All `window.alert()` → toast notifications (15 sites)
  - All `window.confirm()` / `window.prompt()` → `ConfirmModal` + custom modal
  - Error handling rollout: every mutation in panel has `onError` toast handler — no silent failures
  - Polish-only UI: all English strings translated across panel and landing
  - Bulk delete for services and products (with checkboxes)
  - TimeBlockModal for calendar time blocks
  - Commission base rate editor + display
  - Delivery row-level receive/cancel actions
  - Recipe tab on service detail; delete button on product detail
  - `/settings/customer-origins`, `/settings/data-protection` pages added
  - Account page: profile section added; API module English toasts removed
  - `rel="noopener noreferrer"` on all `target="_blank"` links

  **Phase 1 — a11y: Interactive elements:**
  - `href="#"` / `javascript:;` → `<button type="button">` everywhere
  - `type="button"` added to 56+ buttons missing it (prevents form submission)
  - SalonBreadcrumbs: `<nav aria-label="Nawigacja">` + `aria-current="page"`

  **Phase 2 — a11y + SEO: Titles + meta:**
  - Page-specific `<title>` on ALL 44+ panel pages; default fallback in `_app.tsx`
  - Custom 404 page for panel
  - Full OG/Twitter/JSON-LD meta on all landing pages; `og:locale=pl_PL`, dimensions, canonical with `absUrl()`
  - `theme-color` + Twitter card; JSON-LD Organization schema (services, gallery, contact)
  - Missing `<meta name="viewport">` fix in landing

  **Phase 3 — a11y: Form semantics:**
  - `htmlFor`/`id` linkage on ALL labels across all modals and forms
  - `aria-label` on all icon-only buttons and controls without visible labels
  - `aria-hidden="true"` on 25+ decorative icons (FontAwesome + SVGs)
  - `role="alert"` / `role="status"` on error/warning/success messages
  - `aria-describedby` on hint-associated inputs
  - `autoComplete` attributes on customer form inputs

  **Phase 4 — a11y: Modal dialogs + nav:**
  - `role="dialog"` + `aria-modal="true"` + `aria-labelledby` on ALL modals (3 batches)
  - Focus trap + ESC handler + focus restoration on close

  **Phase 5 — a11y: Tables + pagination + nav:**
  - `scope="col"` on ALL `<th>` in panel
  - Pagination: `<nav aria-label="Paginacja">` on all paginated pages
  - `aria-current="page"` on active nav items in ALL navigation components
  - Non-interactive `<a>` → `<span>`; `<a onClick>` → `role="button"` + keyboard nav

  **Phase 6 — Route integrity + calendar:**
  - Legacy rewrites fixed; time-block validation hardened; calendar overlap queries fixed

- **Bootstrap 5 migration — COMPLETE (2026-03-27)**
- **Faza E — Versum visual parity sprint — COMPLETE (2026-05-24)**
- **Core booking/appointment flow — COMPLETE (2026-05-24)**
  - Client booking wizard `/booking` (3-step: service → slot → confirm) — implemented
  - `online_pending` + `rescheduled_pending` statuses — API + DB migration + UI
  - Available slots endpoint `GET /calendar/available-slots` — implemented
  - Formula service bug fixed (admin 403 + confirmed-status 400)
  - FinalizationModal: usageMaterials (from recipe) + usageItems (manual) + deduction — all wired
  - AppointmentDrawer: formula UI, internalNote, client contact (tel:/mailto:), visit history
  - Online pending badge in topbar — implemented
- **Dead code cleanup — COMPLETE (2026-05-25)** (PR #1352)

---

## In-progress work

- Branch: master (latest commit `38eabc5` — fixy po przeglądzie notes-drop; api+panel deployed via CI push 2026-07-09)
- Panel production: `64abb87ad` content — DEPLOYED 2026-06-10 (strict CSP without unsafe-eval verified on prod)
- Landing production: `0fb490ec3` — DEPLOYED 2026-06-10 (run `27282009631`, success; verified: ticker gone, stats strip + MapFacade + reveal-item live)
- **Landing modernization plan (Fazy 0–4)**: Faza 0 DONE (home 10→8 sections, strips consolidated, CTAs reduced, content-visible reveals, map facade; Lighthouse mobile 100×4). Faza 1 DONE 2026-06-10 (`7dc36d1d5`): Consent Mode v2 basic — gtag mounts only post-consent, CookieConsent banner pl/en/de, _ga* cleanup on decline, 4-case test suite; verified live in prod bundle (`sbw-consent` in _app chunk). **FINDING: CI never injects NEXT_PUBLIC_GA_ID/ENABLE_ANALYTICS → prod analytics was and is OFF; banner appears only once owner provides a GA4 id to the deploy build env.** Faza 2 DONE 2026-06-10 (`23bf257c4` + redeploys `27299302570`/`27299559832`): perf trace mobile Fast-4G 4xCPU → LCP 397ms, TTFB 26ms, CLS 0.00 (all "good"); root fix = deploy no longer hard-copies .next/static into public/_next, so Node serves /_next/static with `immutable` (was TTL 0 via nginx bypass — every visit re-downloaded all JS/CSS/fonts on BOTH frontends). Known limitation (P3): /images/* still nginx-served with no Cache-Control (etag/304 revalidation only) — would need image-optimizer routing or MyDevil nginx config. Faza 3 DONE 2026-06-10 (`33425440c`, deploy `27302862108`): public anonymized `GET /calendar/nearest-slot` (no auth by design — single timestamp only, Throttler-limited, 2-min cache, +1h floor, 14-day scan over shortest active service) + hero teaser 'Najbliższy wolny termin: czw., 11 cze, 09:00' (verified live, pl/en/de, hides on error) + HairSalon JSON-LD aggregateRating 5.0/4 reviews from on-page testimonials (verified in prod DOM). FAQPage schema skipped on purpose — no FAQ content on site, schema without visible content violates Google guidelines. **MVP Booking runbook** (`docs/MVP_BOOKING_RUNBOOK.md`): Dzień 1 (L1 sloty×godziny pracy) DONE 2026-06-10 `aa66337d4` — API live, fallback pon–sob 9–19/niedziela zamknięta do czasu wpisania realnych godzin w panelu (Dzień 3). Dzień 1b DONE 2026-06-10 (`4df90ca35`): godziny salonu dynamicznie z grafiku pracownika — precedencja w slotach (grafik wygrywa nad branchem, zaplanowana niedziela bookowalna), publiczny `GET /calendar/opening-hours` (unia grafików), landing renderuje godziny live na 5 powierzchniach (hero/footer/navbar/kontakt×2) ze statycznym fallbackiem; prod zweryfikowany: „Pn–So 09:00 - 17:00" z realnego timetable. Known drift: JSON-LD openingHours statyczne. CI: backend smoke retry rozszerzony (`9e3a9584c`, 8×capped-backoff) po drugim false-fail na oknie 403 Passengera. Dzień 2 DONE 2026-06-11 (`2ab5af17e`, deploy `27309782335` success): L2 e-mail do salonu przy samodzielnej rezerwacji klientki (EmailsService, odbiorca BOOKING_ALERT_EMAIL default kontakt@salon-bw.pl, treść PL z linkiem do panelu, błąd wysyłki nie blokuje rezerwacji, wpis w email_logs; WhatsApp do pracownika zostaje drugim kanałem). L3 WYCOFANA — audyt: usePendingBookingsCount polluje refetchInterval=2min od zawsze, runbook się mylił. Testy 234/234. Next: Dzień 3 = konfiguracja danych przez właścicieli (usługi, konto pracownika, grafik — godziny brancha już zbędne), Dzień 4 = E2E (w tym pierwszy realny test maila L2). Faza 4 landing (optional polish) = view transitions, dark-mode var audit, bento grid
- **Landing IA alignment to LANDING_DESIGN_DIRECTION DONE 2026-06-14** (`8b88796c1` footer, `4ce08dc60` home): owner felt landing "miał inaczej wyglądać". **Key infra fact (re)confirmed: the modern landing only deploys to `dev.salon-bw.pl` (CI var `MYDEVIL_PUBLIC_APP_NAME`=dev); the public `salon-bw.pl` is a SEPARATE legacy non-Next site that CI never touches — intentional, not yet cut over (owner: stary salon-bw.pl zostaje, landing testujemy na dev).** Per the design doc cut from home: removed `TrustStrip` (stats 15/3000/4.9/30 + partner-brand strip on warm-cream — the main "over-designed/try-hard" signal), trimmed `AboutSpread` to a short founder note (dropped 3-principles grid), removed the duplicated opening-hours block from the footer (hours live in the Kontakt section + /contact; footer now clean 2-col). Testimonials KEPT (already adjacent to gallery + the on-page reviews back the HairSalon `aggregateRating` JSON-LD — removing them would orphan the schema like the FAQ case). PartnerBrands marquee NOT reintroduced (motion anti-pattern); optional follow-up = static brand strip on /services. Verified live on dev (desktop+390 mobile: no beige strip, no h-scroll; HTML: TrustStrip aria-label absent, footer "Godziny" label gone). Remaining doc items not done (owner decision): hero floating-card hours still present on mobile, cennik widełkowy on /services, multilingual cut-vs-translate, palette A(true mono) vs B(warm-as-name).
- **Project skill `.claude/skills/salonbw-brand/`** (versioned) — B&W design tokens, contrast table, motion/CTA rules; load for any UI work
- API production: `3a6ad7d77` — DEPLOYED 2026-06-10 (dispatch run `27266949144`, success; migrations ran; verified: healthz ok, /calendar/available-slots returns 401 not 404)
- **Dzień 3 (dane Booksy) DONE 2026-06-14** — Versum+Booksy wyłączone; właściciel dał wsad z Booksy (zrzut grafiku Aleksandry + PDF cennika). Zaimplementowane migracjami (API prod, HEAD `8c6cf004f`):
  - `1760980000000-SeedBooksyCatalogAndHours` — 4 kategorie + 60 płaskich usług (27 rodziców Booksy × warianty długości włosów; booking nie ma pickera wariantów, więc każdy wariant = osobny Service z własnym czasem/ceną), przypisane do Aleksandry, `onlineBooking=on`; placeholder-grafik 9–17 podmieniony na realny (Pon 9–16, Wt 12–19, Śr wolne, Czw 12–19, Pt 9–16, So 9–13, Nd wolne).
  - `1760990000000-CleanupLegacyServicesBooksyOnly` — ~64 stare usługi sprzed Booksy (duplikaty „X"/„X Ola", literówki) → `onlineBooking=false` (zostają isActive do historii/statystyk, do włączenia w panelu); kanonizacja 60 (naprawa 12 kolizji nazw: Fryzura ślubna 150→280, Olaplex 30→60min).
  - `1761000000000-DedupeBookingServices` — legacy miał zduplikowane nazwy → window `row_number()` zostawia min(id) per nazwa bookowalny (zachowuje linki historyczne), resztę hide.
  - **Zweryfikowane live:** `/services/public` onlineBooking=true = **dokładnie 60** (27/21/10/2), zero dup nazw; `/calendar/opening-hours` = realny grafik; healthz ok.
  - Landing (`5053defbb`, deploy run `27477653998`): JSON-LD `openingHoursSpecification` (index+contact) + `BUSINESS_INFO.hours` fallback dopasowane do realnego grafiku (Pon+Pt 9–16, Wt+Czw 12–19, So 9–13). **Known drift JSON-LD openingHours — RESOLVED.**
  - **NOWY DŁUG CI (do naprawy):** krok „Run DB migrations" w deploy.yml = `node dist/src/migrate.js || node dist/migrate.js`; gdy primary padnie (exit 1), `||` odpala fallback `dist/migrate.js`, który kończy 0 → deploy fałszywie „success", maskuje błąd migracji (pierwszy seed `inconsistent types deduced for parameter $1` przeszedł jako success, dane się nie zapisały). Fix: `if [ -f dist/src/migrate.js ]; then …; else …; fi` zamiast `||`.
  - Lekcja: idempotencja-po-nazwie na brudnym katalogu (duplikaty, kolizje) jest zawodna — patrz pamięć [[seed-migration-dirty-catalog]].
- **Dzień 4 (E2E na prod) DONE 2026-06-14** — pełny happy-path przeszedł na żywo, sterowany przez API (cookie jar + `X-XSRF-TOKEN`; CSRF middleware obejmuje wszystkie POST `forRoutes('*')` poza listą, nie zwalnia Bearera; login zwraca `access_token` w body + cookies):
  - Rejestracja klienta (`/auth/register`, tylko rola Client) → `/services/online-booking` zwraca **60** pogrupowane → `/calendar/available-slots` realny grafik (Pon 14 slotów 9:00–15:30) → POST `/appointments` → `online_pending` (#29) → **mail L2 WYSŁANY** (`email_logs`: kontakt@salon-bw.pl, „Nowa rezerwacja online — 2026-06-15 13:30", status sent) → admin PATCH `/appointments/29/status confirmed` → POST `/29/finalize` (cash 3000 gr) → `completed` → konflikt: ponowna rezerwacja tego slotu = **409 „Employee is already booked for this time"**.
  - Pierwszy realny test L2 = ✅ (potwierdzony w email_logs, nie tylko unit-mock).
  - Konta uprzywilejowane: rejestracja daje tylko Client, więc admin/employee przez migrację `1761010000000-CreateE2eTestAccounts` (bcrypt hash w pliku, hasła poza repo). **users.role to natywny enum `users_role_enum`** (NIE varchar jak priceType) → INSERT wymaga `$n::"users_role_enum"`.
  - **Potwierdzenie, że fix `migrate.js` działa:** gdy ta migracja miała błąd (enum), deploy POPRAWNIE padł (exit 1) zamiast zamaskować — wcześniejszy `|| fallback` by to ukrył.
  - Sprzątanie (brak DELETE w API → migracje): `1761020000000-CleanupE2eTestArtifacts` (appt #29 + dzieci + log L2 + 3 konta e2e) i `1761030000000-CleanupVerificationAccount` (throwaway vcheck.*). Wzorzec: dynamiczny `DO`-block po `information_schema` (tylko BASE TABLE) — odporny na FK RESTRICT, bez hardkodowania nazw tabel. Zweryfikowane live: e2e.admin login=401, slot 15.06 13:30 wolny, katalog=60.
  - Lekcja zapisana: [[e2e-flow-driving-via-api]] (CSRF/enum/cleanup-via-migration).
- MVP booking — **wszystkie 4 dni DONE**. Pozostałe drobne: konto pracownicze Aleksandry (id 29) działa (ma usługi + grafik, dostaje rezerwacje); WhatsApp do klienta wysyłany przy confirm (nie zweryfikowany na realnym numerze — testowy był fałszywy).

---

## Backlog — open findings / next tasks

### P1 — Blockers
- _(none open)_
- ~~API deploy needed~~ — DONE 2026-06-10: dispatch run `27266949144` (success) shipped API at `3a6ad7d77` with online_pending migration, available-slots endpoint, formula fix; healthz verified ok
- ~~Panel redeploy needed~~ — DONE 2026-06-10: push deploy `27266421553` shipped panel + landing at `21efc2459`

### P2 — Accessibility (remaining — all other a11y DONE in full-session sprint)
- ~~Color contrast audit~~ — DONE 2026-06-10 (`2b57d0c12`, `64abb87ad`): Lighthouse run on landing (/, /services, /contact, /gallery, /services/balayage) + panel /auth/login — all now score a11y 100. Fixed: white-on-silver CTA → dark-on-silver; new `--brand-silver-ink` (#6e7278) for silver text on light bg; `--brand-warm-label` darkened; low-alpha white text on dark raised to 0.55; slider dots got 24px touch targets; login got `<main>` landmark. ~~Panel authenticated pages NOT audited~~ — DONE 2026-06-21 (logged-in Lighthouse as test.admin): `/dashboard` 95→**100** (`dc2af6aa7`: silver `#b4b8be`/`#7d848c` text → `#6e7278` at appointment-time/section-footer/sidebar nav-header; KPI stat-card titles h3→h2 to fix h1→h3 skip) + `/customers` 93→**100** (`a6fca9a6b`+`f8ab3a615`: export-button outline-secondary #6c757d→#5a6470 via --bs-btn vars on `.products-export`; row/header select checkboxes 13px→24px w/ 2px vertical margin, scoped to td/th). All deployed + re-audited live=100. **SEO 57 on panel is CORRECT/desired** — panel is auth-gated `noindex` (is-crawlable "blocked" + no meta-description behind login are intentional, not bugs).
- ~~Toast aria-live~~ — DONE 2026-06-10 (`4d016e554`): error toasts now `role=alert` + `aria-live=assertive`; success keeps react-hot-toast default polite status
- ~~Landing image alt text~~ — AUDITED 2026-06-10, no gaps: all content images have descriptive Polish alts in `content.ts`; decorative bg images (BookingCta, ServicesTeaser) correctly use `alt=""` + `aria-hidden`
- ~~Focus-ring CSS audit~~ — AUDITED 2026-06-10, no gaps: every `outline: none` in salon-shell.css pairs with border-color + box-shadow focus style; global `:focus-visible` rule (line ~8247) gives 2px accent outline to buttons/links/tabindex

### P3 — Code quality
- ~~`data_protection.tsx` inner `edit_branch_form` nested form~~ — STALE (2026-06-20): page has a single well-formed `<form>` (81-181), "Logi i audyt" is outside it; no nested form, no `edit_branch_form`. Nothing to fix.
- ~~`DashboardLayout` dead code~~ — STALE (2026-06-20): file + all references already gone from the repo.
- ~~Push-triggered CI deploy runs failing~~ — stale: push deploy `27266421553` (2026-06-10) succeeded end-to-end
- ~~Dead CSS audit: `default.css` / `new-ui.css`~~ — STALE (2026-06-20): neither file exists anywhere in the repo; no references.
- Opening-hours hardening DONE 2026-06-20 (`8e4c5e760`): `getOpeningHours` unions only timetables of staff with an active onlineBooking service (test/non-bookable employee can't widen public hours); calendar L1 spec dates made dynamic (`nextDow`). Verified live: wed=[], source=timetables.

---

## Recent decisions

- **WYBÓR KANAŁÓW POWIADOMIEŃ (panel/SMS/WhatsApp/e-mail) 2026-07-05** (`bc4629d19` backend, `c2c646b69` frontend, oba deployed+zweryfikowane live; owner: „ma być dostępne wybór notyfikacji"). Stan przed: `receiveNotifications` (master) + `smsConsent`+`emailConsent`; WhatsApp wysyłał się TYLKO na masterze (SMS+WhatsApp zlepione w UI pod jednym). Zbudowane: **backend** — `users.notifyPanel`(default true) + `users.whatsappConsent`(default false, opt-in) + migracja 1761300000000; `UpdateConsentDto`+`updateConsent` obsługują 4 kanały; **wszystkie 7 wysyłek WhatsApp do klienta** (booking confirmation, reschedule) zbramkowane na `whatsappConsent` (obok mastera) — WhatsApp idzie tylko gdy klient wybrał ten kanał. **frontend** — `/account`: sekcja „Powiadomienia i zgody kontaktowe" = 4 osobne checkboxy (Panel w aplikacji / SMS na telefon / WhatsApp / E-mail) zamiast zlepionego SMS/WhatsApp; wysyła notifyPanel+smsConsent+whatsappConsent+emailConsent + refreshProfile. **Live-verified:** 4 kanały renderują (Panel✓ domyślnie, WhatsApp opt-in), toggle WhatsApp→Zapisz→API potwierdza whatsappConsent=true. **Test-context fixture (mock DI) i typ panel User wymagały nowych pól** — przy każdym nowym polu User dołożyć do fixtures+types.ts. Backend 234/234, panel 283/283. UWAGA: SMS do klienta na eventy wizyt NIE jest jeszcze podpięty (tylko WhatsApp+email-do-salonu są client-facing) i SMSAPI wymaga tokena od ownera — preferencja jest ZAPISYWANA i uszanuje kanał gdy delivery dojdzie; web-push (telefon przy zamkniętym panelu) = osobny większy krok (SW+VAPID jest w backendzie, brak SW w panelu).
- **KOMUNIKACJA klient↔salon: wątek wiadomości + baner 2026-07-05** (owner: „klient nie ma jak odpisać na komentarz/wiadomość pracownika; notyfikacja by się przydała"; wybrał AskUserQuestion „wątek + baner in-app, bez push"). **USTALENIE STANU przed budową:** akceptacja zmienionego terminu JUŻ DZIAŁA (przycisk „Akceptuj nowy termin" na pulpicie+/visits dla rescheduled_pending, `/accept-reschedule` Client-only) — owner nie wiedział że jest. LUKA = klient mógł tylko CZYTAĆ `notes`, nie odpisać (`/client-note` staff-only). Notyfikacje: infra jest (moduł /notifications wszystkie role, web-push VAPID) ale panel klienta nic z tym nie robił, brak service workera → push nieużywalny. **ZBUDOWANE (backend, `cb871e052`, deployed, zweryfikowane live):** encja `AppointmentMessage` (appointment_messages, FK CASCADE, authorRole) + migracja 1761290000000; `GET/POST /appointments/:id/messages` (klient tylko WŁASNA wizyta — cudza=403 zweryfikowane; staff wszystkie; body max 2000, pusty=400); `/dashboard/client` niesie teraz `pendingRescheduleCount` + `newSalonMessageCount` (wątki gdzie salon pisał OSTATNI — heurystyka „masz coś do odpowiedzi" bez read-state). **Live E2E: salon pisze→klient czyta→odpisuje (201×3); baner: klient odpisał ostatni→newSalonMessage=0, salon ostatni→=1 ✓; cudza wizyta→403; pusty→400.** Test-context (mock DI konstruktora AppointmentsService) wymagał dołożenia mockAppointmentMessagesRepo — pamiętać przy każdym nowym repo w konstruktorze. Backend 234/234. **FRONTEND deleguję Sonnetowi (kontrakt zamrożony+zweryfikowany):** `MessageThread` komponent (dymki wg authorRole, textarea+wyślij) reużywalny w /visits (klient, rozwijalne) i drawerze (staff) + baner na ClientDashboard z pendingReschedule/newSalonMessage → link /visits. Ja robię review+merge+deploy+live-verify.
- **SZYBKOŚĆ CODZIENNEJ PRACY + delegacja agentom 2026-07-04** (owner: „pełnofunkcjonalny produkt, szybki kalendarz, podgląd co klientka miała robione; agentów dobieraj tańszym modelem"). **Ja (zweryfikowane live):** (1) `0f81b83e7` topbar „Szukaj klientki..." — omnibox był ATRAPĄ z klonu Versum; teraz realne wyszukiwanie (debounce 250ms, GET /customers?search=&limit=8, klawiatura, klik→karta). (2) tamże **„Ostatnie wizyty" w drawerze** — po wybraniu klientki (create) lub otwarciu wizyty (edit) 3 ostatnie ODBYTE wizyty (data+usługa+zalecenia) bez wychodzenia z kalendarza; backend: events-history niesie `notes`. **Agenci Sonnet (worktree; ja review+merge+deploy):** `af2a8ad2f` (3) `ServiceCombobox` — typeahead usług w drawerze (WAI-ARIA, filtr client-side, czas+cena, klawiatura) + cyjanowy badge „W trakcie"→ciemny (AppointmentQuickModal); (4) pole „Stały rabat (%)" na grupach klientów create+edit (backend już wspierał — brakowało UI; domyka [[client-standing-discount]]); (5) regres Playwright tests/e2e/regression/ (5 speców, 36 testów, read-only, skip-guard) + workflow `e2e-playwright-regression.yml`; sekrety CI ustawione (E2E_ADMIN_*, E2E_CLIENT_*=e2e.client.0628, var E2E_BASE_URL). 1. run: 9/11 pass, 2 timing-faile (omnibox debounce, calendar week URL). Live-verified: typeahead „strzyż"→13 usług, pole rabatu 0-100+hint. LEKCJE agentowe: (a) 2 fale agentów padły na limicie sesji (subagent_tokens<250 = nic nie zrobił — sprawdzać!); (b) worktree-agent z absolutną ścieżką repo w prompcie pisał do GŁÓWNEGO repo — pisać „pracuj w bieżącym katalogu"; (c) model-fit: Sonnet UI-plumbing/testy, Opus integracja/debug prod. UWAGA: po Fazie 5 konto e2e.client.0628 zniknie → przełączyć sekret E2E_CLIENT_EMAIL na trwałe konto.
- **AUDYT FUNKCJONALNY 144 akcji — ZAKOŃCZONY 2026-07-04, 8 realnych bugów naprawionych+zweryfikowanych live.** Dalsze znaleziska (poza 3 z pierwszej części): **(4)** `995fc85b4` 🔴 KLASA BUGÓW: `ColumnNumericTransformer.to()` mapował `undefined→null` → TypeORM słał jawny NULL zamiast DEFAULT → `POST /deliveries` 500 na `totalCost` (dotyczyło KAŻDEJ encji z transformerem na kolumnie NOT NULL DEFAULT); fix w transformerze (undefined zostaje undefined) + `totalCost:0` w create. Diagnoza ze stack trace z prod: **owner dał zgodę na SSH** (`ssh vetternkraft@s0.mydevil.net`, log: `/usr/home/vetternkraft/domains/api.salon-bw.pl/logs/error.log` — NIE ~/logs/nodejs/ z docs); wcześniej 3 ślepe migracje-hipotezy (nieszkodliwe, idempotentne: FK-cascade movements 1761240, NOT-NULL reconcile 1761260, id-sequence repair 1761270+1761280). Loki (`observability.salon-bw.pl`) NIE ISTNIEJE w DNS — dodany workflow `ops_logs_query.yml` (LogQL bez SSH) na przyszłość gdy obserwability wróci. **(5)** `a766e12fa` stocktaking start 500 — `save(stocktaking)` po `findOne` z załadowaną (pustą) relacją items NULL-owało FK świeżo wstawionych 821 pozycji (orphan-update); fix=`update(id,{status})`. **(6)** `4bfbb7825` restart start dublował pozycje (unique key) — idempotencja (insert tylko brakujących). **(7)** `d970cf11f` 🔴 time-blocks wymagały role===Employee → **Aleksandra-admin nie mogła dodać sobie przerwy/urlopu** (ta sama pułapka co booking 23.06); gate tylko na Client. **(8)** tamże: `GET /settings/reminders` 500 — `findOne({order})` bez where (TypeORM 0.3 rzuca); fix=find+take(1). **Zweryfikowane działa po fixach:** dostawy KOMPLET (create+items 201, receive→stan+2, items DELETE, cancel), sprzedaż POS+void (stan wraca), inwentaryzacja start→in_progress+items+complete-walidacja („821 niezinwentaryzowanych"=poprawna), time-blocks CRUD dla admina, reminders GET/PUT 200, settings/sms PUT 200 (odchudzony payload), branches CRUD, timetable-templates (walidacja „dokładnie 7 dni"=OK, panel wysyła 7). **RESIDUUM audytu do Fazy 5:** produkt 829 „AUDYT flow" (nieaktywny, stan 2, historia dostawy/sprzedaży), inwentaryzacja #1 in_progress (821 pozycji), dostawy #8 received/#9 cancelled, zamówienia #1-2 cancelled, sprzedaż #9 voided, grupa/tag/pole-testowe posprzątane inline. DELETE produktu z historią = czysty 400 PL („oznacz jako nieaktywny") ✓. (odpowiedź na pytanie ownera „jak sprawdzić czy wszystkie menu działają bez klikania wszystkiego"). Metoda: inwentarz wszystkich mutujących wywołań `apiFetch` (144 endpointy) → weryfikacja hurtowa API create→verify→delete Z PAYLOADAMI PANELU (nie zgadywanymi — 6 wczesnych „porażek" to były moje złe kształty). **Zweryfikowane działa:** klienci-komplet (grupy+członkowie+sort, tagi, pola dodatkowe, pochodzenia, notatki), usługi-komplet (warianty+reorder, receptura, komentarze, prowizje, services reorder), produkty (create, inventory/adjust — uwaga: 30s cache TypeORM opóźnia odczyt stanu), gift-cards (create/adjust{amount,notes}/delete), loyalty rewards CRUD, sms/templates CRUD, automatic-messages CRUD+toggle, newsletters CRUD. **3 REALNE BUGI znalezione+naprawione+zweryfikowane live:** (1) `5c73aecba` products DELETE 500 dla produktu z historią korekt — FK inventory_movements RESTRICT→CASCADE (migracja 1761240000000) + czysty 400 PL dla pozostałych referencji; (2) `3c2911d2b` **cały system PUNKTÓW lojalnościowych martwy na prod** — encja+FIFO używają `points_remaining`, żadna migracja nie stworzyła kolumny (drift rodzina typeorm-postgres-drift-traps); migracja 1761250000000; (3) `d98a6b312` ujemne korekty punktów zawsze padały („Dostępne: 0") — adjustPoints nadpisywał saldo PRZED deductPoints, który waliduje na już-zmniejszonym i odejmuje drugi raz. **Cykl punktów zweryfikowany live: award +5=201, adjust −5=201, saldo 0, earned5/spent5.** POZOSTAŁE chunki audytu: orders send/receive, deliveries items/receive/cancel, stocktaking start/items/complete (ostrożnie — complete koryguje stany!), time-blocks, timetable-templates+exceptions, settings PUTs (sms/reminders/payment-config), branches, sales POST (odejmuje stan — na tymczasowym produkcie). Wysyłki (sms/email/newsletter send) celowo pominięte (brak tokena/realne maile). KONTEKST OD OWNERA (zapisany w pamięci [[salonbw-project-purpose]]): panel powstaje bo Booksy kupiło Versum — żona nie chce ich polityki/reklam; cel = własny panel dla 1-os. działalności.
- **FAZA 3 planu 10/10 DONE: design sweep + a11y 2026-07-03** (4 commity: `4e730462d` purga niebieskich, `e2a102bc4`+`3f46f477b` kontrast sidenava, `7353b424a` czerwień+focus, `674dc8636` aria-label paginacji; wszystkie deploye success). **(1) Purga ostatnich niebieskich Versum** (najpierw inspekcja realnego DOM — lekcja z mini-cala): aktywne taby magazynu `#3795de`→`#0d0d0d` (`.column_row.secondary_menu .simple-list li.active a`+icon), stats-tab-link `#3795de`→`#6e7278` (aktywny zostaje ciemny), 4 link-buttony ustawień + add_phone_number hover→`#1a1a1a`, ramka inputów wyszukiwania `#8bcafb`→`#bfbfbf`, focus ring `.form-control:focus` `#9ccdf2`/niebieski shadow→srebrno-czarny. **Zero #3795de/#8bcafb w arkuszu.** **(2) Kolumna „Nazwa" w /sales/history** — `.wrap{max-width:0}` zwijał do min-content (1 znak/linia) → czysty wrap + `td.wrap.link_body{min-width:180px}` (wąskie komórki .wrap w services nietknięte). Live: 393px, pełna nazwa. **(3) Kontrast sidenava (Lighthouse 97→100)**: `#sidenav/.sidenav .nav-list li.active a` miał srebro `#b4b8be` na `#f0f4f8` (1.8:1) — fix w salon-shell NIE zadziałał, bo **duplikat w globals.css wygrywa kaskadę** (pułapka [[panel-dashboard-css-duplicate]] potwierdzona 2. raz) → naprawione w OBU. **(4) `.btn-danger` `#e74c3c`→`#c0392b`** (3.82→5.44:1 z białym; „Usuń" w szablonach łączności) + consent-no ta sama czerwień. **(5) aria-label="strona"** na 2 brakujących inputach paginacji (inventory, TimetableTemplatesPage; pozostałe 8 stron już miało). **Lighthouse a11y po fixach: /visits=100, /sales/history=100, /communication/templates=100, /inventory=100** (reprezentanci rodzin modułów; reguły współdzielone). Mobile 390px: /visits czytelny bez h-scrolla (gwiazdki+akcje OK), /booking bez h-scrolla. Testy 267/267. Next: Faza 4 = twardnienie (Sentry DSN ja; backup+hasło+domena+SMSAPI owner) → Faza 5 = cleanup danych testowych + finalny E2E → GO.
- **FAZA 2 planu 10/10 DONE: smoke-pass modułów staff 2026-07-03** (`13e1d1d4b` stub-removal, deploy push). Przejście live jako admin (UI w przeglądarce + API): **/inventory, /orders(→history), /deliveries(→history), /manufacturers (realne dane: Wella 480, Kerastase 139…), /sales/history (4 sprzedaże), /sales/gift-cards (czysty B&W), /loyalty, /communication/{templates,mass,campaigns,automatic} (aliasy /messages→campaigns, reminders→automatic działają), /invoices — wszystko renderuje, konsola CZYSTA (tylko PWA-info).** Zapisy: `POST /orders`→draft→`PATCH cancel`→cancelled ✓ (bez residuum); `POST /stocktaking`→draft id 1 ✓ (**RESIDUUM: draft „SMOKE TEST" do sprzątnięcia w Fazie 5** — stocktaking nie ma DELETE). Usunięty jedyny stub panelu: zakładka „Wykres (wkrótce)" w `statistics/employees` (tabela renderuje się wprost, martwy activeTab usunięty). **ZNALEZISKA DESIGNOWE do Fazy 3** (funkcjonalnie wszystko OK): (1) niebieskie aktywne taby modułu magazynu (PRODUKTY/SPRZEDAŻ/ZUŻYCIE/INWENTARYZACJA + niebieski kwadracik ikonki) na inventory/orders/manufacturers/sales; (2) niebieskie przyciski „pobierz…Excel" (Versum button-blue); (3) niebieska ramka focusa na inputach wyszukiwania; (4) /sales/history: kolumna „Nazwa" ściśnięta do 1 znaku/linia (szerokości kolumn); (5) gift-cards/loyalty już on-brand. Next: Faza 3 = design sweep (te 4 punkty + Lighthouse a11y + mobile 390px).
- **FAZA 1 planu 10/10 DONE: historia wizyt + oceny klienta 2026-07-03** (`fd80e6dc9`, api+panel deployed `28653302061`; plan: `docs/PANEL_10_10_PLAN.md` `108a77310`). Owner: „panel 10/10 design+funkcjonalność do wdrożenia; testów nie wycofujemy". Zbudowane: **(a)** `GET /dashboard/client/visits` — jawna client-safe projekcja (service+employee name, `notes`=zalecenia, sklejona własna ocena; ZERO paidAmount/internalNote — przy okazji **załatany żywy wyciek na `GET /appointments/me`**, surowa encja serializowała internalNote+kwoty; endpoint nie miał konsumentów w panelu); **(b)** `POST /reviews` przyjmuje `appointmentId` — walidacje: wizyta klienta + completed + jeszcze nieoceniona, pracownik brany Z WIZYTY (nie z body); **(c)** strona `/visits` (client-only): sekcje Nadchodzące/Odbyte/Anulowane-i-nieodbyte, notatki+zalecenia, akcje Anuluj/Akceptuj/Umów-ponownie, **gwiazdki 1–5 + komentarz** na odbytych (`StarRating` — dostępny radiogroup, 44px, B&W), „Zmień ocenę"=DELETE+ponowny formularz; **(d)** rail klienta: pulpit/moje wizyty/rezerwacja (lookup po key, nie indeksach — `SalonModuleKey`+`clientVisits`); pulpit linkuje „zobacz wszystkie wizyty i oceń odbyte". **Zweryfikowane LIVE na prod:** complete #101 → klient ocenia 5★ (review id 1, PIERWSZA realna opinia) → duplikat=400, anulowana=400 → ocena sklejona w visits → **admin `/reviews` pokazuje opinię w tabeli** (ID/Wizyta 101/Pracownik/Autor/5/komentarz + Edytuj/Usuń). UI: /visits renderuje się on-brand (screenshot ok), konsola czysta. Testy: panel 267/267 (nowy visitsPage 2), backend 234/234. Next: Faza 2 = smoke-pass modułów staff (magazyn/sprzedaż/loyalty/gift-cards/łączność) + usunięcie stuba `statistics/employees`.
- **Live E2E całej drogi klienta + fix least-privilege 2026-06-28** (`42129fda5`, panel deployed `28316083610`). Owner: „skupmy się na drodze klienta". Przeszedłem CAŁĄ ścieżkę na żywo na prod (świeży klient id 53, sterowane API + weryfikacja UI w przeglądarce): (1) rejestracja — oba consents wymuszone (400 bez termsConsent, 400 bez gdprConsent, 201 z oboma); (2) login, profile role=client; (3) booking — `/services/online-booking`=60, sloty, POST→`online_pending` (#101); (4) `/dashboard/client` — upcoming+recent+stats+favorites, notatka klienta widoczna, **zero wycieku ceny**; (5) reschedule→accept — admin confirm→`/reschedule`→`rescheduled_pending`→klient `/accept-reschedule`→confirmed; (6) cancel — #102 booked→`/cancel`→cancelled, konflikt slotu trzyma; (7) edycja profilu+zgód (PATCH `/users/profile`, `/users/profile/consent`); (8) UI /dashboard, /booking, /account renderują się on-brand, konsola czysta. **API+UI bez bugów w głównym flow.** ZNALEZIONY BUG: klient mógł wejść z URL-a na **staffowy `/reviews`** (manager opinii) — rola client miała w `access.ts` martwe `nav:reviews`/`nav:invoices`/`nav:appointments`, a `/reviews` bramkuje tylko po permission (backend `GET /reviews`=`@Roles(Admin)` więc dane 403, ale pusta staffowa strona się renderowała). Fix: `client: new Set(['dashboard:client'])` (least privilege; booking idzie przez `roles=['client']`, dashboard przez `dashboard:client`). **Zweryfikowane live: /reviews=403 „Nie masz uprawnień", /dashboard nadal działa.** DŁUG: dane testowe na prod do sprzątnięcia — klient id 53 „E2E Klient Zmieniony", appt #101 (confirmed, 2026-06-29 11:00), #102 (cancelled) + 2 maile L2 do kontakt@.
- **BUG: klient dostawał 403 po rezerwacji („moje wizyty" → /calendar?view=client) — naprawiony 2026-06-27** (`32cbee985`, panel deployed `28294901559`). Po rezerwacji ekran sukcesu miał przycisk „moje wizyty", który robił `router.push('/calendar?view=client')` — ale `/calendar` jest staff-only (RouteGuard `nav:calendar`), więc klient dostawał pełnoekranowy 403 „Nie masz uprawnień" + w konsoli 403 na `/calendar/events`. Hub wizyt klienta to `/dashboard` (zgodnie z CLIENT_MODULES „moje wizyty"→/dashboard). Fix: redirect na `/dashboard`. `booking.tsx` był jedynym miejscem kierującym klienta na /calendar (rail i postLoginRoute były już poprawne). Panel-only. Uwaga: `calendar.tsx` ma martwy „client mode" (linie 457/1315) zablokowany przez RouteGuard — do ew. usunięcia.
- **BUG: 400 na /sms/history i /emails/history (page/limit query → @IsNumber bez @Type) — naprawiony 2026-06-27** (`5a8589f62`, api deployed `28294708899`). Owner widział w konsoli przy nawigacji na moduł łączności: `GET /api/sms/history?page=1&limit=20 400` i to samo dla `/emails/history`. Query paramy przychodzą jako STRINGI, a `SmsHistoryFilterDto`/`EmailHistoryFilterDto` walidowały `page`/`limit` (i recipientId/appointmentId) przez `@IsNumber()` bez `@Type(() => Number)` → „1" nie przechodzi @IsNumber → 400. Fix: `@Type(() => Number)` na polach numerycznych (ValidationPipe koeruje przed walidacją). **Zweryfikowane live: oba endpointy 200 z `{items,total,page,limit}`.** API-side, działa na otwartej karcie usera bez redeployu panelu. Lekcja: numeryczny query-param wymaga `@Type(() => Number)`, nie samego `@IsNumber()`.
- **🔴 BUG: mini-kalendarz — przez WIELE prób stylowałem ZŁY (ukryty) komponent — naprawiony 2026-06-27** (`2a6339480`, panel deployed `28278547844`). Po serii pudeł owner otworzył devtools i pokazał: widoczny mini-kalendarz to `div.salonbw-mini-cal` (custom, w `CalendarNav` przez `SalonSecondaryNav`), a NIE react-datepicker `.salonbw-datepicker` (to `CalendarSidebar` w `CalendarView`, **display:none** — stąd wszystkie moje pomiary = 0 i „nic się nie zmienia" mimo deployów). PRAWDZIWY bug w `.salonbw-mini-cal`: nagłówek `__weekdays` był `display:flex; justify-content:space-around` a liczby `__days` były `display:grid repeat(7,1fr) gap:2px`, plus `__day` miał sztywne `width:28px` przepełniające ~22px komórki → dwa różne systemy layoutu = trwały dryf + ucięta niedziela. Fix: OBA rzędy na ten sam `display:grid; grid-template-columns:repeat(7,minmax(0,1fr)); gap:2px`, `__day` wypełnia kolumnę (`width:100%`, bez `margin:0 auto`). Cofnięte błędne edycje `.salonbw-datepicker` do minimalnego oryginału. **LEKCJA (owner): przy debugowaniu CSS ZAWSZE najpierw zinspekcja realnego wygenerowanego DOM w przeglądarce — nie zakładać który komponent się renderuje.** Tooling chrome-devtools-mcp + panel auth w automatyzacji zawodne (sesja nie trzyma, getBoundingClientRect=0 na display:none subtree) → weryfikacja przez ownera.
- **BUG: mini-kalendarz — nagłówki vs liczby drift, PRAWDZIWA przyczyna: span vs button + `1fr`=minmax(auto,1fr) — naprawiony 2026-06-26** (`243eff437`, panel deployed `28253901898`). Po kilku nietrafionych próbach (flex→grid `5c5433bdf`, width:100% `dbf4975bf`, block-tree `cc067f0f5` — wszystkie dalej źle) **owner sam znalazł**: dni-NAZWY renderują się jako `<span>`/div, a dni-LICZBY jako `<button>` (role=button) — różne intrinsic rozmiary (border/padding/font UA buttona). KLUCZ którego brakowało: `grid-template-columns: repeat(7, 1fr)` to faktycznie `minmax(auto, 1fr)`, więc kolumna NIE może zejść poniżej min-content swojej zawartości; buttony mają większy min-content niż spany → kolumny rzędu liczb urosły szersze niż rzędu nazw → dryf, 7. kolumna (niedziela) uciekała. Fix: `repeat(7, minmax(0, 1fr))` (kolumny ściśle równe niezależnie od zawartości) + pełny reset buttona (appearance/border/background/font→inherit). **Zweryfikowane: oba rzędy computed `repeat(7, minmax(0px, 1fr))`.** Lekcja: do równych kolumn siatki ZAWSZE `minmax(0, 1fr)`, nie `1fr` — inaczej szersza zawartość (np. button vs span) rozpycha kolumnę. Tooling chrome-devtools-mcp miał zepsute getBoundingClientRect (subtree raportowane jako display:none mimo że malowane) — pomiary pikselowe bezużyteczne, stąd seria pudeł; computed-style + diagnoza ownera rozwiązały.
- **BUG: mini-kalendarz — nagłówki dni nie pokrywały się z liczbami (flex→grid) — naprawiony 2026-06-26** (`5c5433bdf`, panel deployed `28236357888`). Po fixie flex (`88b46058b`) owner: „pod niedzielą mam 6 a powinno być 7". Przyczyna: nagłówki dni (`__day-names`) są w `__header`, a wiersze tygodni w `__month` — przy flex `space-between` różne szerokości kontenerów powodowały dryf, liczby pełzły w prawo i 7. kolumna wychodziła poza siatkę. Fix: oba wiersze na **`display:grid; grid-template-columns:repeat(7,1fr)`** + wyzerowane poziome paddingi `__header`/`__month` → identyczne tory kolumn. **Zweryfikowane live: maxMisalignPx=0, środek każdego nagłówka == środek liczby; „7" pod „nie", 21/28 widoczne.** Lekcja: do siatki kalendarza używać CSS grid (wspólne tory), nie flex w osobnych kontenerach.
- **BUG: mini-kalendarz ucinał kolumnę niedzieli (nie „dni kolejnego miesiąca") — naprawiony 2026-06-26** (`88b46058b`, panel deployed `28226776888`). Owner doprecyzował: „21 czerwca jest poza minikalendarzem" — to nie były dni lipca, tylko **bieżący miesiąc nie mieścił się w całości**. react-datepicker ma sztywne szerokości dni+marginesy szersze niż sidebar 220px → 7. kolumna (niedziela) wychodziła poza kontener i była przycinana (7/14/21/28 znikały, a przycięte cyfry wyglądały jak śmieciowe „1 2 3 4"). Fix CSS: `.react-datepicker__week`/`__day-names` na `display:flex`, dni `flex:1 1 0; width:auto` → 7 kolumn zawsze się mieści. **Zweryfikowane live: nagłówki pon..nie (7), dni 1–30 obecne (has21/has28=true), dni lipca opacity:0/hidden.** Razem z ukryciem outside-month: mini-kalendarz pokazuje cały bieżący miesiąc i nic więcej. To wyjaśnia całą sagę „faint 1 2 3 4" — przycięta kolumna, nie next-month.
- **🔴 BUG: „wizyty znikają" = 429 throttle, nie cache/widok — naprawiony 2026-06-26** (`22d0b29be`, api deployed `28219395085`). Owner po reloadzie/przełączaniu widoków: pusty kalendarz; konsola (zrzut) = **429 Too Many Requests** na `/calendar/events` i `/appointments/online-pending-count`. ROOT CAUSE: globalny `THROTTLE_LIMIT=10/60s per IP` — jeden load kalendarza to ~10 calli (profile, events, online-pending-count, customer-alerts…), a każde przełączenie widoku + poll badge'a dokłada → legalny user przekracza 10/min → events dostaje 429 → siatka renderuje się PUSTA (stąd „znikają wizyty", mylone z bugiem widoku/cache). Fix: globalny default 10→**120** (login 5/min, register 3/min mają własne stricter `@Throttle`, niezmienione) + `@SkipThrottle()` na `online-pending-count` (tani authenticated COUNT pollowany z każdej karty). **Zweryfikowane: 25 szybkich żądań na OBA endpointy = 25×200, zero 429** (events padał po 10). API-side, więc działa też na otwartej karcie usera bez redeployu panelu. Lekcja: globalny throttler 10/min jest absurdalnie niski dla authenticated SPA — to nie był bug widoku ani cache, tylko rate-limit zabijający własne dane panelu. See [[opening-hours-unions-all-timetables]] (poprzedni SkipThrottle na opening-hours, ta sama rodzina).
- **Kalendarz Booksy-style: siatka miesiąca + wyszarzenie poza godzinami 2026-06-25** (`a0aec1890`, panel deployed `28194309982`). Owner (2 zrzuty Booksy): (1) „widać dni kolejnego miesiąca z prawej strony" — siatka miesiąca pokazywała szare dni lipca (12 komórek `.fc-day-other` + dopełnienie do 6 tygodni). Fix: `views.dayGridMonth.showNonCurrentDates=false` + `fixedWeekCount=false` → tylko bieżący miesiąc, 5 wierszy dla czerwca, czysto. Mini-kalendarz (react-datepicker) outside-month już był `visibility:hidden` (zrzut ownera był ze stale bundle). (2) „poza godzinami otwarcia szary kolor" — `.fc-non-business` istniało ale 0.035 (ledwo widoczne) → 0.06, teraz wyraźny pas zamkniętych godzin jak w Booksy. Wizyty zakończone/anulowane już wyblakłe (status-based EventCard) — bez zmian. **Zweryfikowane live: month grid 0 komórek next-month / 5 wierszy; week non-business bg rgba(13,13,13,0.06).** calendarPage 38/38.
- **🔴 BUG: przełącznik widoku kalendarza „czasem działa, czasem nie" — naprawiony 2026-06-25** (`d4f1a64c6` właściwy fix + `2fda8ea8c` key-remount + `be52c9d08` 1. próba, panel deployed `28191269433`). Owner: „czasem widok się zmienia a czasem ciągle widać ten sam (tygodniowy/miesięczny)". Diagnoza live (chrome-devtools, próbkowanie URL+DOM): klik „Tydzień" dawał URL **`view=day&date=2026-06-22`** — tylko data się zmieniała, widok zostawał. ROOT CAUSE: `updateCalendarQuery` mergował na `{...router.query}`, a shallow `router.push` jest ASYNC → dwa pushe w jednym ticku (zmiana widoku + date-sync z FullCalendar `datesSet`) czytały STARY `router.query`; drugi push trzymał stale `view=day` i **klobował** `view=week` z pierwszego → `useCalendarUrlSync` re-derywował widok z URL → revert → siatka stała. Fix: merge na ref (`pendingQueryRef`) aktualizowany synchronicznie przed każdym pushem (pushe się kumulują, nie kasują nawzajem). Plus key-remount `key={currentView}` na FullCalendar (czysty mount, bez `changeView` race) i scalenie gotoDate+changeView w jeden efekt. **Zweryfikowane live: 12-transition matrix + rapid-fire (700ms) = 100% trafień, każdy Dzień→day/Tydzień→week/Miesiąc→month, czyste URL-e.** Lekcja: wiele `router.push({...router.query})` w jednym ticku = klobber przez stale snapshot; mergować na synchronicznym ref. See [[calendar-router-query-clobber]].
- **BUG: przełączanie Miesiąc→Dzień/Tydzień lądowało na pustym 1. dnia miesiąca — naprawione 2026-06-25** (`06ca07bea`, panel deployed `28172767737`). Owner: „wizyty są widoczne, ale jak zmieniam dzień→tydzień→miesiąc to znikają". Diagnoza live: widok Miesiąc ma datę-kotwicę = 1. dnia miesiąca (np. 2026-06-01); `onViewChange` zachowywał tę datę przy zwężaniu widoku, więc Miesiąc→Dzień lądował na 1 czerwca (pusty, bo wizyty testowe od 24 cze) → „wizyty znikają". To NIE był bug ładowania eventów (week=11/month=25 ładowały się OK na właściwej dacie) — tylko data przeskakiwała na pusty 1. Fix: `onViewChange` przy zwężaniu do day/week sprawdza czy DZIŚ mieści się w pokazywanym okresie (`isSameMonth`/`isSameISOWeek`) i jeśli tak — skacze na dziś (populated). **Zweryfikowane live: Miesiąc→Dzień = 2026-06-25 (2 wizyty, było June 1/0), Miesiąc→Tydzień = 2026-06-22 (11 wizyt, było June 1/0).** Inna nawigacja bez zmian. Lekcja: widok miesiąca kotwiczy na 1. — zwężanie widoku musi wybrać sensowny dzień (dziś), nie kotwicę.
- **BUG: 404 w konsoli na karcie klienta (link inicjałów stylisty → nieistniejący `/employees/[id]`) — naprawiony 2026-06-25** (`a16e3b4f9`, panel deployed `28155330787`). Owner: wejście na kartę klienta z drawera → czerwony `GET /_next/data/<buildId>/employees/29.json 404`. Zdiagnozowane przez `list_network_requests` na ŚWIEŻEJ sesji (aktualny buildId, nie stale): historia wizyt na `customers/[id].tsx:672` owijała inicjały stylisty w `<Link href={/employees/${id}}>`, ale **nie ma `pages/employees/[id].tsx`** (tylko `index.tsx`; pracownicy zarządzani pod `/settings`) → Next prefetchował nieistniejącą trasę → 404 (klik też by padł). Fix: inicjały to label, nie nawigacja → `<span title={name}>` zamiast Link; CSS `.visit-employee a` → `.visit-employee a, .visit-employee span` (zachowany badge). Link usługi (`/services/[id]` istnieje) bez zmian. **Zweryfikowane live: nowy buildId, karta /customers/49 = zero 404, services 95/96/97 dalej 200.** Lekcja: `<Link>` do trasy bez `pages/` = prefetch 404 w konsoli + klik 404; diagnozować na świeżej sesji (odróżnić od stale-buildId churn).
- **🔴 BUG: crash szczegółów wizyty (totalSpent string-concat .toFixed) — naprawiony 2026-06-25** (`135aa3fa5`, api+panel deployed `28154483878`). Druga odsłona rodziny „decimal jako string" (po EventCard paidAmount wczoraj). Otwarcie drawera wizyty → `totalSpent.toFixed is not a function` → cały kalendarz padał. Przyczyna GŁĘBSZA: `customer-statistics.service` sumował `completed.reduce((sum,a)=> sum + (a.paidAmount||...))` — `paidAmount` to decimal-string `"600.00"`, więc `0 + "600.00"` = **konkatenacja** `"0600.00"` → `totalSpent` wracał STRINGIEM → panel `.toFixed` crash. Ujawniło się dopiero po pierwszej sfinalizowanej wizycie (wcześniej `completed`=[] → totalSpent=0 number). Fix: `Number()` w reduce (`serviceSpent`) + `spent` per-month (źródło, naprawia też kartę klienta) + defensywnie `ClientSection`. SQL `SUM` w `customers.service:180` tylko do filtra HAVING (nie renderowane) — bez zmian. **Zweryfikowane live: `/customers/49/statistics` totalSpent=1120 (number), drawer #41 otwiera się, „Łączne wydatki: 1120.00 PLN".** Testy 3/3. See [[decimal-string-tofixed-crash]].
- **429 na publicznym /calendar/opening-hours + przestarzała PWA meta — naprawione 2026-06-25** (`f2827070d`, api+panel deployed `28153955482`). Owner po zalogowaniu widział w konsoli: (1) czerwony `GET /calendar/opening-hours 429 (Too Many Requests)`, (2) żółty deprecation `apple-mobile-web-app-capable`. (1) Endpoint jest PUBLICZNY i wołany przez landing (każdy gość) ORAZ panel (calendar.tsx + booking.tsx), a globalny throttle `THROTTLE_LIMIT=10/60s per IP` dawał fałszywe 429 legalnym userom (panel SPA + moje testy + landing łatwo przekraczają 10/min na ten route). Endpoint jest tani, **5-min cache** (`openingHoursCache`, 300_000ms), bez PII → dodany `@SkipThrottle()` (cache chroni DB; `nearest-slot`, który skanuje 14 dni, zostaje throttlowany). **Zweryfikowane live: 20 szybkich żądań = 20×200, zero 429** (było: 11-20 → 429). (2) Dodany nowoczesny `<meta name="mobile-web-app-capable">` obok apple-prefixed (ten zostaje dla starego iOS). Panel nie woła opening-hours na dashboardzie wprost — 429 było resztką wyczerpanego okna throttle z innych powierzchni. Lekcja: publiczne, cacheowane read-only endpointy współdzielone landing+panel nie powinny dzielić globalnego throttle 10/min — `@SkipThrottle()`.
- **🔴 BUG: crash kalendarza po finalizacji (paidAmount string .toFixed) — naprawiony 2026-06-24** (`9ef05752b`, api+panel deployed `28126914627`). Owner sfinalizował realną wizytę → kalendarz rzucił `paidAmount.toFixed is not a function` + „Application error: a client-side exception" (cała strona padła). Przyczyna: kolumny **decimal wracają z drivera/JSON jako STRING** (`paidAmount='600.00'`), a `EventCard` wołał `event.paidAmount.toFixed(0)` na stringu. Ścieżka nigdy nie odpalona na prod (0 sfinalizowanych wizyt) aż do pierwszej realnej finalizacji — `paidAmount>0` przechodziło (koercja w porównaniu), `.toFixed` nie. Fix dwustronny: backend `mapAppointmentToEvent` → `Number(apt.paidAmount)`; panel `EventCard` defensywnie `Number()`+`isFinite` przed `.toFixed`. Test regresji dla string-paidAmount. **Zweryfikowane live: kalendarz 24.06 renderuje się bez crasha, kafelek #41 „Zapłacono 600 zł · karta".** Dane bezpieczne (wizyta #41=completed,600,karta — błąd był tylko w renderze). **Lekcja: każde decimal z backendu (paidAmount/tipAmount/price/totalSpent) może być stringiem — `Number()` przed `.toFixed`.** Latentne ryzyko w `AppointmentDrawer:585 selectedService.price.toFixed` i `ClientSection:259 totalSpent.toFixed` (działają, bo te endpointy zwracają number, ale to ta sama rodzina). See [[typeorm-postgres-drift-traps]].
- **Finalizacja: „Kwota zapłacona" zamiast ręcznego napiwku 2026-06-24** (`d278205de`, panel deployed `28109684079`). Owner zakwestionował sens ręcznego napiwku; wybrał (AskUserQuestion) model **kwota zapłacona → nadwyżka = napiwek**. Pole „Napiwek (PLN)" usunięte, dodane „Kwota zapłacona (PLN)" (placeholder = należność). Model: `amountDue = cena − rabat + produkty + dodatkowe`; `paid = puste ? amountDue : wpisane` (puste = płaci dokładnie, brak problemu sync gdy zmienia się należność); `tip = max(0, paid − amountDue)`; `underpaid = max(0, amountDue − paid)`. Podsumowanie pokazuje „Do zapłaty" = należność, pod polem auto „→ Napiwek (nadwyżka): +X" lub „⚠ Niedopłata — brakuje X" (warning, nie blokuje). Payload bez zmian backendu: `paidAmountCents = paid`, `tipAmountCents = tip`. **Zweryfikowane live (#40, należność 70):** zapłata 100 → napiwek +30.00; zapłata 50 → niedopłata −20.00; stare pole Napiwek nieobecne. Gotówkę z resztą obsługuje wpisanie kwoty „która zostaje" (po wydaniu reszty). Panel-only, testy 3/3. Decyzja odrzucona: usunięcie napiwku całkowicie (owner wolał zachować statystykę).
- **Finalizacja: opcjonalna rozwijana receptura 2026-06-24** (`115737242`, api+panel deployed `28096618841`). Owner (2 zrzuty): receptura/formuła koloru była tylko w drawerze („Formularz zabiegu"), w finalizacji jej brak — niespójność. Owner wybrał (AskUserQuestion) wariant **rozwijane pole w finalizacji**. Dodany toggle „+ Dodaj recepturę / formułę koloru" (domyślnie zwinięty, czysty checkout dla strzyżeń); po rozwinięciu textarea. Zapis **atomowy** w transakcji finalizacji przez nowe opcjonalne `formula` w `FinalizeAppointmentDto` → `manager.getRepository(Formula).save({description, date, client, appointment})` (brak ryzyka utraty rekordu po oznaczeniu wizyty completed; `manager.getRepository` działa dla każdej encji DataSource, nie trzeba forFeature w AppointmentsModule). **Zweryfikowane live (UX):** toggle obecny, pole ukryte domyślnie → pokazuje się po kliknięciu, hint „Zapisze się w historii klienta". Zapis end-to-end NIE testowany klikaniem (wymaga finalizacji = completed wizyty), pokryty 28/28 backend. Panel 3/3. **Follow-up (`3f6d57756`, panel deployed `28108385543`):** owner „te pola opcjonalne mają być również ukrywalne" → notatka wewnętrzna + zalecenia dla klienta dostały ten sam wzorzec zwijania (toggle „+ Dodaj …", ukryte domyślnie, reset przy zamknięciu). Zweryfikowane live: 3 toggle linki obecne, wszystkie 3 pola ukryte domyślnie, klik rozwija. Rabat/Napiwek zostają widoczne (rdzeń kasy, małe inline inputy); pickery produktów/usług/materiałów już miały toggle.
- **Finalizacja: wyszukiwarka w pickerach 2026-06-24** (`b1d8c41a5`, panel deployed `28094062208`). Owner: „produkty trzeba jakoś wyszukiwać i usługi też – dodaj fancy search". Pickery listowały cały katalog (821 produktów) → trzeba było scrollować. Dodany komponent `PickerSearch` (input + ikona lupy Heroicons) na górze WSZYSTKICH trzech pickerów (sprzedaż produktów, użyte materiały, dodatkowe usługi). Filtr client-side po już-załadowanej liście (natychmiastowy): produkty/materiały po `name/brand/sku/barcode`, usługi po `name`; komunikat „Brak … dla „q"", reset przy zamknięciu modalu. **Zweryfikowane live: „color touch"→103, „wella"→480 (filtr po nazwie+marce, z 821); zła fraza→komunikat o braku.** Panel-only, testy 3/3.
- **Finalizacja/drawer — 3 fixy z review właściciela 2026-06-24** (`91bc21e70`, panel deployed `28084852869`). Owner (3 zrzuty): (1) **podwójny „Anuluj wizytę"** w drawerze dla `rescheduled_pending` — `ActionsSection` renderował dedykowany przycisk pod `isRescheduledPending` ORAZ ogólny pod `canCancel`, a `canCancel` już zawiera `isRescheduledPending` (`AppointmentDrawer.tsx:258`) → duplikat. Usunięty dedykowany. (2) **picker produktów/materiałów pusty („Brak produktów")** — modal pytał `/products?isActive=true`, ale `QueryProductsDto` NIE ma `isActive` (ma `includeInactive`, default false=aktywne) → globalny `forbidNonWhitelisted` zwracał **400** → react-query łapał → pusta lista. Regresja z 2026-06-17 (wtedy zmieniono na `?isActive=true` myśląc że filtruje). Fix: `/products` (domyślnie aktywne). **Zweryfikowane live: `/products` = 821 aktywnych** (było 400/0). Uwaga: picker USŁUG `/services?isActive=true` DZIAŁA (services DTO akceptuje isActive — niespójność z products, ale realna). (3) **dodatkowe usługi**: cena per-pozycja teraz **edytowalna** (ręczna korekta sugerowanej z cennika), usunięty per-pozycja rabat — rabat liczony RAZ od całości przez istniejące pole „Rabat (PLN)" (`grandTotal = usługa − rabat + napiwek + produkty + dodatkowe`, `maxDiscount` = suma wszystkiego). Panel-only, 17 testów zielone. **Lekcja: panel wysyłający query-param spoza whitelisty DTO = 400 → cichą pustą listę; products używa `includeInactive`, NIE `isActive`.** See [[typeorm-postgres-drift-traps]] (rodzina forbidNonWhitelisted).
- **Finalizacja: edytowalna cena usługi z cennika 2026-06-23** (`2231a398a`, api+panel deployed `28052791466`). Owner (zrzut FinalizationModal): „w realizacji usługi powinna być sugerowana cena z cennika, którą Aleksandra może ręcznie zmienić + dodać produkty/usługi/rabat". Bug: modal pokazywał **„Usługa: 0.00 PLN"** bo `appointment.service.price` nie jest w payloadzie kalendarza (event niesie tylko `{id,name}` usługi). Fix: modal fetchuje `GET /services/:id` (autorytatywna cena z cennika), pre-wypełnia edytowalny input **„Cena usługi (PLN)"** (hint „· z cennika N zł"), personel może nadpisać per wizyta. Edytowana cena wchodzi w grand total (`paidAmount`) ORAZ — przez nowe opcjonalne `servicePriceCents` w `FinalizeAppointmentDto` — w bazę prowizji usługi głównej (`commissionBaseOverride` gdy podano cenę lub są extras). Dodatkowe produkty/usługi/rabat/napiwek już były. **Zweryfikowane na poziomie danych:** service id 95 „Fryzura wieczorowa – włosy średnie" = 200 zł w cenniku (`/services?isActive=true`), więc modal pre-wypełni 200.00 zamiast 0.00. Testy: panel finalizationModal 3/3 (+servicePriceCents w payloadzie), backend appointments 28/28. **Nie zweryfikowane wizualnie klikaniem** (dotarcie do modalu = kilka kroków + status wizyty). Lekcja: payload eventu kalendarza jest slim — ceny/relacje trzeba dociągać z `/services/:id`, nie ufać `appointment.service.*`.
- **Rejestracja: regulamin + RODO OBA obowiązkowe 2026-06-23** (`b7a1083a7`, api+panel deployed `28051581049`). Owner: gość nie rezerwuje — musi się zarejestrować i zaakceptować regulamin oraz RODO; marketing zostaje opt-in. Wcześniej tylko RODO (`gdprConsent`) było wymuszone. Dodany równoległy `termsConsent`: kolumny `users.termsConsent`+`termsConsentDate` (migracja `1761230000000`, default false dla istniejących), `RegisterDto` ma `@Equals(true)` na termsConsent, `createUser` zapisuje zgodę + timestamp (rekord prawny), formularz rejestracji ma wymagany checkbox „Akceptuję Regulamin" (link do `/policy`) z własną walidacją + `role=alert`. **Zweryfikowane live:** rejestracja bez regulaminu → 400 „Akceptacja regulaminu jest wymagana"; bez RODO → 400 „Zgoda na przetwarzanie danych osobowych jest wymagana" (oba gardłują, bez tworzenia śmieciowych kont). Testy 234/234. Lekcja: nowa nieopcjonalna kolumna na `User` wymaga aktualizacji fixture'ów (`test-context.ts`) i asercji `createUser` (`users.service.spec`).
- **Rezerwacja respektuje reguły — AUDIT (kod) 2026-06-23, bez zmian:** owner chciał potwierdzenia, że klient może umówić się „dowolnie w ramach reguł". `getAvailableSlots` (`calendar.service.ts:432`) już pokrywa wszystko: godziny otwarcia (grafik pracownika → fallback godziny salonu → 9–19 pn–sob), **przerwy w grafiku** (sloty `isBreak` odejmowane, `:691/:701`), blokady ad-hoc (time blocks), istniejące wizyty (poza anulowanymi), urlopy/wolne/własne godziny (wyjątki grafiku per data), podłoga `teraz+1h`, wiele zakresów/dzień. Gość zablokowany (`booking.tsx:219` `roles=['client']`). Nic nie trzeba było budować.
- **Kalendarz: drag na zajęty termin + modal potwierdzenia nakładania 2026-06-23** (`892bdf11d`, panel deployed `28051272902`). Owner: Aleksandra ma móc przeciągnąć wizytę na istniejącą ruchem myszki, a potem modal „wizyty się nakładają — potwierdzasz?". Wcześniej `handleEventDrop` przy konflikcie **po cichu cofał** wizytę (`checkConflicts.hasConflict → revert`), więc nie dało się dwubookować myszką mimo że backend to wspiera. Fix (panel-only, backend był gotowy): upuszczenie na zajęte otwiera `ConfirmModal` nazywający kolidujące wizyty („Wizyty się nakładają … Przenieść mimo to?"); potwierdzenie robi reschedule z `force:true` (`appointments.service.ts:741` pomija `assertNoConflict`), anulowanie cofa drag. 38 testów kalendarza zielone. **Nie zweryfikowane live drag automatem** (FullCalendar DnD trudny do sterowania) — logika pokryta testami + backendowy `force` potwierdzony.
- **Konto testowe pracownika 2026-06-23** (`b77fb2576`, api deployed `28050524691`, migracja `1761220000000`). Nie było osobnego loginu roli `employee` (jedyny staff to admin Aleksandra), więc nie dało się przetestować ograniczonego widoku pracownika. Dodane `test.pracownik@salon-bw.pl` (role=employee, bcrypt w pliku, hasło w czacie), `down()` usuwa. Zweryfikowane live: login 200, `/users/profile` role=employee (id 52). **Komplet loginów testowych: admin `kontakt@salon-bw.pl`, pracownik `test.pracownik@salon-bw.pl`, klient `test.klient@salon-bw.pl`.** Pracownik bez usług/grafiku = niebookowalny (tylko do testu UI roli).
- **🔴 BUG: „wizyty oczekujące" pusta mimo badge=1 — naprawiony 2026-06-23** (`0ff8603ed` 1. próba, `6e83a8482` właściwy fix, panel deployed `28028911502`). Owner: „jak wchodzę na wizyty oczekujące to nie ma nic a świeci się jedna jako nie zatwierdzone". Przyczyna: `/appointments` domyślnie filtrowało okno `[dziś-30, dziś]` (przeszłość), a rezerwacje `online_pending` są w PRZYSZŁOŚCI → nigdy nie wpadały w zakres, podczas gdy topbar badge (osobny endpoint online-pending-count, bez dat) poprawnie liczył 1. **1. fix (`0ff8603ed`) robił status-aware DEFAULTY w useState — padł na hard-load** bo `router.query` jest pusty przy mount, więc `?status=online_pending` był gubiony i strona spadała na okno przeszłe. **Właściwy fix (`6e83a8482`):** logika przeniesiona do `useEffect` kluczowanego `router.isReady` — po wypełnieniu query synchronizuje status z URL i dla statusów pending ustawia okno do przodu (dziś..+90d). **Zweryfikowane live (zalogowany admin, hard-load `?status=online_pending`):** from=2026-06-23, to=2026-09-21, select=online_pending, „1 wizyt", wiersz „26.06.2026 11:30 · Test Klient · Strzyżenie dziecięce · Aleksandra Bodora · Oczekuje" — zgadza się z badge. Lekcja: `router.query` pusty na 1. renderze hard-load → status z URL czytać w `router.isReady` useEffect, nie w initial useState.
- **🔴 BUG: widoki Tydzień/Miesiąc ładowały tylko 1 dzień — naprawiony 2026-06-23** (`f92a7d23b`, panel deployed). Wykryty przy weryfikacji danych testowych: po zasianiu 60 wizyt kalendarz dalej był pusty w tygodniu/miesiącu. Przyczyna: `useCalendar.ts` w liście `attempts` próbował `view:'day'` JAKO PIERWSZY — zwraca 200, więc nigdy nie sięgał do `backendView` (week/month) → tydzień/miesiąc renderowały tylko eventy jednego dnia (start tygodnia, zwykle pusty). Fix: prowadzić aktualnym `backendView`, `day` zostaje fallbackiem tylko na 400. Backend wspiera week (11 eventów, zakres tygodnia) i month (25, zakres miesiąca) — zweryfikowane. 45 testów kalendarza zielone. **Zweryfikowane live z danymi: tydzień pokazuje bloki + liczniki „2 rezerwacje/3 rezerwacje", miesiąc pokazuje nazwy dni (Poniedziałek…) + czarne badge'y z liczbą (24②, 26③) + bloki w komórkach (Booksy-style).** To bug dla WSZYSTKICH (nie tylko danych testowych) — week/month były połamane od zawsze, niewidoczne bo prod miał 0 wizyt. **Dane testowe: 60 wizyt (2/dzień, 24 cze–23 lip) pod „Test Klient" id 49** — do sprzątnięcia migracją FK-safe gdy owner skończy testować widoki.
- **🔴 KRYTYCZNY BUG: rezerwacja online była ZEPSUTA — naprawiony 2026-06-23** (`6a1d3a2b6`, api deployed). Wykryty przy okazji tworzenia danych testowych: POST `/appointments` z employeeId=29 → 400 „Provided employeeId does not belong to an employee". Przyczyna: `loadEmployeeOrThrow` w `appointments.service.ts` wymagał `role===Role.Employee`, a właścicielka-stylistka Aleksandra została awansowana na **admina** (2026-06-22, `42f56dd5c`). Skutek: klientki WIDZIAŁY jej wolne sloty (available-slots bramkuje przez employee_services, nie rolę) ale POST rezerwacji się wywalał → jedyna osoba do umówienia była NIEUMÓWIALNA. To regresja z awansu na admina, niewidoczna bo deploy nie odpala testów + prod miał 0 wizyt. Fix: bramka odrzuca tylko `Role.Client`; employee/admin/receptionist są wszyscy poprawnymi wykonawcami (bookowalność i tak gardłuje employee_services). 28/28 testów appointments. **Lekcja: awans roli osoby bookowalnej może zerwać rezerwację — rola ≠ bookowalność w tej bazie.**
- **Kalendarz: naprawa nagłówka miesiąca + liczniki rezerwacji per dzień 2026-06-23** (owner pokazał 3 zrzuty Booksy Dzień/Tydzień/Miesiąc — „zobacz widoki kalendarza"). Diagnoza live: strukturalnie pokrywamy Booksy (day=timeGrid bloki, week=7 kolumn, month=siatka). Dwie zmiany (panel, FullCalendar w `CalendarView.tsx`): (1) `22fd152a5` — widok **miesiąca** miał w nagłówkach kolumn DATY („Pon., 5 / Wt., 6") bo globalny `dayHeaderFormat={weekday:'short',day:'numeric'}` (poprawny dla day/week) przeciekał do `dayGridMonth`; fix = per-view override `views:{dayGridMonth:{dayHeaderFormat:{weekday:'long'}}}` → pełne nazwy dni („Poniedziałek…"), data zostaje w komórce. Zweryfikowane live. (2) `188e371f9` — **liczniki rezerwacji per dzień** (Booksy „6 rezerwacji"): `dayHeaderContent` (week) renderuje pod datą „N rezerwacja/rezerwacje/rezerwacji" (poprawna polska liczba mnoga przez `rezerwacjeLabel`), `dayCellContent` (month) mały czarny badge z liczbą obok daty; liczy tylko `type==='appointment'` (nie time-blocks); nagłówek miesiąca (nazwa dnia, nie konkretna data) bez licznika. tsc+63 testy zielone, render pusty bez regresji. **NIE zweryfikowane z danymi — prod ma 0 wizyt** (wszystkie statusy `/appointments`=0; dashboardowy „Test Client pending" to był STALE cache, realna wizyta już nie istnieje; „Test Client" to tylko resztkowe konto klienta bez wizyt). Licznik pokaże się przy pierwszej realnej rezerwacji. Booksy ma jeszcze bogatsze filtry lewego panelu (status/płatność) — pominięte (mało istotne dla 1-os. salonu).
- **Google social login dla klientów 2026-06-23** (`241236292`, api+panel deployed). Owner chce rejestracji przez Google/Apple; wybrał Google teraz (darmowe), Apple później (wymaga płatnego Apple Developer ~$99/rok). Scaffold `auth-social` istniał (moduł+google.strategy+migracja googleId/facebookId, `passport-google-oauth20`) ale był ZEPSUTY/nieaktywny: callback zwracał JSON zamiast `res.redirect`, callbackURL relatywny, moduł ładuje się tylko gdy `GOOGLE_CLIENT_ID/SECRET` ustawione (404 na prod). Naprawione: callback → `res.redirect` na panel (`successUrl` abs. URL z env PANEL_URL, default panel.salon-bw.pl/dashboard; błąd→/auth/login?error=social), callbackURL abs. (`GOOGLE_CALLBACK_URL` default api.salon-bw.pl/...). Serwis już tworzył `Role.Client` + ustawiał SSO cookies (`Domain=.salon-bw.pl` → panel widzi sesję), link konta po emailu. Frontend: `GoogleAuthButton` (oficjalne kolorowe G = trademark Google, dozwolony wyjątek) na login+register, gated `NEXT_PUBLIC_GOOGLE_AUTH_ENABLED!=='true'`→null (żeby nie pokazywać przycisku 404 zanim skonfigurowane), dywizor „lub" w komponencie. deploy.yml: flaga wpięta (dormant). Zweryfikowane live: login renderuje czysto, przycisk ukryty (flaga nieustawiona). **AKTYWACJA (owner):** Google Cloud Console → OAuth Client (Web), redirect URI `https://api.salon-bw.pl/auth/social/google/callback`, JS origin `https://panel.salon-bw.pl`; ustawić `GOOGLE_CLIENT_ID`+`GOOGLE_CLIENT_SECRET` w API .env (scripts/safe-update-api-env.sh) + var `NEXT_PUBLIC_GOOGLE_AUTH_ENABLED=true` + redeploy. Apple = osobny build (appleId kolumna + passport-apple + JWT client-secret) gdy owner założy Apple Developer.
- **🔴 CI był czerwony — naprawiony 2026-06-22** (`88b18bcfe`): 1 nieaktualny test prowizji (`commissions.service.spec`) — `createFromAppointment` dostał param `baseAmountOverride` (czerwcowy G2), a `toHaveBeenCalledWith` nie zaktualizowano. Deploy nie uruchamia testów jednostkowych, więc czerwień była niewidoczna miesiąc. Teraz 234/234. **Audyt produkcyjny (2026-06-22): system gotowy z zastrzeżeniami** — 🟢 health/deploy 8x/testy/sekrety(Gitleaks+brak NEXT_PUBLIC secret)/email(SPF+DKIM+DMARC)/a11y100/admin-login; 🟡 4 high vuln zależności (ws/form-data/multer/nodemailer, CI nie blokuje), Sentry OFF (wpięty, brak DSN), E2E flaky (warm-up 403), „Test Client" residue; 🔴 owner-side: BACKUP bazy (krytyczne, brak SSH by sprawdzić), zmiana temp hasła admina, decyzja domeny.
- **„Booksy-feel" polish — purga niebieskich (verified live) 2026-06-22.** Owner wybrał „a" = szerszy pass gęstości/whitespace. NA ŻYWO (zalogowany jako Aleksandra) okazało się, że tabele NIE są przegęszczone (lista klientów ma sporo oddechu) — prawdziwym powodem „mniej dopieszczone niż Booksy" były **resztkowe niebieskie/cyjanowe akcenty Versum w monochromatycznym brandzie B&W**. Pivot na purgę kolorów (`03a805687`+`148d9e983`, panel, verified live computed): floating help `#37a5e6`→`#0d0d0d` (czarny, na każdym ekranie), przyciski okresu/labelki dashboardu `#008bb4`→czarny, mini-cal/breadcrumb/checkbox cyjan `#25b4c1`→czarny (`.text-salonbw-blue`→silver-ink), Versum button-blue `#56aef0` (linki+sprite excel)→`#6e7278`, `.salonbw-blue #3b82f6`→silver-ink, reception „confirmed" border `#0d6efd`→czarny, ORAZ `--bs-link-color #0d6efd`→`#1a1a1a` (linki „kalendarz"/„szczegóły"/btn-link były niebieskie default Bootstrap). Zweryfikowane: floating=rgb(13,13,13), linki=rgb(26,26,26). Semantyczne green/red zostają. Układ nietknięty. **Pozostały DŁUG testowy:** klient „Test Client" (600100200) ma pending online-booking → śmieci na pulpicie banner „Rezerwacje online czekające"; do sprzątnięcia migracją FK-safe (jak konta staff).
- **„Booksy-feel" polish — kierunek + pierwszy krok 2026-06-22.** Owner: nie chodzi o 100% zgodność, ale Aleksandra ZNA UX Versum (najłatwiej wdrożyć znajomy układ) + ma wrażenie, że Booksy/Versum „działają lepiej" niż nasz panel. Diagnoza: układ mamy dobry (klon Versum), brakuje **oddechu i dopieszczenia** — Booksy jest nowoczesny/przestronny, nasz klon gęsty/tabelaryczny. **Zasada: zachowujemy znajomy układ, podnosimy jakość (whitespace, typografia, miększe karty, spójne stany, zero emoji).** Owner zaoferował loginy do Booksy/Versum — ODMÓWIONO (29 zrzutów wystarcza na układ; login wciągnąłby PII klientek + ryzyko flagi Booksy; tylko jako ostateczność). Owner wybrał start: **codzienny flow kalendarz→wizyta→finalizacja.** Zrobione (`2741aa523`, panel): FinalizationModal („kasa") — emoji metod płatności (💵💳🏦📱🎁 = anti-pattern brandu) → **Heroicons + czysty B&W selected state** (active=czarny fill/biała ikona, inactive=biały+lekka ramka, zamiast blue-tint), więcej oddechu (mb-4). AppointmentDrawer shell już dopieszczony (rounded-4, soft-shadow 0 24px 64px, blur, brak emoji) — bez zmian. **Nie zweryfikowane wizualnie live** (brak wizyt na prod; appointment ma tylko soft-cancel → tworzenie testowej zostawia residue). Dalej: szerszy pass gęstości/whitespace albo iteracja na realnej wizycie podczas miękkiego startu.
- **Versum/Booksy reference audit + payment-on-calendar-block 2026-06-22.** Właściciel dostarczył zrzuty Versum (16) + Booksy (13) — przeanalizowane. Wniosek: SalonBW wiernie klonuje Versum (pulpit/klienci/usługi/magazyn/łączność/statystyki/grafiki), kalendarz po redesignie zgodny strukturalnie (day/week/month + recepcja, overlap side-by-side, godziny, now-line; B&W status vs Versum-szary/Booksy-teal). Realne luki (nie blokujące startu, 1-os.): faktury (Booksy ma pełne VAT — SalonBW tylko encja), zunifikowany POS z kartami podarunkowymi/pakietami/karnetami (rozproszone), obłożenie + prognoza w statystykach, per-customer Excel export (= „zaawansowane eksporty"), kolumna „Popularność" usług. Zrobione (owner wybrał „a"): **płatność na kafelku kalendarza** (`ef8414a04`, api+panel) — `mapAppointmentToEvent` niesie `paidAmount`+`paymentMethod`, EventCard pokazuje „Zapłacono N zł · metoda" na sfinalizowanych wizytach (jak Versum „Płatność: 320 zł karta"). Tylko kalendarz personelu (klient nie widzi). tsc+38 testów zielone; live niezweryfikowane wizualnie (brak sfinalizowanych wizyt na prod — wypełni się po finalizacji).
- **Admin/owner login + test-account cleanup + topbar/calendar polish 2026-06-22.** Podczas przeglądu właściciel zgłosił: białe prostokąty w topbarze, liczby kolejnego miesiąca w mini-kalendarzu, testowi pracownicy obok żony (Aleksandry). Zrobione:
  - **Visual** (`0d3b7c0e3`, panel): topbar — kontrolki (zadania/Pomoc/avatar) były gołymi `<button>` z domyślnym jasnym button-face (czytane jako „białe prostokąty" na ciemnym pasku) → transparent + jasne ikony/tekst (Heroicons currentColor) + subtelny hover. Mini-kalendarz (react-datepicker): ukryte dni `--outside-month` (znikają „1 2 3 4" z lipca) + zaznaczony dzień z cyan `#25b4c1` → brand black `#0d0d0d`. Zweryfikowane live (computed: outside vis=hidden, selected bg=rgb(13,13,13)).
  - **🔴 KLUCZOWE — prawdziwy login admina** (`42f56dd5c`, migracja `1761200000000`): odkryto, że właściciel NIE MIAŁ działającego konta admina — jedyny admin to była śmieciowa „Test Admin", a konto Aleksandry (29) miało email-zaślepkę `…@local.invalid` (nie mogła się zalogować). Owner wybrał „Aleksandra zostaje adminem". Migracja: user 29 → email `kontakt@salon-bw.pl`, role `admin` (cast `::"users_role_enum"`), hasło TYMCZASOWE (bcrypt hash w migracji; plaintext przekazany właścicielowi w czacie, do zmiany przy 1. logowaniu przez Konto→Zmień hasło — **brak self-reset flow** w panelu). Zweryfikowane live: login 200, role=admin, /settings/calendar 200.
  - **Usunięcie kont testowych** (`35e8f6d83`, migracja `1761210000000`) PO potwierdzeniu działającego admina: FK-safe cascade (wzorzec z czerwca) usuwa 47 (Test Admin) + 48 (Ola test). Zweryfikowane live: `/employees` = tylko Aleksandra (29, admin); test.admin login → 401. **PRACOWNICY = tylko Aleksandra.** Uwaga arch.: `calendar.service.getEmployees` filtruje `role='employee'` a renderowana lista PRACOWNICY używa STAFF_ROLES (admin+employee) — dlatego Aleksandra-admin dalej widoczna jako stylistka; available-slots znajduje pracownika przez employee_services (nie role), więc bookowalna mimo roli admin.
  - **Rada launch (advisory):** nie budować więcej funkcji — przed startem: decyzja o domenie (landing na dev, salon-bw.pl=stary), niezawodność powiadomień o rezerwacji (mail L2 działa, dodać 2. kanał + test na telefonie żony), backup bazy, deliverability maili (SPF/DKIM), Sentry DSN na prod, miękki start z Booksy jako backup.
- **Klient UX — test + 3 nowe funkcje 2026-06-22.** Test live (isolated context): „UMÓW WIZYTĘ" anon → modal logowania „Zaloguj się… aby przejść do kalendarza rezerwacji" (OK); pulpit klienta pokazuje wizyty bez paidAmount (constraint „nie widzi ile zapłacił" SPEŁNIONY). Zbudowane (owner wybrał 3 z 4 + mechanizm rabatu „% per klient + z grupy"):
  - **(1) „Umów ponownie"** (`e66db14e5`, api+panel): pulpit klienta — wizyty completed/cancelled/no_show mają przycisk → `/booking?serviceId=<svc>` (kreator już pre-selektuje usługę i skacze do wyboru terminu). Dodane `serviceId` do payloadu dashboardu (DTO+map+typ).
  - **(2) Kalendarzowy widok wolnych terminów** (`fbd5a9c5f`, panel): krok 2 rezerwacji ma przełącznik Lista/Kalendarz; `MonthCalendarPicker` (B&W siatka miesiąca, dni przeszłe + zamknięte-weekdaye z `/calendar/opening-hours` wyszarzone/przekreślone, klik → sloty dnia poniżej). Lista = natywny date input.
  - **(3) Stały rabat per klient/grupa** (`d8aa2a630`, api+panel, migracja `1761190000000`): `discount_percent` na `users` + `customer_groups`; `customers.findOne` dolicza `resolvedDiscountPercent` = własny % klienta, inaczej max z jego grup; admin ustawia „Stały rabat (%)" na karcie klienta (sekcja Zaawansowane); FinalizationModal pobiera `/customers/:id`, pre-wypełnia rabat (PLN = cena×%/100) + hint „rabat stały klientki X%", personel może edytować. Rezerwacja online klienta NIE rusza (tylko podpowiedź przy finalizacji). See [[client-standing-discount]]. **NIE zbudowane (świadomie, owner odznaczył):** ceny usług w historii wizyt klienta. **Follow-up (backend gotowy, brak UI):** edycja `discountPercent` na GRUPIE klientów wymaga pola w formularzu grupy (kolumna + resolver już działają; do dodania w UI grup). Testy: panel 25 + backend customers 12 zielone; tsc czyste.
- **Finalizacja + dane klienta — AUDIT (kod) 2026-06-21:** flow jest feature-complete vs wizja właściciela. FinalizationModal pokrywa: `paymentMethod` (wybór płatności), `discount`/`tip`, `usageMaterials` (z receptury) + `usageItems` (ręczne materiały zużyte), **`productSales` (ODSPRZEDAŻ — produkty kupowane przez klientkę, np. odżywki, osobno od materiałów)**, `additionalServices`, `note` (wewnętrzna) + `clientNote` (widoczna dla klientki — „co klient zobaczy"). AppointmentDrawer: kontakt klienta, historia wizyt, receptura, notatki dwukierunkowe. Wszystko live-zweryfikowane w czerwcowych sprintach (G1/G2, deducja magazynu, POS). Brak luk — nic do budowy.
- **Widok finansowy solo + wpływ rabatów DONE + verified live 2026-06-21** (`a9c99524f`, api+panel deployed). Właściciel: wyniki dzienne/tygodniowe + wpływ rabatów, BEZ prowizji (firma 1-os.). Backend: `RevenueDataPoint.discount` (suma `appointment.discount` per bucket) w `getRevenueChart`. Panel `/statistics`: snapshot solo (4 karty B&W) — Utarg dziś (+ liczba wizyt + śr./wizytę) / Rabaty dziś (% potencjalnego utargu) / Utarg ten tydzień / Rabaty ten tydzień + linie rabatów w CSV. Zweryfikowane live (karty renderują, 0 bo brak sfinalizowanych wizyt w zakresie — wypełnią się po finalizacjach). `discountPct = discount/(discount+revenue)`. Uwaga: nav „Prowizje" zostaje (istniejąca funkcja, nieszkodliwa dla 1-os.).
- **Double-booking (nakładanie wizyt) ENABLED 2026-06-21** (owner: 1-os. salon świadomie bookuje 2 na raz, np. w czasie farby). Backend (`c6895d073`, api deployed): `assertNoConflict` honoruje istniejący setting `allow_overlapping_appointments` (calendar_settings) dla wizyt tworzonych/przekładanych przez PERSONEL; rezerwacja online klienta (actor===client) DALEJ respektuje zajętość. Nowy `isOverlapAllowed()` (inject `CalendarSettings` repo do appointments.module/service). Migracja `1761180000000` włącza setting (owner-authorised). Testy: +staff-overlap-allowed, +online-self-booking-still-blocked, istniejący reject (self-booking) bez zmian — 28/28. Frontend (`092a6df98`, panel deployed): AppointmentDrawer.handleCreate robi best-effort `checkConflicts`; pierwszy konflikt → ostrzeżenie „Masz już wizytę w tym czasie…" + przycisk zmienia się na „Dodaj mimo to" (one-tap confirm, nie twarda blokada); stale-reset przy zmianie czasu/pracownika/usługi; błąd checkConflicts non-fatal. Setting jest też przełączalny w panelu (ustawienia/kalendarz). See [[appointment-overlap-setting]].
- **Calendar Versum-style redesign DONE + verified live 2026-06-21** (owner: „kalendarz UX wciąż zły, Booksy/Versum lepsze"). Owner chose **dopracowana siatka 1 zasobu** (bez kolumn pracowników — to FullCalendar Premium/płatne, bezsensu dla 1-os. salonu). Stack bez zmian (FullCalendar timeGrid + @dnd-kit już były). **Phase A desktop** (`d55ef918d` + 🔴`2358aeae0`): eventy kolorowane wg STATUSU w skali B&W (`getEventStatusVisual` w EventCard — confirmed=czarny, scheduled=slate, completed/cancelled=wyblakłe srebro, online/rescheduled-pending=bursztyn = jedyny semantyczny akcent; było per-employee blue #3b82f6 bez sensu); EventCard/CalendarHeader odbluszczone; FAB #1a1a2e→#0d0d0d; FullCalendar skin dopieszczony (nagłówki dni z datą+capitalize, today bold, czerwona linia „teraz", kropkowane półgodziny, miększy cień, dayHeaderFormat+scrollTime). **🔴 KLUCZOWY BUG (2358aeae0):** data NIE pokazywała się na desktopie — nagłówek pokazywał tylko zegar „19:30"; przyczyna: `.d-none .d-sm-block` — w bundlu panelu `.d-none !important` wygrywa kaskadę z `@media .d-sm-block !important` (kolejność), więc span daty miał `display:none` na każdej szerokości desktop. Fix = matchMedia + inline `display:block` zamiast kruchych klas responsywnych. (`.d-md-inline` na zegarze akurat wygrywa — dlatego zegar było widać.) See [[panel-d-sm-block-cascade-trap]]. Zweryfikowane live: desktop pokazuje „Niedziela, 21 Czerwca" (#0d0d0d, width 174px). **Phase B mobile** (`1cb7ff3b6`): mobilny kalendarz prowadził widżetami CRM (KPI/insights/follow-up/audyt — same zera) spychając agendę wizyt pod fold → owner na telefonie nie widział „co mam dziś umówione". Fix bez ruszania JSX: agenda owinięta w `.reception-mobile-agenda` + `order:-1` w reception flex-column (mobile), `::after` dzieli sekcję labelem „WGLĄD OPERACYJNY". Zweryfikowane live na 390px: „Brak wizyt na dziś" (agenda) na górze, potem dywizor, potem CRM. **Phase A.2 godziny salonu DONE + verified live** (`0d0f6f7c6` + test `07fe1752d`): nowy util `openingHoursCalendar.ts` (computeSlotBounds/toBusinessHours/isDayClosed); calendar.tsx fetchuje `/calendar/opening-hours` (apiFetch<{hours}>, non-fatal) → slot bounds z realnego grafiku (start 08:00 zamiast hardcoded 07:00), `businessHours` (FullCalendar wyszarza poza godzinami via `.fc-non-business`), banner „🔒 Salon zamknięty w tym dniu (niedziela)" na zamkniętym day-view. Zweryfikowane live: niedziela=banner+bounds, poniedziałek=brak bannera+09–16 białe/reszta szara. **UWAGA:** `/calendar/opening-hours` jest Throttler-limited (dostałem 429 przy testach) — panel ma graceful fallback do 07–21 przy 429/błędzie; ew. follow-up = nie-throttlowane źródło godzin dla zalogowanego panelu. Martwy dark-theme `.fc` blok w globals (~1526) nadpisany przez light blok (~2466) — zostawiony.
- **Booking-wizard a11y/brand plan VERIFIED COMPLETE 2026-06-21** (plan `wise-enchanting-teapot.md`). Edits już były w drzewie (commit `a2d0066c0`, `/booking`): `STEP_DEFINITIONS` + semantyczny `<ol>`/`<li>` z `aria-current="step"`, per-step `<h2>` + focus na sukcesie (`role=status aria-live=polite`), `role=alert` na błędach, `aria-busy` na submit, `aria-describedby` na dacie, `.booking-back-link`/`.booking-slot-btn` ≥44px touch target, usunięty duplikat `.btn-salon` (white-on-silver fail), zero purpli `#8b5cf6`. **Weryfikacja: eslint clean, `tsc --noEmit` 0, bookingPage test 4/4 pass.** Live health-check tego samego dnia: api healthz 200, /services/public = 60 bookable, /calendar/opening-hours 200, panel 307→login, landing(dev) 200 — system w pełni funkcjonalny. Brak otwartych pozycji kodowych; pozostałe to wyłącznie decyzje właściciela (stawki prowizji, NIP/REGON, realny numer WhatsApp do testu L3).
- **Cleanup/hardening sprint 2026-06-20** (po audytach pracownik+admin). Wykonane krok po kroku: (1) opening-hours hardening (`8e4c5e760`, wyżej w P3); (2) **dane salonu wpisane** migracją `1761170000000` (`2768d3753`, owner-authorised): branch_settings = „Salon Black & White", ul. Webera 1a/13, 41-902 Bytom, +48 723 588 868, kontakt@salon-bw.pl (NIP/REGON null — do właściciela); zweryfikowane live GET /settings/branch. (3-5) `DashboardLayout`/`data_protection` nested-form/dead-CSS — **wszystkie stale** (już nie istnieją, zob. P3). (6) **konta testowe 47/48/49 ZOSTAJĄ** — decyzja właściciela (przydatne do weryfikacji na żywo; test.admin@ hasło z migracji `1761160000000`). NIE zrobione (świadomie): reguły prowizji per usługa/pracownik (decyzja biznesowa właściciela — stawki), test WhatsApp na realnym numerze (brak numeru).
- **Edytor stawki prowizji (commission-base) 400 NAPRAWIONY 2026-06-20** (`e2218d532`, zweryfikowany live: PATCH=200 było 400). `UpdateCommissionBaseDto.commissionBase` nie miało żadnego dekoratora class-validator → globalny ValidationPipe (`forbidNonWhitelisted:true`) odrzucał body panelu jako „property commissionBase should not exist" → edytor stawki nigdy nie zapisywał. Dodane `@IsNumber/@Min(0)/@Max(100)`. **Lekcja: inline DTO bez dekoratorów = 400 przy forbidNonWhitelisted** (sprawdzić przy każdym nowym inline @Body DTO; response-type shapes są OK, tylko @Body przez ValidationPipe). Reszta inline DTO w employees/customers OK (to typy odpowiedzi, nie body).
- **Pełny audyt flow admina (live na prod) DONE 2026-06-20** (commity `cf302a45a` reset hasła, `979e00063` fixy+cleanup). Przejście funkcji admina przez API (test.admin@, hasło zresetowane migracją `1761130000000`). **Potwierdzono działa** (m.in. odblokowane przez enum-fix z 2026-06-19): suppliers CRUD, settings/branch PUT, employee create/role-change/delete, gift-card create/delete (soft→cancelled), loyalty program PUT, większość statystyk, ustawienia, logi RODO. Model uprawnień OK. **3 REALNE BUGI znalezione+naprawione** (rodzina entity≠DB drift przy synchronize=false, patrz [[typeorm-postgres-drift-traps]]): (1) 🔴 `/statistics/services` i `/statistics/tips` → 500: TypeORM `.orderBy('<camelCaseAlias>')` emituje alias bez cudzysłowów, Postgres składa do lowercase ≠ `AS "bookingCount"` → „column does not exist" (parse-time, 500 nawet na pustym zakresie). Lowercase aliasy (`visits`/`month`) OK. Fix = cudzysłowy w orderBy (bookingCount, tipsTotal). (2) 🔴 `POST /services` bez `description` → 500: kolumna `services.description` założona NOT NULL w pierwszej migracji 2024, a encja+DTO traktują ją jako opcjonalną → save pomija → NULL → violation. Seed Booksy nie trafiał (zawsze ustawiał description=name). Fix = migracja `1761140000000` DROP NOT NULL. (3) round-trip 400 na `/settings/online-booking` PUT (echo GET-a z nadmiarowymi polami — endpoint działa, payload do dopasowania, nie bug). **Test data SPRZĄTNIĘTE** migracją `1761150000000` (testowy gift-card NWHQ + defensywnie flowtest.emp@) + inline (supplier/employee/service hard-delete, loyalty przywrócone). Zweryfikowane live: stats services/tips=200, POST /services bez opisu=201, gift-card NWHQ=0, flowtest.emp=0, service 146=404. **Uwaga:** statystyki admina (np. ranking usług/napiwków) były martwe na prod do tej pory — panel statystyki→usługi/napiwki by się sypał; warto sprawdzić czy panel łapie 500 gracefully.
- **Pełny audyt flow pracownika (live na prod) DONE 2026-06-19** (commity `41589a190` reset hasła test, `911e8cb4a` enum fix, `57d2a1bfc` landing fallback + cleanup). Przejście całego flow przez API (test.admin@/test.ola@, hasła zresetowane migracją `1761100000000`, plaintext poza repo). **Zweryfikowane działa:** akceptacja/confirm, notatka wewn.+kliencka, reschedule personelu, finalizacja (płatność karta + materiał → **deducja stocku** prod 80 4000→3970 + zalecenia klienta), edycja notatek odbytej wizyty (czas/płatność zablokowane po `completed` — „Only scheduled or confirmed can be rescheduled"), zmiana statusu, **cancel = model „usunięcia"** (NIE ma hard-delete appointmentu), pracownik finalizuje WŁASNĄ wizytę, edycja profilu pracownika, dodanie produktu/zmiana stocku/usunięcie produktu, edycja klienta (pole `firstName/lastName/phone`, nie `name`). **Model uprawnień pracownika poprawny:** Ola 403 na cudzej wizycie (29), 200 na własnej (39). **2 REALNE BUGI znalezione+naprawione:** (1) 🔴 **landing pokazywał środę otwartą** — Aleksandra (tt id=2) ma śr wolne, ale testowy pracownik Ola (tt id=3) miał aktywny grafik pon–pt 09–17 zanieczyszczający unię publicznego `/calendar/opening-hours`; booking i tak był poprawny (śr = 0 slotów), psuł się tylko WYŚWIETLANY zakres. Fix = deaktywacja tt Oli → `wed: []`, godziny = dokładnie Aleksandra. See [[opening-hours-unions-all-timetables]]. (2) 🔴 **edycja grafiku (i settings/suppliers/deliveries/branches/employee-CRUD/gift-cards/stocktaking) zwracała 500 mimo zapisu** — `logs_action_enum` (natywny pg enum, synchronize=false) zamrożony na 14 wartości, a LogAction urósł do 59; bare `logAction` → 500 po zapisie (np. „Mój grafik" zapisywał ale błąd), try/catch callerzy (appointments/products/retail) → cicho gubili audyt RODO. Fix = migracja `1761110000000` `ALTER TYPE ... ADD VALUE IF NOT EXISTS` ×59. Zweryfikowane: timetable PATCH 500→200. **Reguła: każda nowa wartość LogAction WYMAGA migracji ADD VALUE.** See [[logs-action-enum-drift]]. **Landing fallback fix** (`57d2a1bfc`): statyczny no-JS/crawler fallback pokazywał envelope „09:00 - 19:00" pon–pt (śr otwarta); dodany `BUSINESS_INFO.weeklyHours` (realny grafik) przez `buildLines` → SSR HTML pokazuje „Zamknięte" w środę (zweryfikowane na dev contact/home). SEO JSON-LD był już poprawny. **Test data SPRZĄTNIĘTE** migracją `1761120000000`: appts 37/38/39 + children usunięte, prod 80 stock przywrócony →4000, telefon Oli →NULL. Zweryfikowane live: brak appts 37-39, stock 80=4000, Ola phone=None, opening-hours wed=[]. **DŁUG (opcjonalne hardening):** `opening-hours` mógłby unionować tylko pracowników z usługami online-booking, żeby przyszłe konta testowe nie psuły landingu; konta testowe 47/48/49 zostają (świadomie).
- **G2 dodatkowe usługi przy finalizacji DONE 2026-06-19** (live, commit `b7986f5f8`, migration `1761080000000`). Owner choice = line-items + wspólna prowizja. Staff dodaje dodatkowe usługi w modalu finalizacji (sekcja „Dodatkowe usługi", picker aktywnych usług, rabat per-pozycja) obok rabatu na całość. Backend: `appointment.extraServices` jsonb (denormalizowane name+price+discount), `FinalizeAppointmentDto.additionalServices[]`, finalize zapisuje + ustawia bazę prowizji = usługa główna (variant jeśli jest) + Σ(dodatkowe netto) przez nowy `baseAmountOverride` na `createFromAppointment`/`calculateAndSaveCommission` (przypadek jednousługowy bez zmian). Drawer pokazuje extraServices w podsumowaniu sprzedaży; klient NIE widzi pozycji rozliczeniowych (pulpit bez ceny — zgodnie ze spec). Verified live (appt 36): paidAmount 240.00 = 70 + (200−20) − 10, extraServices zapisane, discount 10.00. **Uwaga: prowizja = base×percent; percent zależy od reguł prowizji per usługa/pracownik — na teście percent=0 → amount 0 (brak reguły, nie bug G2).** Cała sekwencja realizacji (booking→zatwierdzenie→realizacja z notatkami/farbami/materiałami/płatnością/rabatem/dodatkowymi usługami/zaleceniami→deducja magazynu→klient widzi odbytą bez ceny→pracownik pełne detale) — KOMPLETNA. **Test data SPRZĄTNIĘTE** migracją `1761090000000` (commit `4b84cf428`): appts 31-36 + children + warehouse usages + L2 maile + client id 50 usunięte; product 213 stock przywrócony 19→39. Zweryfikowane live: appts 31-36 nieobecne, stock 213=39, login e2e-client-0618=401.
- **POS enabled on prod + visit-realization flow fully verified DONE 2026-06-18** (commits `cdf5fae99` POS+silent-failure, `f29ec9d36` cleanup). Owner chose: enable POS + fix silent-failure + G2=line-items+wspólna prowizja. **Part A DONE+verified live:** (1) `POS_ENABLED=true` — but a deploy.yml quirk wasted cycles: prod push-deploys read the STAGING GitHub-environment vars (job `environment:` resolves to staging while DEPLOY_ENV=production), so setting it only in production env did nothing; fixed by setting `POS_ENABLED=true` in BOTH staging+production envs + adding it to the `.env` generation from `vars.POS_ENABLED`. Verified via temp DIAG echo → `.env` has `POS_ENABLED=true`; POST /usage 201 (was 501). (2) **Stock deduction works:** finalized appt 34 with 20ml of product 213 → stock 39→19 ✓. (3) **Silent-failure fixed:** added `RetailService.assertUsageStockAvailable` pre-flight check before completing the visit — finalize with 100ml (only 19 in stock) → 400 „Za mało na stanie: …(dostępne 19, potrzebne 100)" and appt stays `confirmed` (atomic, not completed) ✓; post-commit deduction keeps a logged (not swallowed) safety net. See memories [[pos-disabled-no-stock-deduction]], [[deploy-env-binds-staging-vars-on-prod]]. **Part B (G2 dodatkowe usługi line-items + wspólna prowizja) — NOT yet built.** Plan: migration `extraServices` jsonb on appointment; FinalizeAppointmentDto `additionalServices[]`; finalize stores them + commission base = service.price + Σ(additionalService net) via a base override on `calculateAndSaveCommission` (currently base = `appointment.paidAmount ?? serviceVariant.price ?? service.price`, in-memory paidAmount is null at commission time so effectively service.price); FinalizationModal service-picker + per-service discount + summary; display in employee history + client (no price). **DŁUG test data prod:** appts 31-35 + planned usage U20260600001 + client id 50.
- **Full visit-realization flow test + finalize client-recommendations DONE 2026-06-18** (live on prod, commit `8148390e2`). Tested the whole flow (klient bookuje→pracownik zatwierdza→realizacja/finalizacja→klient widzi odbytą wizytę). **Built G1: client-visible recommendations at finalize** — added `clientNote` to FinalizeAppointmentDto → appended to client-visible `appointment.notes` (finalize's existing `note` stays → `internalNote`/staff-private); FinalizationModal now has two fields „Notatka wewnętrzna (tylko personel)" + „Zalecenia dla klienta (widoczne dla klienta)". Verified live (appt 32): finalize → status completed, paidAmount 60.00 cash, discount 10.00, internalNote=„Stan włosów… Farba 7.1 + 6% 1:1" (proporcje farb, prywatne), notes=client recommendation „Zalecenia: myć włosy co 3 dni…"; client dashboard shows appt jako **Zrealizowana, BEZ ceny, z zaleceniami** (dokładnie wg spec); admin/API widzi pełne detale (cena/płatność/rabat). **2 BLOKERY znalezione (decyzje właściciela):** (1) 🔴 **stock NIE schodzi** — cały retail/POS jest za flagą `POS_ENABLED` (default false, OFF na prod), więc `createUsage`/`createSale` rzucają „POS is disabled"; finalize łapie błąd usage w **non-fatal try/catch** (silent-failure) → finalizacja 201 ale stock 39→39 bez deducji. Żeby „materiały schodzą ze stanu" działało → `POS_ENABLED=true` w prod API .env (`scripts/safe-update-api-env.sh`) — włącza CAŁY retail (sprzedaż/zużycie/ruchy magazynowe). (2) 🟡 **G2 „dodanie kolejnej usługi z rabatem na całość/jedną"** — NIE zbudowane: Commission ma `@Index unique on appointmentId` (jedna prowizja na wizytę) → multi-service per appointment wymaga decyzji o modelu rozliczeń (line-items+wspólna prowizja / osobne wizyty / zmiana schematu). See memory [[pos-disabled-no-stock-deduction]], [[panel-appointment-notes-model]]. **DŁUG test data:** appt 32 = completed (test), klient id 50 — do migracji czyszczącej.
- **Client E2E test + booking/notes feature DONE 2026-06-18** (live on prod, commit `a2d0066c0`). Ran full client E2E (fresh account `e2e-client-0618`, id 50): ✅ rejestracja (z 3 zgodami), ✅ booking (kreator), ✅ modyfikacja = accept-reschedule (klient NIE przekłada sam — tylko akceptuje propozycję salonu lub anuluje; admin confirm→reschedule→klient „Akceptuj nowy termin"→Potwierdzona), ✅ anulowanie (Anuluj→ConfirmModal→Anulowana), ✅ edycja profilu (imię/tel), ✅ edycja zgód (email↔sms, utrwalone). Then built the 3 requested features: **(a) past-slot bug** — `getAvailableSlots` filtrował tylko po busy, oferował dzisiejsze przeszłe godziny → POST odrzucał „startTime must be in the future"; dodany `now+1h` floor (jak nearest-slot). Verified: dzisiaj po zamknięciu = `[]`, przyszły dzień = pełne sloty. **(b) notatka klienta przy rezerwacji** — `notes` opcjonalne w CreateAppointmentDto + controller przekazuje; kreator ma textarea „Uwagi do wizyty" na kroku potwierdzenia. **(c) dwukierunkowe notatki** na `appointment.notes` (≠ staff-only `internalNote`): klient pisze przy rezerwacji → personel czyta+dopisuje w drawerze kalendarza (nowy `ClientNoteSection`, widoczny dla KAŻDEGO statusu — bo `canShowFormulaSection` pokrywa tylko confirmed+, a notatkę trzeba czytać już przy online_pending) przez `PATCH /appointments/:id/client-note` (nowy endpoint + `updateClientNote`) → klient widzi na pulpicie „Ostatnie wizyty" (recentAppointments niesie `notes`). Bez migracji (istniejąca kolumna `notes`). Verified live: appt 32 — klient zapisał „Proszę o krótsze boki, alergia na amoniak", admin dopisał „[Salon] Użyto farby bezamoniakalnej…", klient widzi cały wątek na pulpicie. **Model notatek:** `notes`=wspólna notatka widoczna dla klienta (oba kierunki), `internalNote`=prywatna personelu. See memory [[panel-appointment-notes-model]]. **DŁUG: dane testowe na prod do sprzątnięcia** — konto klienta id 50 (`e2e-client-0618@example.com`) + appt 31 (Anulowana) + appt 32 (online_pending z notatką) → potrzebna migracja czyszcząca (brak DELETE endpointu; wzorzec jak `CleanupE2eTestArtifacts`).
- **Client-role panel FIXED + brand consistency pass DONE 2026-06-18** (live-verified, Lighthouse a11y 100). Commits `88be8f303` (client), `2252534dd` (primary retheme), `d4e396a9c` (chip/topbar a11y), `7eed37862` (topbar toggle contrast). Owner reported: client can't reach own visits + edit profile (403). **Root causes:** (1) `/account` gated on `permission="nav:calendar"` which the client role lacks → RouteGuard returns `<Forbidden/>` even though client was in the `roles` list (RouteGuard requires BOTH roles AND permission). (2) `visibleSalonModules('client')` was empty (client has no `nav:*` module perm) → empty rail, stuck on /booking. (3) `/dashboard`+`/account` resolved to the calendar module → leaked staff mini-calendar + PRACOWNICY sidebar. **Fixes:** dropped /account permission gate (self-service, roles-only); added `CLIENT_MODULES` (moje wizyty→/dashboard, rezerwacja→/booking, gated on client-exclusive `dashboard:client`) + `CLIENT_SHELL` (clean full-width); made `resolveSalonModule(path, role)` role-aware (clients → CLIENT_MODULE always, no staff shell) — passed role from SalonShell + SalonShellMobile; client lands on /dashboard (visits hub). Verified live: client rail = moje wizyty/rezerwacja, /account 200 + profile SAVE works ("Dane profilu zostały zapisane"), no PRACOWNICY leak. See memory [[panel-client-role-experience]]. **Brand consistency pass (same session):** (a) `--bs-primary` was default blue `#0d6efd` → every text-primary/bg-primary/border-primary leaked blue (loyalty/gift-cards KPIs/badges/tabs); rethemed to `#0d0d0d` in salon-theme.css `:root` (one edit, panel-wide; `.btn-primary` unaffected). (b) loyalty+gift-cards action chips had `bg-opacity-10 text-white` = invisible (white on 10% tint, 1.31:1) → text-dark. (c) topbar user-dropdown `aria-label` vs visible initials → label-content-name-mismatch; initials marked aria-hidden. (d) topbar toggles (Pomoc + avatar) sit on light #efefef pills but were #7a7068 (4.2:1) → `.navbar .dropdown-toggle` #1a1a1a. Result: Lighthouse a11y 96→**100** on logged-in pages (BestPractices 100). Menu sweep (owner ask): top menu (search/tasks badge/Pomoc/avatar) + side rail (7 monochrome modules + active highlight, no dodatki) + secondary navs all verified readable + on-brand. Semantic `text-success` (green status) kept per prior decision.
- **Dodatki (`/extension`) storefront RETIRED + features re-homed DONE 2026-06-18** (live-verified). Commits `9388922fd` (refactor) + `20f1d9009` (loyalty full-width fix). Owner: „sprawdź co warto zaadoptować i gdzie logicznie umieścić bez osobnej podstrony dodatki". Audit finding: `/extension` was a 1:1 Versum **storefront** clone (7 fake add-ons, invented SaaS pricing tiers solo/basic/medium/pro, trials, dead „wypróbuj za darmo" buttons, screenshots hot-linked from `app-cdn.versum.net`) — meaningless for a salon's own panel AND it was hiding 2 fully-built features behind a fake paywall. **3 of the 7 advertised add-ons are already implemented** (backend module + big panel page): Marketing Automatyczny (`/communication/automatic`, already in nav), Program Lojalnościowy (loyalty page, 1235 linii, was orphaned), Bony i Karnety (gift-cards page, 1588 linii, was orphaned). The 2 orphaned pages were reachable only by URL (their only linker `AdminSidebarMenu` isn't mounted) + nav-mapped to the junk extension module. Changes: deleted `pages/extension/*` + dead `ExtensionNav` + `nav:extension` permission + the `extension`/'dodatki' main-nav module (removed `SALON_MODULES[7]`, positional indices 0–6 unchanged); moved `/admin/loyalty`→`/loyalty` (Klienci module via `withShellOverride`, full-width — SalonSecondaryNav switches on `module.key==='customers'` so it also needed an explicit `pathname.startsWith('/loyalty')→content=null` guard or the customer-filter sidebar renders, whose buttons would push junk query params onto /loyalty); moved `/admin/gift-cards`→`/sales/gift-cards` (auto-resolves to Magazyn/Sprzedaż module, added „bony i karnety" to WarehouseNav SPRZEDAŻ group); ClientsNav got a „NARZĘDZIA → Program lojalnościowy" discovery `button.filter_link` (NOT a `<Link>` — that rendered Bootstrap-blue; button inherits dark sidebar styling); settings dashboard „Ustawienia dodatków"→„Narzędzia", fixed Marketing tile (`/extension/tools/4`→`/communication/automatic`), dropped „Premium"/storefront tile, added Loyalty + Gift-cards tiles. **Not adopted** (no backend / out of scope): Zasoby, Fiskalizacja (needs fiscal printer; only `invoice.entity` partial), Kalendarz Google (only `auth-social` scaffold), Ograniczenie dostępu (overlaps roles). tsc+eslint clean, 278/278 tests. Verified live: /extension=branded 404, no „dodatki" in rail, /loyalty full-width under Klienci, /sales/gift-cards under Sprzedaż w SPRZEDAŻ sidenav, settings Narzędzia tiles, discovery link dark `rgb(97,105,113)`. See memory [[panel-versum-clone-features-already-built]].
- **Usługi (karta usługi) module review DONE 2026-06-18** (live-verified). Two commits: `88c2d2b91` (table text), `b8922d7d8` (source label). Findings + fixes: (1) **Panel-wide table-text contrast bug** — the `.table-bordered` block in `salon-theme.css` (loaded first, #10) carried stale Versum DARK-theme text colors (th #b4b8be silver, td #c0b8af warm-taupe). `salon-shell.css:4034/4051` (loaded #11) redeclares `.table-bordered th/td` with LIGHT backgrounds (#f5f8fa/#fff/#fafafa) but NO `color`, so theme's dark-theme colors won for text → ~1.9-2.0:1 on the light surface, making every list cell (Kategoria/Czas/Cena/VAT) barely readable across ALL modules using `.table-bordered`. Fix = theme th→#6e7278 (silver-ink AA), td→#1a1a1a. Verified live on /services: th `rgb(110,114,120)`, td `rgb(26,26,26)`. NOTE: `salonbw-table` (the OTHER panel table class, used by the service-detail tabs) does NOT have this bug — its dark-theme leftovers at `globals.css:1891` are overridden by the later id-scoped `globals.css:2209` (th #495057, td #212529), which wins. So only `.table-bordered` needed the fix. (2) Comments tab rendered raw `ServiceReviewSource` enum (booksy/moment/internal) → "internal" leaked English into the Polish-only panel; added `formatReviewSource` map (Booksy/Moment/Wewnętrzny, falls back to raw). Service card otherwise clean across all 7 tabs (podsumowanie/statystyki/historia/przypisani pracownicy/komentarze/prowizje/receptura): neutral-gray active sidenav (not blue), dark breadcrumb, outline + silver-primary buttons, uppercase tracked silver-ink KPI labels, monochrome RevenueChart, proper empty states. See memory [[panel-table-bordered-dark-theme-trap]].
- **Klienci (customers) module brand sweep DONE 2026-06-18** (live-verified). Four commits: `b4d503075` (list view), `eaec01b67` (panel-wide links), `4b9aab73e` (employee badge), plus dashboard fixes earlier same day. Fixes: (1) Sidebar `.tree > .root` ("wszyscy klienci") was a direct `<button>` child of `.tree`, so the `.tree li button` UA-reset never matched it → browser-default `2px outset black` border (ugly box); added the reset to `.root`. (2) Sidebar active/hover used `color:var(--salon-accent)` = silver #b4b8be on light bg (1.99:1, fails WCAG) across root/li-a/li-button → switched to #0d0d0d on neutral #f0f0f1 (was bluish #eef3f7). (3) Quick-filter chips were Versum blue → re-skinned to brand `booking-cat-chip` pattern (white→silver-active, dark text, 2px radius). (4) Customer name links (`.row-title`) fell back to Bootstrap blue (rule was scoped only to `.list-table`) → added `.customers_index a.row-title` #1a1a1a. (5) **Owner decision: panel-wide content links dark (#1a1a1a) + hover-underline, NOT blue** — recoloured all 11 `#2b7bbd` link rules → #1a1a1a, added `.main-content` tel:/mailto: rule + scoped hover-underline affordance. (6) `.visit-employee a` badge was a fixed red (#fde8e8/#c54b4b) for every employee (not data-driven) → silver #e8e9eb/#1a1a1a. Note: customer-detail tabs (dane osobowe/statystyki/historia/komentarze/komunikacja/galeria/pliki) are in-page (only `[id]/edit.tsx` is a sub-route) so they inherit the same global CSS. See memory [[panel-link-color-dark]], [[panel-dashboard-css-duplicate]]. **Pulpit (dashboard) also DONE same day** — KPI mini-chart bars + positive-trend monochromed; needed fixing BOTH salon-shell.css and globals.css (globals wins cascade).
- **Service-category management page built 2026-06-17** (`57afb6a05`, panel) — page-by-page completeness pass (owner: unified layout/design/UX, complete functions). Found: backend `/service-categories` had full CRUD + the panel hooks existed (`useCreate/Update/DeleteServiceCategory` in useServicesAdmin), services assignable to a category in the service form, and landing+booking already group by category — but there was NO panel UI to add/edit/delete service categories (only product categories had `/settings/categories`). Built `/settings/service-categories.tsx` (list with per-category active-service counts, add/edit via Bootstrap-utility modal matching ConfirmModal, delete with confirm), added to SettingsNav ("Kategorie usług" + relabeled the product one to "Kategorie produktów"), fixed the product page's mislabeled `<title>`. Verified live full CRUD (create→list refresh→delete→confirm, no residue); counts 27/21/10/2 = 60 match the catalog. NOTE: reorder hook exists (`useReorderCategories`) but the page doesn't expose drag-reorder yet (flat 4-category list; add if needed). Remaining modules to sweep next: service detail tabs (recipe/commissions/employees/history), customer detail tabs, settings pages, per-role dashboard.
- **Panel UI/UX sweep (admin modules) 2026-06-17** — reviewed magazyn/statystyki/łączność live. Fixes deployed: (1) sidebar `.nav-list` carried Bootstrap `.nav` (display:flex row) so multi-item groups ran together (communication KAMPANIE: "Wiadomości masoweNewslettery") → forced `flex-direction:column` (`07d69d5d7`). (2) statistics charts used a bright Versum palette (green #88ca2a / blue / orange / purple) → recolored ALL chart hexes across 9 statistics files to a monochrome black→silver brand scale (`#0d0d0d`/`#6e7278`/`#9a9ea4`/`#3a3d42`/`#c4c8ce`/`#54585e`/`#87898f`); each source hue→distinct gray so no within-chart collision; text-stat colors use silver-ink #6e7278 (AA). Verified live: payment pie now black, not green (`5015ef19e`). Owner chose full monochrome over muted-accent. Non-issues confirmed: communication "Ładowanie wiadomości…" resolves to "Brak wiadomości" (not stuck); magazyn "0 mililitry (0 ml)" redundant unit is backend-formatted (left). Deploy note: the nav-fix run failed at the flaky backend smoke-test (403 warm-up) AFTER panel deployed; the chart run on top succeeded and carries both.
- **Data-sourcing audit (owner: "wszystko z bazy — config + robocze") DONE 2026-06-17** (`b0ffb755c`). Swept panel for hardcoded/mock business data. Result: all operational entities (customers/products/employees/services/appointments) AND config (categories, customer-origins, settings/salon/hours/payment-config, data-protection) are DB-backed via API; NO hardcoded salon contact/hours/mock arrays in the panel. Two picker consistency fixes (sourced-but-unfiltered, like the services case): service picker now `useActiveServices()`→`/services?isActive=true`; FinalizationModal product picker now `/products?isActive=true` (sales/new already used `includeInactive:false`). Genuinely-hardcoded (flagged, NOT operational): the `/extension` (dodatki) marketplace catalog — `TOOLS`/`PLANS`/`cards` consts in `pages/extension/*` are a static Versum-clone upsell page. Borderline-but-standard: payment-method options (cash/card/transfer) in `pages/sales/new.tsx` mirror a backend enum. Observation: warehouse has 821 active products (all isActive, no inactive pollution) — worth a real-vs-legacy look but not a sourcing issue.
- **Landing warm-cream → neutral B&W migration DONE + DEPLOYED 2026-06-16** (`76dc2a147`, dev landing): the `--brand-warm-*` tokens (warm-brown text on `#faf9f7` cream) were owner-flagged migration debt. Redefined the token VALUES in `globals.css` to a neutral cool-gray scale (bg `#f6f6f7`, text `#23252a`/`#5f6369`) so every consumer migrates at once; fixed the one hardcoded `#ede9e3` border in ValuesSection. Contrast re-verified (≥5.5:1 on `#f6f6f7`). Live tokens confirmed via computed style. Brand skill (`salonbw-brand/SKILL.md`) updated so "warm-cream" no longer reads as legacy-approved-keep.
- **Calendar review (3 roles) DONE + DEPLOYED 2026-06-17** (`5a263586b`, api+panel): walked /calendar as admin/employee/client. Findings + fixes: (1) 🔴 the "Nowa wizyta" service dropdown (shared `AppointmentDrawer`, admin+employee) listed all 165 services incl. pre-Booksy legacy dups/typos ("Rozjasnienie globalne wlosow Ola", "…Ola", "Farbowanie Koleston Perfect") because it maps unfiltered `/services` while booking uses `/services/online-booking`. Fix = migration `1761070000000` deactivates the legacy set (`isActive=false WHERE onlineBooking=false`) + drawer filters `isActive!==false` (keeps an existing visit's service visible). Verified live: active services 60 (was polluting with 81 now-inactive), dropdown shows clean 60. (2) ⚠️ client hitting /calendar → RouteGuard `Forbidden` page was English ("You don't have permission…") → translated to "Nie masz uprawnień, aby wyświetlić tę sekcję." (test updated). (3) ✅ confirmed B2 live: employee PRACOWNICY filter now populated; client correctly blocked. Retracted as non-bugs: header "00:00"/midnight = live clock + "default to now" (it was past midnight).
- **Multi-role panel UX/UI review DONE 2026-06-16** (live on prod, all 3 roles via test.admin@/test.ola@/test.klient@). Findings + fixes:
  - 🔴 **Employee "Mój grafik" was broken** — `GET /employees/:id` + `/employees/staff-options` were Admin-only → 403 → "Nie udało się pobrać grafiku". Calendar PRACOWNICY filter also empty (`GET /employees` 403). FIX (`employees.controller.ts`): opened the 3 GET endpoints to Admin+Employee+Receptionist; `:id` ForbiddenException unless admin OR own id; non-admins get PII-sanitized `toStaffView()` ({id,name,role,full/first/lastName}, NO email/phone/commissionBase); admin keeps full record. See memory [[salonbw-employee-self-read]] (id 1fb4e41d).
  - ⚠️ **B3 settings nav leaked to employee** — persistent shell auto-resolver (`SalonSecondaryNav`) renders `<SettingsNav/>` for any /settings/* route regardless of role, overriding the page's `useSetSecondaryNav(null)` (SalonShell falls back to auto when context nav is null). FIX: `SalonSecondaryNav` now returns null for `settings` module when `role!=='admin'`. (Was cosmetic — RouteGuard already blocked access.)
  - 🗑️ **A1 data pollution** — every staff/customer list was full of QA/automation junk (Browser Validator, Curl/Cypress/E2E/Final/QA/Test accounts). Migration `1761060000000-CleanupTestQaAccounts` deletes 27 junk `users` (staff 13,17,30,31,34,40,41,42 + ~19 customers), merges duplicate Marzena 12→10, KEEPS Aleksandra(29)+active test trio(47,48,49). Owner approved deleting even owner-domain accounts (admin@/kontakt@bodora.pl/gniewko@bodora.pl). Uses dynamic information_schema FK scan (repoint-then-delete; RESTRICT-safe; validated on pg16 container: keepers survive, junk appts/reviews removed, dangling created_by nulled). down()=no-op (irreversible; audit list embedded in migration).
  - **DEPLOYED + VERIFIED live 2026-06-16** (api `8d0d57f59`, panel build `XPbQvYT5szyy8fUZlAxcl`): Ola GET /employees/48 own=200 sanitized, /47=403; /timetables=200; "Mój grafik" page renders (month grid + edytuj grafik, no error); settings nav hidden for employee (only kalendarz+klienci in rail); staff list=3, customers=2 (Marzena merged). Root-cause of the schedule bug was the JWT user shape `{userId,role}` (no `.id`) — `jwt.strategy` now also returns `id` so `actor.id` resolves codebase-wide (also fixes timetable assertCanManage so employees can SAVE their own schedule). Deploy note: the FIRST attempt failed at the migration step (commit `887c4e7e8`) AFTER the panel bundle had uploaded → panel served HTML for a build whose _next chunks 404'd (blank for everyone); fixed by a clean `target=panel` dispatch. Migration needed 2 fixes before it passed: (1) merge unique-collision on customer_tag_assignments → savepoint+drop-dup, (2) grandchild FK (commissions→appointments) → recursive `pg_temp.cleanup_cascade_del`. See memories [[salonbw-employee-self-read]], [[salonbw-deploy-partial-panel-build-mismatch]].
  - **Cosmetic polish DONE + DEPLOYED 2026-06-16** (panel build `RyPtOYZcu49648Qot82nt`+`697c0c0a0`, verified live): (1) admin filter sidebars (customers GRUPY KLIENTÓW / WYBIERZ KRYTERIA, services categories) — the Versum `.tree`/`.simple-list` items are `<button>` not `<a>` so they showed UA chrome (bare outlined boxes); added a reset + matched them to the anchor styling, and forced `.list_container .simple-list` to `flex-direction:column` (base `.simple-list` is flex/nowrap for module tabs, which crammed the criteria into columns). (2) client `/booking` resolved to the default calendar module → leaked the staff mini-calendar + empty PRACOWNICY sidebar; added a dedicated `BOOKING_MODULE` (secondaryNav:false, no_sidenav) + `/booking` case in `resolveSalonModule`. (3) booking service cards hide description when it equals the name (Booksy flat services had description==name).
- **`'use client'` prohibition (Pages Router):** Remove from ALL panel components/hooks. Pages Router components are server-renderable by default; `'use client'` is App Router only. Removed from 73 files in this session.
- **Error handling contract:** Every `mutation.mutate()` / `mutation.mutateAsync()` call must have `onError: (err) => toast(...)`. Never log silently. Rolled out across all panel pages in this session.
- **Polish-only UI:** All user-visible strings must be in Polish. No English in buttons, labels, toasts, errors, placeholders. Translated all remaining English strings in this session.
- **`type="button"` required:** All `<button>` elements that are not `type="submit"` must explicitly declare `type="button"`. Untyped buttons inside forms default to submit.
- **No `href="#"` / `javascript:;`:** Use `<button type="button">` for interactive elements that aren't real navigation links.
- **`window.confirm()` / `window.alert()` → ConfirmModal / toast:** Native dialogs inaccessible and unstyled. All replaced in this session.
- **Modal semantics:** All modals require `role="dialog"` + `aria-modal="true"` + `aria-labelledby` (title id) + ESC closes + focus trap + focus restore on close.
- **SEO meta pattern:** Every public landing page needs title, og:title/description/url/image/locale/type, twitter:card, canonical. Services/gallery/contact also get JSON-LD Organization schema. `og:url` and canonical must use absolute URLs via `absUrl()`.
- **Form labels:** Every `<input>`/`<select>`/`<textarea>` must be linked to a `<label>` via `htmlFor`/`id` OR have `aria-label`/`aria-labelledby`. `aria-label` used when no visible label (icon buttons, table row selects).
- **Decorative icons:** `aria-hidden="true"` on all decorative FontAwesome `<i>` and inline SVGs. Icon-only buttons get `aria-label`.
- **Pagination landmark:** Wrap with `<nav aria-label="Paginacja">`. Active page: `aria-current="page"`. Form-based pagination: add `aria-label="Paginacja"` to `<form>`.
- **Nav `aria-current`:** All active nav items across ALL navigation components must have `aria-current="page"` (links) or `aria-current="true"` (filter/group buttons).
- **a11y form toggle:** Clickable `<div>` that shows/hides content → `<button type="button" aria-expanded={bool}>`. Caret icon gets `aria-hidden="true"`.
- **`rel="noopener noreferrer"`:** Required on ALL `target="_blank"` links. Security + performance.
- **a11y — `<form>` pagination:** Can't convert to `<nav>` without breaking form semantics. Add `aria-label="Paginacja"` to the `<form>` instead.
  Evidence: TimetableTemplatesPage and ActivityLogRoute use `<form>` for page navigation
- salonbw-btn → Bootstrap 5 btn btn-* migration: all done; dead CSS removed.
- Formula service: accepts Role.Admin + Confirmed status (was bug: 403 for admin, 400 for confirmed)
- `PanelTable` API: `columns[]` (label?, ariaLabel?, className?), `isEmpty?`, `emptyMessage?`, `children` (tbody rows).
- `PanelSection` API: `title?` (renders h2), `action?` (renders actions div), `children`, `className?`.
  - Use `title` prop only when h2 is at direct top level of section.
  - For form-wrapping pages: replace outer div with PanelSection, leave h2 inside form children — do NOT use `title` prop.
- VersumShell made persistent in `_app.tsx` via nesting-detection pattern.
- Next.js upgraded to 15.5.10 on panel/landing + root pnpm.overrides updated.
- Codex (`gpt-5.3-codex`, `reasoning_effort=low`) known to skip pre-commit lint checks.
  Evidence: commit `0e93a771` had lint errors (no-misused-promises, prettier) — always audit Codex commits with tabular format: Problem | Naprawiony?

---

## Stack reminders

- Panel: Next.js 15.5.10, pnpm, TypeScript, Bootstrap 5.3 (no Tailwind)
- Backend: Node.js, pnpm
- Host: MyDevil (FreeBSD, Passenger)
- CI: GitHub Actions (.github/workflows/deploy.yml)

---

## Versum source of truth

- Offline dump: `/Users/gniewkob/Repos/Scrapling/output/versum-dump-200`
- Required reading order (HANDOFF_PANEL_AGENT.md steps 1-9):
  1. HANDOFF_PANEL_AGENT.md
  2. VERSUM_DUMP_ALIGNMENT_2026-03-17.md
  3. IMPLEMENTATION_MATRIX.md
  4. INVENTED_BEHAVIOR.md
  5. VERSUM_CLONING_STANDARD.md
  6. AGENT_EXECUTION_PLAYBOOK.md
  7. ROUTE_INDEX.json
  8. UI_PATTERN_CATALOG.md
  9. DOMAIN_SCHEMA_INVENTORY.md
- DON'T use VERSUM_CLONE_COMPLETE_GUIDE.md — deleted (obsolete Sprint 1 doc)

---

## Panel layout architecture (VersumShell)

### Persistent shell pattern

`_app.tsx` → `PersistentShellWrapper` → `VersumShell` (mounted once, never unmounts for authenticated users).

- Topbar, main nav, secondary nav are persistent — only content and active elements change.
- Individual pages still call `<VersumShell>` internally — the nesting guard makes them transparent pass-throughs.
- `/calendar` is excluded from persistent shell (uses full document replacement via Calendar embed).

### Secondary nav

- **Auto-resolved** (default): `VersumSecondaryNav` reads `router.pathname` and renders the correct nav per module. No action needed in pages.
- **Custom nav** (3 customer pages only): call `useSetSecondaryNav(jsx)` hook **before any early return** in the page component. Do NOT pass `secondaryNav` prop to `<VersumShell>` — the persistent outer shell won't see it.
- Use `useLayoutEffect` (not `useEffect`) for secondary nav push to prevent first-render flicker.

### Rules

- DO call `useSetSecondaryNav` before `if (!role) return null` (Rules of Hooks).
- DON'T pass `secondaryNav` prop to per-page `<VersumShell>` — use the context hook instead.
- DON'T add `/calendar` to persistent shell (document replacement incompatibility).
- `if (!role) return null` guards in pages are harmless but redundant — remove when refactoring.

### Key files

- Persistent shell + PersistentShellWrapper: `apps/panel/src/pages/_app.tsx`
- Shell component: `apps/panel/src/components/salon/SalonShell.tsx`
- Secondary nav context: `apps/panel/src/contexts/SecondaryNavContext.tsx`
- Secondary nav auto-resolution: `apps/panel/src/components/salon/SalonSecondaryNav.tsx`
- Module routing map: `apps/panel/src/components/salon/navigation.ts`
- **Landing/booking polish DONE 2026-06-14** (all on dev landing + panel booking): hero floating hours card removed (`bc50fcb4e`); `/services` cennik widełkowy — flat Booksy services regrouped by concept (name before " – ") → "od {min} zł" (`bc50fcb4e`); `/services` filtered to `onlineBooking!==false` so legacy isActive duplicates stay off the price list (`285caef40`); legacy service_categories deleted, only Booksy 4 remain (`9e2909c79` migration `1761040000000`); category filter chips ("Wszystkie / Fryzjerstwo / Koloryzacja / Pielęgnacja / Przedłużanie") on `/booking` (panel, 60-service list) + `/services` (landing) — brand-styled silver-active chips (`3ed6c8fa9`). Owner decisions captured: hero hours card unneeded, keep 4 categories (no Fryzjerstwo sub-split), palette = B&W + silver/gray accent (brand skill governs, warm-cream = migration debt). **PENDING: full PL/EN/DE translation (`dotłumaczyć wszystko`)** — service detail pages (coloring/balayage/highlights ~10-17 hardcoded PL lines each, partial i18n) + legal pages (policy/privacy 60-80 lines each, ZERO i18n, fully hardcoded PL). Legal machine-translation is risky (owner/lawyer review needed); service+UI strings are safe to translate.
- **Full PL/EN/DE translation DONE 2026-06-14** (owner: „dotłumaczyć wszystko", legal maszynowo z założeniem korekty prawnika): service detail pages (coloring/balayage/highlights) → per-locale `src/i18n/serviceDetail.ts` (eyebrow/h1/lead/items/SEO meta/OG/JSON-LD, og:locale switches); legal pages (/policy, /privacy) → structured per-locale `src/i18n/legalContent.ts` (sections of p + lists with bold lead-ins) rendered by shared `components/LegalArticle.tsx`; EN/DE legal carry an on-page italic notice „PL is the legally binding version — convenience translation pending review". /services filter chip labels → `SERVICE_FILTER` locale map. Verified live on dev: /policy switches PL↔EN with review notice + preserved structure. Commits `a4d0a7fe0` (services), `fd13ed4a0` (legal), `6011920a8` (chips). NOTE: legal EN/DE are machine-assisted — flag for professional legal review before relying on them. Panel stays Polish-only by project rule (booking chips not translated).
- **Employee self-service schedule DONE 2026-06-16** (owner chose "Employee edits OWN timetable"): backend (`59a7c9afd`) — `timetables` POST/PATCH/DELETE + exception PATCH/DELETE now allow `Role.Employee`; service `assertCanManage` enforces employee.id === timetable.employeeId (Admin/Receptionist unrestricted); GET `/timetables` scoped to self for employees; **approve leave request stays Admin-only**. Panel (`d02d0e45d`) — topbar user menu shows "Mój grafik" (employees only) → `/schedule` (redirects to `/settings/timetable/employees/<own id>`); the timetable page now allows role employee (RouteGuard `['admin','employee']`, dropped `nav:settings`), clamps the URL to the employee's own id, hides the staff-picker sidenav for them. Admin flow unchanged. **Verify by logging in as an Employee-role account** (e.g. Aleksandra if her account is Employee not Admin) → topbar → Mój grafik → edit hours → save. Role-capability audit recorded: Employee can fully manage appointments (create/reschedule/status/finalize) + customers (CRUD minus delete); cannot touch services catalog/products/stats/communication/settings/employee-params (services-assignment + commission rules + role changes are Admin/Receptionist).

- **Modal-unify + services-module review 2026-06-17** (`b93748753`). Owner asked to apply landing-style modals where professional. Judgment applied: modals for simple/quick forms, full pages for complex ones. Converted PRODUCT categories add/edit from separate `/settings/categories/new` + `/[id]/edit` pages to an in-page modal (name + parent dropdown, add-subcategory pre-sets parent), matching the new service-category modal — both category managers now share one UX; kept delete + reorder; standalone routes still work but aren't the primary path. Fixed product page `<title>` ('usług'→'produktów'). NOT modalized: service CREATE (`/services/new`) is a rich full-page form (variants/recipe/photos/advanced timing) — appropriately a page; service EDIT + variants already use modals. OPEN FINDING (next): admin `/services` catalogue lists all 141 services incl. the 81 deactivated legacy ones with NO active/inactive indicator → clutter; add a status badge + a 'pokaż nieaktywne' toggle (default hide inactive).
- **Services catalogue active/inactive filter DONE 2026-06-17** (`b288638f2`): admin /services now hides the 81 deactivated legacy services by default (pagination 141→60), with a "pokaż nieaktywne (81)" toggle + "nieaktywna" badge. Verified live: clean 60 canonical.
- **Customer-card visits box coherence DONE 2026-06-17** (`c7670ab7f`): right box was "zrealizowane wizyty: {completedVisits=1}" above a 3-item recent-visit list (any status) → retitled "ostatnie wizyty: {totalVisits}" + "zrealizowanych: N" sub-line. Added totalVisits to the local CustomerSummaryStats type. Verified live on Marzena: 4 total / 1 completed / 3 shown — reconciles.
- **Customer module COMPLETE 2026-06-17** (`8f9f3db1e`+`55ad848a5`). Walked all tabs: podsumowanie (visits box coherence, prior `c7670ab7f`), dane osobowe (complete read-only), statystyki (recoloured bars/amounts/total-visits to monochrome — earlier statistics recolour missed customer components; semantic ok/cancelled/noshow status colours kept), historia wizyt (rich timeline, counts coherent 4=3+1), komentarze, komunikacja (channel matrix + RODO), galeria + pliki (good empty states). Also CustomerSidebar active-tag #008bb4→#0d0d0d. CI note: 2 deploy runs hung ~25min on the GitHub-runner pnpm install (transient registry issue); cancel+re-dispatch cleared it. Next module: settings pages (Dane salonu / Płatności / RODO).
- **Settings module review 2026-06-17** (`d02c6e359`+`c4a589392`). 🔴 ROOT-CAUSE BUG: Dane salonu (`/settings/branch`) returned 500 on every load — the branch_settings entity's `postalCode` column had no explicit `name:`, so TypeORM queried camelCase `postalCode` while the table column is snake `postal_code` (every other multi-word col sets name:; this one was missed; no naming strategy in datasource). Added `name: postal_code` → 200, form loads. Also: surfaced English raw API error → Polish; RODO/privacy page GDPR contact was placeholder `salon@example.pl` → `kontakt@salon-bw.pl`; settings dashboard colourful Versum illustrations → grayscale filter (on-brand B&W), cyan hover→neutral. Verified live: branch 200, icons monochrome, form renders. Other settings pages OK: Godziny otwarcia (per-day toggles), Płatności (Moment Pay — orange partner logo kept), Rezerwacja online (full config). Note: salon name still default "Salon Beauty & Wellness" (owner to set). Remaining quick-checks: Logi RODO / Ochrona danych / Kalendarz / SMS / Komentarze.
- **Settings module COMPLETE 2026-06-17**: reviewed remaining pages, all functional — Logi RODO (audit log w/ logins), Ochrona danych (protection-mode toggle), Kalendarz settings (view/hours/slot/back-edit), SMS i łączność (Premium/Standard plans + advanced), pola klientów (Grupy/Pola dodatkowe/Pochodzenie), Komentarze klientów (reviews, empty state). All settings fixes deployed+verified live (branch 500 fix, grayscale icons, Polish error, real RODO email). Module done.
- **Topbar redesign + self-service consents 2026-06-17** (`b474cb0dc`/`05bd34a44`). Owner: top-right had off-brand colourful boxes + thin/non-functional menu. Redesigned per brand+UX skill: user initials → circular SILVER avatar (was blue rounded-square #eff7ff); task/notification count badges → silver + HIDDEN when count=0 (no more pointless green "0"); in-menu count chip → black. User dropdown → proper identity card (avatar + name + role + email) with "Edytuj profil i zgody" / "Zmień hasło" / [Mój grafik] / "Wyloguj". `/account` gained a "Zgody marketingowe i kontaktowe" section (email + SMS toggles → PATCH /users/profile/consent) — verified live: save works + persists across reload. Contrast follow-up: dropdown bg is #1c1c1c (dark) so card text switched to light (white name / silver avatar) — was invisible at first. All verified live. (Backend UpdateConsentDto already existed: smsConsent/emailConsent.) Possible follow-up: dashboard KPI mini-charts still have small blue/green accents.
- **Self-service profile edit DONE 2026-06-18** (`9d491742f`, api+panel): owner wanted /account to be a real profile editor, not read-only. Added backend `PATCH /users/profile` (name+phone, own profile, all roles) + `usersService.updateProfile` + `UpdateProfileDto`; exposed lightweight `refreshProfile()` in AuthContext (profile-only refetch via fetchProfile, no token refresh / no logout-on-error) so topbar syncs after save; added `phone` to panel User type + testUtils mock. /account "Dane profilu" section now edits name + phone (email/role read-only, admin-managed) above the consents + password sections. Verified live: set phone → "Dane profilu zostały zapisane" → persists across reload (CSRF handled by panel apiFetch; curl gets 401 CSRF as expected).
